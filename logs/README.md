# Logs

Background job logs for groundrails.

- `full-gold-eval.log` - groundrails grounder run over the full gold-v2 dataset (equivalence/metrics check; aggregate metrics only, no client text)
- `joint-wirings-benchmark.log` - lexical+semantic wiring benchmark (experiments/grounding-semantic/joint_wirings.py); live lexical pass + cached cascade scores; aggregate metrics only, no client text
- `semantic-live-smoke.log` - live `--semantic` cascade smoke (pulls the int8 IRs, scores a supported + a fabricated public claim); verifies the serving path end-to-end
- `cascade-score-gold-v3.log` - OV int8 cascade scored over all 7,976 gold v3 pairs (golden + synthetic aug) -> `data/processed/golden_v3_cascade_scores.parquet`; aggregate progress only, no client text
- `lex-pass-gold-v3.log` - lexical manifold (effort=high) pass over gold v3 for `lex_p` -> `data/processed/golden_v3_lex.parquet`; aggregate progress only, no client text
- `vitaminc-combined.log` - combined grounder (lexical + cascade) scored over the 800-row VitaminC slice of gold v4 -> `reports/grounding_vitaminc_combined.md`; aggregate metrics only, public benchmark
- `joint-premise-score.log` - Round 4 joint-premise (SummaC) NLI recompute over the 3,215 cascade-fired gold v3 rows -> `data/processed/golden_v3_joint_premise.parquet`; aggregate progress only, no client text
- `sat_vs_regex_fair_units.log` - fair SaT-vs-regex re-run, units step: per-extractor segmentation of raw_v5 + label inheritance from golden_v5; aggregate counts only, no client text
- `sat_vs_regex_fair_judge.log` - fair SaT-vs-regex re-run, dual-LLM judge (haiku + sonnet) over the new-to-judge segments per extractor; hygienic `claude -p`, aggregate progress only, no client text
- `judge-venv-install.log` - one-off build of the isolated judge venv (`~/.venvs/groundrails-judge`): vLLM + groundrails + SaT deps, for the open-model LLM judge on the local Blackwell GPU
- `llm-judge-model-download.log` - HuggingFace snapshot download of the open judge models (Qwen2.5-72B-Instruct-GPTQ-Int8 + Mistral-Small-3.2-24B-Instruct); progress only, no client text
- `llm-judge-w4a16-download.log` - HuggingFace snapshot download of `RedHatAI/DeepSeek-R1-Distill-Llama-70B-quantized.w4a16` (~39.5GB INT4 primary judge for the vLLM batched backend); progress only, no client text
- `llm-judge-smoke.log` - vLLM w4a16 engine smoke on the Blackwell: load + FP8 KV cache + real 15K-prompt JSON validity + batched throughput (chunks/min, agg tok/s) + VRAM teardown; synthetic prompts only, no client text
- `llm-judge-diag.log` - preemption isolation diagnostic: SHORT vs LONG (15K-prompt) batched throughput at `max_num_seqs` below the KV ceiling; refuted preemption (LONG still 12 agg tok/s), localized the cost to prefill; synthetic prompts only, no client text
- `llm-judge-prefill.log` - prefill-lever sweep (`max_num_batched_tokens` x `kv_cache_dtype`) over the LONG 15K-prompt workload to find what restores prefill throughput; synthetic prompts only, no client text
- `llm-judge-fp8.log` - FP8-weights prefill test (`Qwen2.5-72B-Instruct-FP8-dynamic`) vs the INT4 baseline, to confirm FP8 tensor cores fix the compute-bound prefill; synthetic prompts only, no client text
- `llm-judge-32b.log` - 32B FP8 prefill test (`Qwen3-32B-FP8`) at high concurrency, testing the model-size lever; FAILED on sm_120 DeepGEMM scale-factor bug (block-scaled FP8 checkpoint); synthetic prompts only, no client text
- `llm-judge-27b.log` - 27B FP8-dynamic prefill test (`gemma-3-27b-it-FP8-dynamic`, per-tensor FP8) at high concurrency, re-running the model-size lever with a Cutlass-path checkpoint; synthetic prompts only, no client text
- `llm-judge-marlin-fp8.log` - INT4-weights + FP8-Marlin prefill test (cached `DeepSeek-R1-70B-quantized.w4a16` with `VLLM_MARLIN_INPUT_DTYPE=fp8`), testing whether the FP8-Marlin GEMM path lifts INT4 prefill off the FP16-dequant floor (654 tok/s) toward FP8 speed at INT4 footprint; FAILED - act-order checkpoint (`has_perm=1`) rejected by FP8-Marlin repack; synthetic prompts only, no client text
- `llm-judge-nvfp4-download.log` - HuggingFace download of `nvidia/Llama-3.3-70B-Instruct-NVFP4` (~38GB, native FP4 tensor-core checkpoint via ModelOpt) for the NVFP4 prefill test; progress only, no client text
- `llm-judge-nvfp4.log` - NVFP4 prefill test (`nvidia/Llama-3.3-70B-Instruct-NVFP4`, `modelopt_fp4` + cutlass GEMM backend) on the LONG 14K-prompt workload; native FP4 tensor cores measured 2094 tok/s prefill (3.2x INT4-Marlin, 2.2x FP8) at ~40GB; synthetic prompts only, no client text
- `llm-judge-docker-pull.log` - `docker pull vllm/vllm-openai:cu130-nightly`, fetching the nightly cu130 image to test fresher FP4 kernels/scheduler against the pinned-venv 2094 tok/s NVFP4 baseline; pull progress only
- `llm-judge-docker-nvfp4.log` - NVFP4 prefill benchmark inside the cu130-nightly container (same LONG 14K workload, model staged into a docker volume, captured via `docker logs`); nightly kernels (`fuse_act_quant`) measured 2952 tok/s sustained / 4750 peak, +41% over the pinned-venv 2094; synthetic prompts only, no client text
- `llm-judge-docker-nvfp4-tuned.log` - concurrency-tuned nightly NVFP4 run (`max_model_len=15360`, `max_num_seqs=18`, `max_num_batched_tokens=24576`); held at 2911 tok/s (vs 2952), zero preemptions - refutes concurrency/chunk size as a lever, NVFP4 prefill is GEMM-kernel-bound; synthetic prompts only, no client text
- `llm-judge-r1-nvfp4-download.log` - HuggingFace download of `Kaleto/DeepSeek-R1-Distill-Llama-70B-NVFP4` (confirmed NVFP4, ~40GB), the fast-prefill R1 primary-judge checkpoint replacing the slow INT4-w4a16; progress only, no client text
- `llm-judge-venv-fuseact.log` - venv-parity test: vLLM 0.24.0 with `compilation_config.pass_config.fuse_act_quant=True` on Llama-3.3-NVFP4; the flag exists in 0.24.0 and lifts prefill 2094 → 2669 tok/s (+27%, no Docker); synthetic prompts only, no client text
- `llm-judge-venv-pureprefill.log` - pure-prefill ceiling (venv+fuse_act, gen=1, N=32) to isolate the prefill rate from the decode tail; sustained 2542, peak 3947, sawtooth oscillation confirming 300W power throttling; synthetic prompts only, no client text
- `llm-judge-r1-nvfp4-run.log` - the actual R1 judge model (`Kaleto/DeepSeek-R1-Distill-Llama-70B-NVFP4`, fuse_act, gen=256, N=32) on a realistic workload; confirmed R1-NVFP4 loads + NVFP4 cutlass kernel engages, prefill ~3176 tok/s instantaneous; synthetic prompts only, no client text
- `llm-judge-phase1-smoke.log` - Phase 1 smoke via nbconvert (12-trace sample, PHASE2=0) of the rewired NVFP4 notebook; validates the R1-NVFP4 + Gemma-FP8 dual-judge pipeline end-to-end (valid JSON, preliminary kappa) before the full run; aggregate metrics only, no client text
- `llm-judge-run.log` - full execution of `notebooks/dataset/01-kj-llm-judge.ipynb` (open dual-judge: Phase 1 validation vs gold, Phase 2 extension over the unlabeled traces); aggregate metrics only, no client text
- `llm-judge-judges-redownload.log` - re-fetch of the two judge models (`DeepSeek-R1-Distill-Llama-70B-quantized.w4a16` + `gemma-3-27b-it-FP8-dynamic`, ~65GB) after the benchmark-cache cleanup cleared them; download progress only
- `judge-bakeoff-download.log` - HuggingFace download of the judge bake-off candidates (`Llama-3.3-70B-Instruct-FP8-dynamic`, `Qwen2.5-72B-Instruct-FP8-dynamic`, `gpt-oss-120b` MXFP4, ~200GB) that replace the failed R1 judge; download progress only
- `pipeline-cpu.log` - retrieval pass of `notebooks/dataset/01-kj-grounding-dataset-pipeline.ipynb` (SaT segment -> window -> GPU-batched semantic retrieve -> retrieval-recall gate, judge stage gated off via RUN_JUDGE=0); aggregate progress only, no client text
- `judge-<candidate>.log` - judge bake-off pass of the pipeline notebook for one open judge (`gemma`, `llama70`, `qwen72`, `gptoss-low`, `gptoss-high`), one model per process; writes `verdicts_<candidate>.parquet`; aggregate metrics only, no client text
- `judge-fleet.log` - sequential driver over the remaining bake-off candidates (llama70 -> qwen72 -> gptoss-low -> gptoss-high); one model per process, continues past a single failure; per-candidate logs in `judge-<candidate>.log`
- `judge-<candidate>-progress.log` - live per-request judging progress teed straight to disk from the judge cell (bypasses nbconvert's cell-stdout buffering); `tail -f` to watch a candidate advance prompt-by-prompt; progress only, no client text
- `judge-val.log` / `judge-val-<candidate>.log` - post-bakeoff validation: re-runs gptoss-low + gemma on a 20-trace sample with the fixed config (robust regex parser, CHUNK=5, raw-text capture) into `dataset_pipeline_val/`, to confirm parse jumps to gate-passing before the full re-run; aggregate metrics only, no client text
- `judge-val2.log` / `judge-val2-gptoss-low.log` - second validation pass confirming the `max_tokens=3072` fix lifts gpt-oss-low to 1.000 parse on the sample; aggregate metrics only, no client text
- `judge-rerun.log` / `judge-rerun-<candidate>.log` - full corpus re-run of gptoss-low + gptoss-high with the validated config (robust parser, CHUNK=5, max_tokens=3072) that produces the clean gate-passing verdicts for the final dataset; aggregate progress only, no client text
- `nllb-download.log` - HuggingFace snapshot download of `facebook/nllb-200-3.3B` (dedicated open MT, full Nordic coverage) for notebook 03's frontier-free cross-lingual augmentation; download progress only
- `augment-*.log` - notebook 02 cross-lingual MT augmentation (NLLB translate + back-translation/bge-m3 fidelity gate) building the 8000-claim curated dataset; aggregate progress only, no client text
- `smoke-oss-judge.log` / `smoke-oss-ensemble.log` / `smoke-oss-medium.log` - gpt-oss judge validation before the full re-run: raw-prompt vs chat-template@low/high/medium on a labeled gold sample, proving the chat template + real reasoning_effort beats the old inert raw-prompt path and that low+medium is a genuine diverse ensemble; aggregate metrics only, no client text
- `judgeval.log` / `judgeval-gptoss-*.log` - fixed-judge end-to-end validation on a 20-trace sample (chat template, length bucketing, no-truncation loop with the 0-capped assert) before committing the multi-hour run; aggregate metrics only, no client text
- `rejudge-base.log` / `rejudge-base-gptoss-*.log` - full base re-judge (5857 claims) with the corrected diverse ensemble; gptoss-high hit the no-truncation assert (11 runaway prompts >17k tokens), gptoss-low clean; aggregate progress only, no client text
- `rejudge-medium.log` / `rejudge-base-gptoss-medium.log` - full base re-judge with gptoss-medium (the stable diverse partner replacing the runaway high), 0 truncation; produces the clean records_open ensemble; aggregate progress only, no client text
- `judge-gptoss-medium-progress.log` - live per-request medium-judge progress incl. the no-truncation round log (capped→0); progress only, no client text
- `adjudicate-ambiguous-rulebook.log` / `adjudicate-303-rulebook.log` - rulebook-informed gpt-oss high-effort adjudication of the ensemble-vs-gold disagreements (58 ambiguous + 303 clear-factual), classifying each gold-error vs ensemble-fluke; aggregate metrics only, no client text
- `shape-bench.log` - architecture wall-clock benchmark for `reports/research-grounding-architecture.md`: matched-parameter encoder shapes (53L/512d through 8L/1536d, plus attention-only and parallel-block variants) timed eager and CUDA-graph captured at batch 3 x 512 tokens bf16 on the RTX PRO 6000; random weights, shape stand-ins only, no data and no accuracy measurement. Script `experiments/grounding-semantic/arch_shape_latency_bench.py`
- `shape-bench2.log` - companion sweep for the same report: sequence length 256-1024, candidate specs at ~143-178M body, batch scaling 1/3/8/16, and the MICE-style interaction-layer cost on claim-only tokens; random weights, timings only. Script `experiments/grounding-semantic/arch_shape_seq_bench.py`
- `DR_targeting_build.log` - DR shared targeting module: RAGTruth span-histogram fit, KS/chi-square validation, 500-seed smoke of sample_spans
- `DR_pilot_gen.log` - DR-2 pilot chain (GPU0): DR-H116 splice-only sub-gate (150 long-form docs, splice integrity + degeneracy) followed by pilot-scale generation of the DR lane (H112 ~31k spans, H113 ~5.6k typed swaps, H114 ~23k blinded decodes) with degeneracy gates and bidirectional NLI. Markers `=== DR-H116 SUBGATE DONE ===` and `=== DR PILOT GEN DONE ===`. Public corpora only; metrics and progress, no client text
- `R10_lane_campaign.log` - R10 lane training chain (GPU1): H107 procedural-doc-register then H108 quantitative-nearmiss-register, two draws each, every draw followed by the truncated decomposed-min read and the PRIMARY windowed read through the frozen R8-H77 arena gate. Markers `=== H107 LANE DONE ===` and `=== H108 LANE DONE ===`, per-stage `=== FAILED: ... ===` on a crash. Script `experiments/grounding-semantic/R10_lane_campaign.sh`; public corpora only, metrics and progress
- `DR_pilot_longform.log` - DR-2 long-form top-up (GPU0) after the main session adjudicated DR-H116 SURVIVES: ~6.2k long-form H112 spans over 256-2048-token pysbd documents with exact char-offset ledgers, deduped against the pilot lane, splice integrity re-verified, bidirectional NLI. Final marker `=== DR LONGFORM TOPUP DONE ===`. Public corpora only; metrics and progress, no client text
- `R12_label_ceiling.log` - R12 label-ceiling diagnostic (CPU, ANALYSIS ONLY under Round 12 ruling 5): what mean AUC a perfect per-(sentence, window) entailment oracle reaches on the blind arena under RAGBench's labels, decomposed into four nested oracles (annotation AND, + H92 splitter, + documents[:8] cap, + 1500/750 windows). Script `experiments/grounding-semantic/R12_label_ceiling.py`; arena annotations read for measurement only, no derived quantity enters any build
- `R12-H121_gateBC.log` - R12-H121 DISTRACTOR-WINDOW joint Gate B + Gate C (CPU, zero GPU): 2,000-window candidate sample of same-document support-free negatives built from the TRAINING corpora only, certified by the torch-free lexical tier plus n-gram and numeral exclusion, across five filter strictness settings; reports auto label purity, lexical separability AUC on a held-out half, per-source distribution and the RAGTruth <=50% cap arithmetic. Script `experiments/grounding-semantic/R12-H121_gateBC.py`; public corpora only, the arena is never read
- `R13_cpu_gates.log` - Round 13 CPU admission gates (zero GPU): public-dataset fetch (WiCE from github.com/ryokamoi/wice, SciFact from the AI2 S3 release, cached under `data/external/datasets/`), then the canonical provenance instrument (`experiments/grounding-semantic/provenance_gate.py`, ruling 4) driving `R13-H128_gates.py` (WiCE bidirectional 13-gram containment on claim and evidence sides, yield and license gates, 200-pair construction sample) and `R13-scifact_gates.py` (SciFact 8-gram Jaccard >= 0.3 against pubmedqa/covidqa/expertqa, ruling 2). Public corpora only, counts and verdicts, no client text
- `R13_reads_gpu0.log` - R13 read-amendment instrument (GPU0): per-(sentence, chunk, window) score dump on the frozen R10-H108 draw1/draw2 and R9-H105 draw2 checkpoints, 77,171 pairs each, three-level indices retained (`doc_idx`, `win_in_doc`, `win_idx`) so within-chunk aggregation is implementable. Script `experiments/grounding-semantic/R13_reads_dump.py`, stage-1 helpers imported byte-identical from the R12-H121 Gate A dump; H105 draw1 reuses the banked Gate A matrix. Public arena only, scores and progress
- `R13_H125_gpu0.log` - R13-H125 TOP2-UNION-PREMISE-READ composite scoring (GPU0): 18,392 (sentence, composite) forwards on H108 draw1 - one top-2 composite per sentence on all ten subsets plus the exhaustive pair set on hotpotqa/covidqa/tatqa/hagrid only (11,928, amendment 3). Script `experiments/grounding-semantic/R13-H125_union.py`; reproduction guard, per-subset deltas, fire rates and verdict at the tail
- `DR_campaign_d<draw>_<arm>.log` - DR lane training campaign (GPU1), one draw of one arm (control = BCE only, margin = R11-H117 hinge) followed by the truncated decomposed-min read and the PRIMARY windowed read. `d1_margin` additionally hosts the R12-H120 instruments (EMA buffer decay 0.999 from 80% of the epoch, always-on step-cosine over the final 20%). Final marker `=== DR D<draw> <ARM> CAMPAIGN COMPLETE ===`, per-stage `=== FAILED: ... ===` on a crash. Script `experiments/grounding-semantic/DR_campaign.sh`; public corpora only, metrics and progress
- `fetch_edgar-restricted.log` - EDGAR-CORPUS restricted-slice acquisition (CPU, network): year-2020 shards from `eloukas/edgar-corpus`, S&P 500 constituent exclusion list built from `fja05680/sp500` x the SEC ticker-to-CIK map, per-shard filter counts and the total kept. Script `scripts/fetch_register_corpora.py edgar-restricted`; public records only, counts and progress
- `fetch_scifact.log` - SciFact acquisition from the upstream AI2 S3 release (CPU, network): tarball, `LICENSE.md` from `allenai/scifact`, extraction and the jsonl inventory. Script `scripts/fetch_register_corpora.py scifact`
- `fetch_faa-amt.log` - FAA Aviation Maintenance Technician handbook acquisition (CPU, network): three born-digital PDFs from faa.gov with a PDF-magic check on each. Script `scripts/fetch_register_corpora.py faa-amt`
- `fetch_army-tm.log` - US Army technical manual crawl (CPU, network, multi-day): the 1,766 operator/maintenance manuals from liberatedmanuals.com under a per-day budget, one line per manual with the running count, refusal and backoff lines when the mirror throttles, and `army-tm COMPLETE` at the end. Resumable from `data/external/datasets/army-tm/_state.json`. Script `scripts/fetch_register_corpora.py army-tm`
- `R13-H127_campaign_d<draw>.log` - R13-H127 RAGTruth-parallel-copy-rebalance training campaign (GPU1), one draw: the clean R9-H105 recipe on the public mix with the RAGTruth family resampled to per-row weights EN 4.0 / each translation 0.5714 at fixed family mass (120,720 row-equivalents, 12 DANN groups unchanged), followed by the truncated decomposed-min read and the PRIMARY windowed read. Opens with the realized per-group row-equivalent table and the init fingerprint; closes with per-group discriminator accuracy (registered amendment 4) and `=== H127 D<draw> CAMPAIGN COMPLETE ===`, per-stage `=== FAILED: ... ===` on a crash. Scripts `experiments/grounding-semantic/R13-H127_campaign.sh` and `R13-H127_trainer.py`; design note `R13-H127_launch_design.md`. Public corpora only, metrics and progress, no client text
- `R14-H133_campaign_d<draw>.log` - R14-H133 (A4) derivation-parity lane training campaign (GPU1), one arm draw: the clean R9-H105 recipe on the public mix plus the H133 lane (685,670 + 50,000 = 735,670 rows, 14 DANN groups - the H108 lane does not ride along), followed by the truncated decomposed-min read, the PRIMARY windowed read, the binding anti-gaming held-out present-value near-miss eval (arm against the banked clean draw 1, plus `bind_row`), and the report-only probe bank. Opens with the mix census, the init fingerprint and the permutation fingerprint; closes with `=== H133 ARM D<draw> CAMPAIGN COMPLETE ===`, per-stage `=== FAILED: ... ===` on a crash. Every stage is skipped when its artifact is already on disk and the trainer resumes from `models/R14-H133-arm-draw<N>/resume.pt`, so a relaunch after a container restart is the same command. Scripts `experiments/grounding-semantic/R14-H133_campaign.sh`, `R14-H133_trainer.py`, `R14-H133_antigaming.py`, `R14-H133_probes.py`; design note `R14-H133_launch_design.md`. Public corpora only, metrics and progress, no client text
- `R15_gate_wave.log` - R15 pre-arm gate wave (GPU0 and GPU2, card 1 untouched), all frozen-weights and arena-free: B1 checkpoint-specificity (blocking on the H133 lane build spec), B2 absent-positive expression, B3 binary absent-indicator partial correlation, B4 controlled cross-header binding (blocking on the H138 sub-block), B5 instrument panel arms 2 / 5 / 6 / 7 plus the arm-8 natural-derivation baseline, B6 evidence-conditioning. One `=== GATE <id> START ===` / `=== GATE <id> DONE ===` marker pair per gate, `=== GATE <id> FAILED rc=N ===` on a crash, `=== WAVE CHAIN card <n> COMPLETE ===` at the end of each chain. Results checkpoint to `experiments/grounding-semantic/R15_gate_<id>_result.json`; the driver skips any gate whose result JSON already exists, so a re-run resumes. Driver `experiments/grounding-semantic/R15_gate_wave.sh`, ground truth `R15_synthesis.md`
- `R16-H140_pilot.log` - R16-H140 window-ensemble-readout pilot: gate G0 (dispersion census, CPU, seconds) then the G1 training-slice embedding cache (GPU2). Closes each stage with `=== G0 CENSUS DONE ===` / `=== G1 CACHE DONE (train) ===`; the readout training and blind arena read append `=== G1 READOUT DONE ===`. Results at `experiments/grounding-semantic/R16-H140_G0_census.json` and `R16-H140_G1_pilot.json`, caches under `R16-H140_cache/` (skipped on re-run, so the chain resumes). Scripts `R16-H140_G0_census.py`, `R16-H140_G1_cache.py`, `R16-H140_G1_readout.py`. Public corpora plus the private gold census only as counts, no client text
- `R16-H140_arena_cache_gpu0.log` - the blind-arena half of the same G1 embedding cache, run concurrently on GPU0 (`--only arena`) while GPU2 builds the training half; per-subset pair counts and `=== G1 CACHE DONE (arena) ===`
- `R16-H141_autopsy.log` - R16-H141 step A1 scalar-vs-embedding autopsy of the R16-H140 readout lift (CPU, ~1 min): rebuilds per-sentence aggregators from the cached frozen per-window SCALAR scores only (the `emb` arrays are never opened), fits them on the identical seed-0 public training slice the readout used, and reads the blind arena through each with the campaign protocol unchanged. Opens with the training-slice size and the held-out sentence AUROC of each aggregator, then one line per arena subset carrying the hard-max fidelity control against the banked R10-H108 draw-1 windowed read, and closes with the per-subset table and `=== R16-H141 A1 DONE ===` (or `=== R16-H141 ABORTED (fidelity) ===` if the control misses its 1e-4 tolerance). Result at `experiments/grounding-semantic/R16-H141_autopsy.json`, script `R16-H141_autopsy.py`. Public corpora only, metrics and progress
- `R16-H142_G0_gate.log` - R16-H142 G0 adapter-side-head gate (GPU2, cards 0 and 1 untouched): fine-tunes an adapter branch that conditions the per-window sentence score on the mean-pooled window-ensemble embedding, with the serving max-over-windows / min-over-sentences aggregation unchanged; trainable set is the adapter, the score head and the last 2 mmBERT trunk layers, everything else frozen. Training slice is the R16-H140 G1 public-only rows (RAGTruth train + the R10-H108 manufactured lane), imported from `R16-H140_G1_cache.py` so it is the same rows by construction - no private gold, no RAGBench/arena rows. Opens with the GPU name, the trainable-parameter split and the slice census, then one line per 100 steps and a `[val]` line per half-epoch, then the blind windowed decomposed arena read and the gold_full held-out read, and closes with `=== H142 G0 GATE COMPLETE ===`. Result at `experiments/grounding-semantic/R16-H142_G0_result.json`, checkpoint `models/R16-H142-G0/` (`resume.pt` every 500 steps, `adapter.pt` on completion - a re-run resumes, and exits immediately if the result JSON exists). Script `R16-H142_G0_adapter.py`. Public corpora plus the private gold read as aggregate metrics only, no client text
- `R16-H141_controls.log` - R16-H141 review-ordered controls A and B on the autopsy conclusion (GPU0 only, ~4 min, existing `R16-H140_cache/`, no embedding recomputation). Control A trains the readout's own MLP body on the autopsy's 8 window-score scalars at three capacities (architecture-matched h=256, parameter-matched h=404 at 332k params, and a 6-feature ablation dropping the window-count features) under the readout's optimiser, epochs, seed-0 split and best-val protocol, and adds the smooth max forms the autopsy's grid omitted (log-sum-exp and softmax-weighted mean, temperature swept on training-slice held-out rows only). Control B retrains the FULL readout at seeds 1, 2, 3 by importing `R16-H140_G1_readout.py` and rebinding its seed, plus a seed-0 rerun as a harness-fidelity check, and reads each blind. Opens with the arena hard-max fidelity assertion against the banked R10-H108 draw-1 windowed read, then per-model training lines, and closes with three Polars tables (Control A variants, Control B per-seed deltas, Control B per-subset spread) and `=== R16-H141 CONTROLS DONE ===`. Result at `experiments/grounding-semantic/R16-H141_controls.json`, script `R16-H141_controls.py`. Public corpora only, metrics and progress

- `R14-H133_lane_build.log` - R14-H133 derivation-parity lane build (CPU, seeded, 50,000 rows)
- `R14-H133_lane_verify.log` - R14-H133 lane build-manifest verification pass (CPU)
- `R17-H143_stageA.log` - R17-H143 Stage A tiny-reasoner residual measurement, tiny-decoder half (GPU0, cards 1 and 2 untouched by this process): the five ~300M-class decoders (Baguettotron 321M, Pleias-RAG-350M, Monad 56M, SmolLM2-360M-Instruct, Qwen3-0.6B as the over-budget 596M reference) scoring the 1,000-pair H133-lane-v2 eval sample plus 50 positive-control pairs with a GROUNDED / UNGROUNDED forced-choice verdict. Inference only - no training, no arena or RAGBench contact. One prompt template per model rendered through that model's own chat template (or its documented native format where it ships none), audited at startup; the 50 controls are scored first and a model below 0.90 control AUROC is skipped as a harness defect rather than recorded as a low read (Monad is gate-exempt, floor reference). Per-batch progress carries `forced=N`, the count of traces that spent the whole 512-token budget without stating a verdict and had the answer forced after a closed think block. Scores checkpoint per batch to `experiments/grounding-semantic/R17-H143_scores.parquet` and a re-run skips any (model, pair_id) already present, so a relaunch after a container restart is the same command. Scripts `R17-H143_evalset.py`, `R17-H143_stageA.py`, `R17-H143_analyze.py`; gate reads at `R17-H143_gate.json`, sample traces at `R17-H143_gen_samples.json`, result at `R17-H143_stageA_result.json`. Public corpora only, metrics and progress
- `R17-H143_stageA_teacher.log` - the teacher-ceiling half of the same measurement (GPU2): Mistral-Small-24B-Instruct-2501 under vLLM over the identical eval set and prompt semantics, fp8 first with a bfloat16 fallback and the path that actually loaded recorded in `R17-H143_gate.json`. Same checkpoint file and same resume behaviour as the tiny half
- `R17-H143_teacher.log` - the CACHED teacher read that the Mistral run could not deliver (GPU1): Qwen3-32B-FP8 under vLLM over the identical 1,050-pair eval set, through the same `chat_think` prompt and capped 512-token think budget the Qwen3-0.6B ran, with the forced-choice margin taken at the fixed answer position after a closed think block. Controls first at the 0.90 gate, then the 1,000 real pairs checkpointed every 200 into the shared `R17-H143_scores.parquet` on the (model, pair_id, label) key. Script `R17-H143_teacher.py`, which imports `R17-H143_stageA.py` for every scoring convention. Public corpora only, metrics and progress
- `R14-H133_antigaming_traced_draw1.log` - the R14-H133 anti-gaming clause read on the TRACE-CONDITIONED set (GPU1, minutes, frozen weights): both checkpoints - the arm draw and the banked clean draw 1 - over the 2,620 rows of `R14-H133_antigaming_traced.parquet` in the serving shape the lane trains, reporting near-miss and `bind_row` AUROC per kind and per family with arm-minus-control deltas. Measurement only, no adjudication. Script `R14-H133_antigaming_traced_read.py`, result `R14-H133_antigaming_traced_draw1_result.json`, per-row scores `R14-H133_antigaming_traced_scores.parquet`
- `R17-H144_pairs.log` - R17-H144 Stage B pair-corpus build and verify pass (CPU only, no GPU): ~30,000 rows / ~15,000 BARE-claim derivation minimal pairs at seed 1144 over public TabFact-train and FEVEROUS-train tables, built by the v2-lane reconstruction in `R17-H144_pairs.py` (claims carry no reasoning trace - traces are a later teacher stage). Every document of the R17-H143 eval set is excluded before any claim is drawn, and the log records the disjointness count. The verify pass at the end applies the five abort bars - claim-only TF-IDF probe AUROC < 0.55 on a document-disjoint split, per-family within-pair claim-only accuracy < 0.60, trailing-zero AUROC inside [0.45, 0.55] on every derivation type, arithmetic re-derivation 0 errors on a 1,000-row sample, P(label 0 | asserted value absent) = 0.5 - and the script exits non-zero if any bar fails or any document is shared with the eval set. Outputs `R17-H144_pairs.parquet` and `R17-H144_pairs_manifest.json`
- `R17-H144_tracegen.log` - R17-H144 Stage B / Job 1 teacher verdict-trace generation (GPU1, cards 0 and 2 untouched): Qwen3-32B-FP8 under vLLM over all 30,000 rows of `R17-H144_pairs.parquet`, through the H143 prompt semantics imported from `R17-H143_stageA.py` (same INSTRUCTION, same `chat_think` template, same capped 512-token think budget, same truncate-close-elicit forced answer position). Each row yields the think text plus a forced GROUNDED / UNGROUNDED verdict; a row is ACCEPTED only when that verdict agrees with the constructed label, and the per-batch line carries the running acceptance rate and ETA. Checkpoints every 1,000 rows to `R17-H144_traces.parquet` on the (pair_id, label) key and a re-run skips what is already there, so a relaunch after a container restart is the same command. Script `R17-H144_tracegen.py`, sample trace `R17-H144_trace_samples.json`. Public corpora only, metrics and progress
- `R17-H144_sft.log` - R17-H144 Stage B / Job 2 student supervised fine-tune (GPU0, cards 1 and 2 untouched): full fine-tune of SmolLM2-360M-Instruct on the accepted teacher traces, chat-formatted so the user turn is byte-identical to the eval prompt and the assistant turn is `<think>...</think>` then the verdict word, with `<think>` / `</think>` added as tokenizer special tokens and loss on assistant tokens only. Opens with the tokenizer and data census (accepted rows, evidence-trimmed rows, rows dropped as overlong, document-disjoint 5% validation split), then a line per 50 optimiser steps, then per epoch the validation loss and the generated-format probe (share of validation generations that close the think block and state a parseable verdict). Checkpoints per epoch to `models/R17-H144-student/epoch<N>/`, stats to `R17-H144_sft_stats.json`; a validation format rate below 0.90 after epoch 1 stops the run rather than burning epochs. Script `R17-H144_sft.py`
- `R17-H144_eval.log` - R17-H144 Stage B / Job 3 student read on the banked 1,050-pair H143 eval set (GPU0, minutes): the best-validation and final-epoch checkpoints scored with the Stage-A harness conventions imported unchanged - free trace, closed think block, forced-choice margin at the first answer token, parse failures scored 0.0 with the literal-0.5 reading recorded alongside. The 50 control pairs are scored first and the run aborts below 0.90 rather than trusting the read; the untrained SmolLM2 baseline (0.5011) is carried from the banked Stage A read and never re-run. Scores checkpoint to `R17-H144_student_scores.parquet`, result to `R17-H144_result.json`. Script `R17-H144_eval.py`. Measurement only, no adjudication
- `R17-H144_campaign.log` - R17-H144 Stage B driver chaining the three jobs: it waits out the detached teacher trace generation on GPU1 (Job 1), reaps any vLLM process still holding card 1 and prints the card's returned memory, applies the 0.50 acceptance floor to `R17-H144_traces.parquet`, then runs the student SFT (Job 2) and the eval (Job 3) on GPU0 in sequence. One `=== JOB<n> ... ===` marker per stage, `=== FAILED: ... ===` on any stage failure, `=== R17-H144 CAMPAIGN COMPLETE ===` at the end. Every stage no-ops when its artifact already exists, so a relaunch after a container restart is the same command. Driver `experiments/grounding-semantic/R17-H144_campaign.sh`
- `R17-H145_scaleunit.log` - R17-H145 scale/unit verification pair-corpus build and verify pass (CPU only, no GPU): minimal pairs at seed 1145 over public TabFact-train and FEVEROUS-train tables in which the ONLY difference between a positive and its negative twin is the scale/unit word - digits are never touched, the value is copied verbatim from the cell, the positive's unit is readable in the serialized evidence and the negative's unit is absent from it, so the defect is groundable. Units are read off the source table (a header annotation such as `elevation (m)` or `us viewers (millions)`, or the cell itself such as `2405 m` or `45%`), never invented for unitless values, and swaps stay inside a dimension. Families are hub-shaped and 50/50 in both directions, which makes every word's positive and negative counts equal exactly. Corpus loading, serialization and the eval-set content exclusion are reused from `R17-H144_pairs.py`; documents of the R17-H143 eval set are excluded on content before any claim is drawn and that exclusion is an abort condition; the R17-H144 pair corpus is NOT excluded (build 1 was, and paid two thirds of the unit-bearing supply - the coordinator dropped it because H144 feeds the student decoder while this family feeds the encoder), and the overlap is measured and recorded instead. The magnitude-merge families (percent against million and billion) are capped at 40% of the corpus, and `thousand` is excluded on a measured 0.70 claim-only breach traced to its five-table support. The verify pass applies five abort bars - claim-only TF-IDF probe AUROC < 0.55 with the converged liblinear configuration on direction-stratified document-disjoint 5-fold out-of-fold scores, per-family within-pair claim-only accuracy < 0.60, word label-marginal skew <= 0.02, cited-value presence rate 1.0, unit-surface discipline 1.0 - and the script exits non-zero if any bar fails or any document is shared. Outputs `R17-H145_scaleunit.parquet` and `R17-H145_scaleunit_manifest.json`, builder `R17-H145_scaleunit.py`
- `R17-H145_census.log` - R17-H145 arm pre-launch mix census (CPU only, no GPU): imports `R17-H145_trainer.py` and runs its `build_mix()` alone, so the row counts, the two lane group counts and the 14-group map are checked before any GPU time is spent. Prints the relational lane read (`R14-H133_lane.parquet` filtered to `block == 'rel'`, with its bind_col / compare / bind_row split) and the scale/unit lane read (`R17-H145_scaleunit.parquet`, rows and pairs), then the mix total. Any count mismatch aborts
- `R17-H145_gpu1_wait.log` - R17-H145 launch gate: a poll loop, one line every two minutes, that holds the arm until GPU1 is genuinely free after the R17-H144 trace generation - the traces parquet present and unchanged for at least five minutes, card 1 memory below 1,000 MiB, and no vLLM or EngineCore process alive. Each line carries the parquet age, the card's used memory and the live process count. Exits on `GPU1 FREE`
- `R17-H145_campaign_d<draw>.log` - R17-H145 relational + scale/unit lane training campaign (GPU1, cards 0 and 2 untouched), one arm draw: a FULL-MIX FRESH RETRAIN of the incumbent recipe (1,500-char truncation, pointwise BCE, DANN, H126 seeding) on the clean 685,670-row mix plus two added lanes as their own DANN groups - the 7,500 relational rows carved out of the v3 H133 lane and the 5,672 scale/unit verification rows of build 2, for 698,842 rows over 14 groups at seed 1145. Opens with the mix census, the init fingerprint and the permutation fingerprint, and aborts before training on any count mismatch; then the truncated decomposed-min read, the windowed arena read, the anti-gaming held-out present-value near-miss eval on the untraced banked set (arm against the banked clean draw 1, plus `bind_row`), and the report-only probe bank that carries the three co-primaries (bind_col, bind_row, scale/unit). Closes with `=== H145 ARM CAMPAIGN COMPLETE <ts> ===`, per-stage `=== FAILED: ... ===` on a crash. Every stage is skipped when its artifact is already on disk and the trainer resumes from `models/R17-H145-arm-draw<N>/resume.pt` replaying the same persisted permutation, so a relaunch after a container restart is the same command. Scripts `experiments/grounding-semantic/R17-H145_campaign.sh`, `R17-H145_trainer.py`, with the reads reused from `R14-H133_antigaming.py` and `R14-H133_probes.py` under `--arm R17-H145`. Public corpora only, metrics and progress, no client text
- `R17-H146_lane.log` - R17-H146 misbind-verification lane build and verify pass (CPU only, no GPU): ~30,000 rows / ~15,000 BARE-claim minimal pairs at seed 1146 over public TabFact-train and FEVEROUS-train tables. Every claim is a pure lookup verification - the positive restates the value the table gives for a named row and a named column, verbatim present in a chunk that states that binding; the negative twin is the same claim shape citing a value that is equally present but belongs to a DIFFERENT row (`misbound_row`, 70% of pairs) or a different column of the same row (`misbound_col`, 30%). No derived values, no arithmetic, no traces. Both misbind directions (cited value above / below the correct one) are filled 50/50 inside each family, which is what holds the digit-count and trailing-zero channels at chance where within-pair digit parity is not constructible. Corpus loading, serialization, digit-surface parity and the content-based eval-set exclusion are imported from `R17-H144_pairs.py`; documents of the R17-H143 eval set are excluded on content before any claim is drawn and that exclusion is an abort condition, while overlap with the H144 and H145 corpora is permitted and measured only. The verify pass applies six abort bars - claim-only TF-IDF probe AUROC < 0.55 with the converged liblinear configuration on direction-stratified document-disjoint 5-fold out-of-fold scores, per-family within-pair claim-only accuracy < 0.60, cited- and correct-value presence rate 1.0 (verbatim and canonical), trailing-zero AUROC inside [0.45, 0.55], digit-count AUROC inside [0.45, 0.55], and a 500-pair mechanical audit re-deriving from the source table that every sampled negative cites a value genuinely bound to a different row or column than the claim names, 0 errors - and the script exits non-zero if any bar fails. Outputs `R17-H146_lane.parquet` and `R17-H146_lane_manifest.json`, builder `R17-H146_lane.py`
- `R16-H142_G1_census.log` - R16-H142 G1 pre-launch census pass (CPU only, `R16-H142_G1_arm.py --run <twin|arm> --census-only`): builds the incumbent clean public mix with the evidence UNTRUNCATED, aborts unless it is exactly 685,670 rows over the 12 registered DANN groups, prints the per-group counts and the realised window-set distribution, aborts if the mean set size falls under 1.05 (amendment A1's premise is that untruncated evidence gives real multi-window sets - it is measured, never assumed) or if any set exceeds the 96-pair batch cap, then prints the adapter parameter count with its zero-init check and the trunk+task_head init and permutation fingerprints, and exits without training. Run for BOTH members of the pair before any GPU time is spent: the two must print the same fingerprints, which is what makes the pair init-paired
- `R16-H142_G1_campaign.log` - R16-H142 G1 amendment A1 adapter ABLATION PAIR campaign (GPU1, cards 0 and 2 untouched). The registered single-variable form was unrunnable - the incumbent's 1,500-char chunk truncation equals the 1,500-char serve window, so every training window set had size 1 and the adapter's ensemble input was `ctx == cls` for all 685,670 rows - so review constraint F3's second option is taken: the adapter carries its own ablation. Two runs share one presentation (685,670-row clean mix, 12 DANN groups, evidence untruncated and windowed 1500/750, MIL max-over-window BCE per row plus per-pair domain CE at lambda 0.02, seed 1142, full trunk at lr 1e-5, 14,300 steps of <= 48 sets / <= 96 pairs); the TWIN has no adapter and runs first as the control, the ARM adds the zero-init adapter side-head at lr 1e-3, so arm minus twin attributes to the adapter and nothing else. Because the adapter is constructed last and the seed is re-issued after construction, both runs share their trunk+task_head init draws and their dropout stream, and the twin's adapter stays frozen at exactly zero - which makes one adapter-aware reader correct for both checkpoints. Each run opens with the mix and window census, the adapter parameter count and zero-init check, the init and permutation fingerprints, then a line per 200 steps, then the in-domain suite (gold, gold_full, RAGTruth EN and the seven translations), then the blind truncated and blind windowed decomposed-min arena reads; the pair delta closes the campaign. Markers `=== H142 G1 TWIN COMPLETE <ts> ===`, `=== H142 G1 PAIR COMPLETE <ts> ===`, and `=== FAILED: <stage> ===` on a crash. Every stage is skipped when its artifact is on disk and each trainer resumes from `models/R16-H142-G1-<run>/resume.pt` replaying the same persisted permutation, so a relaunch after a container restart is the same command. Results at `experiments/grounding-semantic/R16-H142_G1_{twin,arm}_result.json`, `R16-H142_G1_{twin,arm}_{truncated,windowed}_result.json` and `R16-H142_G1_pair_result.json`; scripts `R16-H142_G1_campaign.sh`, `R16-H142_G1_arm.py`, `R16-H142_G1_reads.py`, `R16-H142_G1_pair.py`. Public corpora plus the private gold read as aggregate metrics only, no client text
- `R17-H144_lookup.log` - R17-H144 amendment A1 verbatim-lookup family build (CPU, ~2 min): 7,000 rows / 3,500 minimal pairs over TabFact-train and FEVEROUS-train tables content-disjoint from the H143 eval set, built by `R17-H144_lookup.py` reusing `R17-H144_pairs.py`'s corpus loaders, serializers, surface-parity test and content-based exclusion. Positives state the table's own value for the row they name; negatives split 50/50 between absent-value assertions (surface- AND distribution-matched substitutions, no random-integer fallback) and adjacent-row misbinds (a real figure from the neighbouring serialized row). Every row carries a deterministic template trace in the teacher's `<think>...</think>` shape - no teacher inference. Closes with the leak bars: claim-only TF-IDF probe < 0.55 and worst within-pair family < 0.60, both on a document-disjoint split, and exits non-zero if either fails. Outputs `R17-H144_lookup.parquet`, `R17-H144_lookup_manifest.json`
- `R17-H144_sft_c2.log` - R17-H144 amendment A1 cycle-2 student fine-tune (GPU0): the same `R17-H144_sft.py` recipe under `H144_CYCLE2=1`, a FRESH fine-tune from the SmolLM2-360M-Instruct base (not a continuation of cycle 1) on the mixed corpus of 28,526 accepted teacher traces plus the 7,000-row lookup family, shuffled. Same format, same holdout convention, same guard. Checkpoints to `models/R17-H144-student-c2/epoch<N>/`, stats to `R17-H144_sft_c2_stats.json`
- `R17-H144_eval_c2.log` - the cycle-2 read on the banked 1,050-pair H143 bar set (GPU0), harness and conventions identical to cycle 1's Job 3. Scores checkpoint to `R17-H144_student_c2_scores.parquet`; results merge into `R17-H144_result.json` under a `cycle2` key that preserves cycle 1's numbers
- `R17-H146_campaign_d<draw>.log` - R17-H146 misbind-verification lane training campaign (GPU1, cards 0 and 2 untouched), one arm draw: a FULL-MIX FRESH RETRAIN of the incumbent recipe (1,500-char truncation, pointwise BCE, DANN lambda 0.02, MAX_LEN 512, BATCH 48, LR 1e-5 OneCycle, 1 epoch, H126 double seeding) on the clean 685,670-row mix plus ALL 30,000 rows of `R17-H146_lane.parquet` as ONE added DANN group `quant_misbind`, for 715,670 rows over 13 groups at seed 1146 (draw 2 seed 2146), 14,910 steps. The lane is bare-claim present-value misbind verification only - no derivation rows, no arithmetic, no traces - testing at core scale the skill H145 killed at slice scale (bind_col 0.5721). Opens with the mix census, the init fingerprint and the permutation fingerprint, and aborts before training on any count mismatch; then the truncated decomposed-min read, the windowed arena read, the anti-gaming held-out present-value near-miss eval on the untraced banked set (arm against the banked clean draw 1, plus `bind_row`), and the probe bank carrying the primaries (bind_col >= 0.80, bind_row >= 0.95; scale/unit observational). Closes with `=== H146 ARM CAMPAIGN COMPLETE <ts> ===`, per-stage `=== FAILED: ... ===` on a crash. Every stage is skipped when its artifact is already on disk and the trainer resumes from `models/R17-H146-arm-draw<N>/resume.pt` replaying the same persisted permutation, so a relaunch after a container restart is the same command. `PYTORCH_CUDA_ALLOC_CONF` is deliberately never set - `expandable_segments` is unusable under WSL2 on this box. Scripts `experiments/grounding-semantic/R17-H146_campaign.sh`, `R17-H146_trainer.py`, with the reads reused from `R14-H133_antigaming.py` and `R14-H133_probes.py` under `--arm R17-H146`. Public corpora only, metrics and progress, no client text
- `R17-H146_gpu1_wait.log` - the R17-H146 arm's queue watcher: polls `R16-H142_G1_campaign.log` every 5 min for the G1 pair's terminal marker and, on completion, additionally waits for GPU1's used memory to fall below 20,000 MiB (a co-resident external process of roughly 15 GB may legitimately hold the card, so the gate is not zero). Emits one line on release or on a G1 failure; no compute of its own

- `R17-H147_autopsy.log` - R17-H147 floor-subset autopsy (GPU2 only, cards 0 and 1 untouched; ANALYSIS ONLY - nothing is trained, tuned or selected on its findings). Per-item reads of the two blind-arena floor subsets, hagrid and emanual, under the PRIMARY windowed decomposed-min convention (1,500-char windows at stride 750 over `documents[:8]`, max over windows then min over H92 sentences) with five FROZEN banked checkpoints: the clean control pair (`models/R9-H105-mmbert-dann-clean`, `models/R9-H105-draw2`), the H108 lane pair (`models/R10-H108-lane-draw{1,2}`) and the window-ensemble adapter (`models/R16-H142-G0`, read through R16-H142 G0's own `pair_logits` so the adapter logit is not dropped). The run opens with a ten-cell POSITIVE CONTROL - every (checkpoint, subset) AUROC must reproduce its banked JSON value to within 1e-3 or the run aborts before any analysis - then banks per-item response scores, per-sentence scores and the source document of each sentence's arg-max window to `R17-H147_item_scores.parquet`, and writes `R17-H147_autopsy.json`: error taxonomy at each subset's macro-F1-optimal operating threshold plus a threshold-free per-item rank-loss decomposition, evidence-dispersion measures, the R12/R16-H140 faithful-oracle ladder recomputed per item, the error-concentration slice table against the R15 30%-share gate (share reported with prevalence and lift), and pairwise cross-checkpoint error-set Jaccard. Script `experiments/grounding-semantic/R17-H147_autopsy.py`. Public arena data only, metrics and progress

- `R17-H148_gate.log` - R17-H148 procedural-register lane, STAGE 1 only (source census + probe build + kill-gate read); the full 30k lane is NOT built here. Three stages append to this one log. (1) `R17-H148_extract.py` (CPU, conda interpreter - PyMuPDF is absent from `.venv`) walks the staged R14-H136 procedural PDFs (`data/external/datasets/army-tm/pdf`, `.../faa-amt/pdf`), detects runs of at least three consecutively numbered list items per page, applies an OCR-quality filter and banks `R17-H148_blocks.parquet` plus the census `R17-H148_census.json`. (2) `R17-H148_probe.py` (CPU) builds the held-out probe `R17-H148_probe.parquet` and its manifest: bare minimal pairs over one enumerated list, families `misbound_step` (twin cites a different real step number at most two away, the cited step's text token-Jaccard-dissimilar) and `misbound_value` (twin cites a numeral real in the chunk but belonging to another step and absent from the claimed one), with an exact mirror-balance on the step numeral so every per-numeral feature is chance by construction. Verify battery on H146 conventions: converged claim-only tf-idf probe over direction-stratified document-disjoint folds (bar < 0.55), within-pair claim-only accuracy (bar < 0.60), value presence 1.0, digit-surface channels, and a 100-pair mechanical re-derivation audit (bar 0 errors); exits non-zero on any failed bar. (3) `R17-H148_gate.py` (GPU2 only, `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=2`; cards 0 and 1 untouched) scores the probe with the frozen campaign control `models/R9-H105-mmbert-dann-clean` and, as a non-comparable reference, `models/R16-H142-G0`, then writes `R17-H148_gate_result.json` with the census, the probe bars, the control AUROC with its Hanley-McNeil standard error, per-family and per-corpus reads, and the LICENSE flag (registered bar: control AUROC <= 0.65). Flags only - the coordinator adjudicates. Public-domain US-government corpora only, metrics and progress

- `R17-H149_gate.log` - R17-H149 bare-assertion prose lane, STAGE 1 only (source census + probe build + kill-gate read); no lane is built here. Three stages append to this one log. (1) `R17-H149_extract.py` (CPU, conda interpreter - PyMuPDF is absent from `.venv`) collects running-PROSE passages, the deliberate complement of H148's list blocks: SciFact upstream abstracts (`data/external/datasets/scifact/data/corpus.jsonl`) plus paragraph runs from the FAA AMT handbooks and army-tm PDFs, rejecting enumerated, captioned and table-like material, and banks `R17-H149_passages.parquet` with the census `R17-H149_census.json`. (2) `R17-H149_probe.py` (CPU, spaCy `en_core_web_lg`) builds the held-out probe `R17-H149_probe.parquet` and its manifest: single-sentence bare assertions restating one dependency-extracted (subject, relation, object) proposition in new words - voice recast plus a tense-preserving verb synonym, never a verbatim substring of the passage - twinned with ONE proposition-level substitution. Families `role_swap` (the two arguments of an asymmetric relation exchange roles; the twins are token-multiset identical) and `direction_flip` (the direction verb replaced by its antonym from one closed pair, up and down sources balanced inside each pair). Verify battery: converged claim-only tf-idf probe over direction-stratified document-disjoint folds (bar < 0.55), within-pair claim-only accuracy (bar < 0.60), measured paraphrase depth with a zero-verbatim-positive bar, surface-parity AUROCs, and substituted-string balance. (3) `R17-H149_gate.py` (GPU2 only, `CUDA_DEVICE_ORDER=PCI_BUS_ID CUDA_VISIBLE_DEVICES=2`; cards 0 and 1 untouched) scores the probe with the frozen campaign control `models/R9-H105-mmbert-dann-clean` and writes `R17-H149_gate_result.json` with the census, the probe bars, the hand-audit result `R17-H149_audit_result.json`, the control AUROC with its Hanley-McNeil standard error, per-family, per-corpus and per-direction reads, within-pair accuracies, and the LICENSE flag (registered bar: control AUROC <= 0.70). Flags only - the coordinator adjudicates. Public-domain US-government corpora and CC BY / ODC-By SciFact only, metrics and progress
- `R17-H144_ctrlval.log` - R17-H144 amendment A2 held-out control-family validation split build (CPU): the `R17-H144_lookup.py` generator at seed 11442 under `H144_VALSPLIT=1`, over tables content-disjoint from BOTH the SFT corpus (derivation pairs + lookup family) and the banked H143 eval set, with surface parity REQUIRED rather than preferred on the misbind half (the residual table pool is small and atypical; best-effort parity left that half readable from the claim alone at 0.624). Outputs `R17-H144_ctrlval.parquet` and `R17-H144_ctrlval_manifest.json`, and exits non-zero if either leak bar fails
- `R17-H144_selector.log` - R17-H144 amendment A2 checkpoint selection (GPU0): scores cycle-2 epochs 1, 2 and 3 on the held-out control-family split through the eval harness imported verbatim from `R17-H144_eval.py`, and applies the rule registered before the scores existed - argmax control-validation accuracy, tie-break lower SFT validation loss. Opens with the disjointness verification (asserted, not merely reported), then per-checkpoint progress, and closes with the selector scores, the selected epoch, that checkpoint's banked-eval bars and the verdict that follows. Scores checkpoint to `R17-H144_selector_scores.parquet`, result to `R17-H144_selector.json` and into `R17-H144_result.json` under `cycle2.selector_A2`. Script `R17-H144_selector.py`. Measurement only, no adjudication

- `R18-H150_lane_build.log` - R18-H150 scale/unit verification lane build + verify (CPU only, no GPU, no training). `R18-H150_scaleunit_lane.py` at seed 1150 with `PYTHONHASHSEED=0`, over public TabFact-train and FEVEROUS-train tables content-disjoint from the R17-H143 eval set. Bare-claim minimal pairs on R17-H146 machinery with the corrupted token moved from the numeral to the scale/unit word; the H148 anti-shortcut rule is met by SURFACE-DISJOINT CLAIMS (the evidence writes the unit as an abbreviation, the claim spells it out, and neither twin's unit phrase - nor any token distinguishing the twins - is readable in the chunk) plus an in-chunk-distractor subset where the corrupted unit is attested on another column. Both directions of every word-pair family are matched bucket for bucket on the numeral's surface (digit count, decimal point) so the claim alone cannot settle the pair, and the document-disjoint probe folds are packed on PAIR counts with both directions packed jointly. Logs the source census, per-unit positive supply, the doc-cap ladder, family construction and the full verify battery; writes `R18-H150_scaleunit_lane.parquet`, `..._manifest.json` and `..._verify.json`, and exits non-zero on any failed bar
- `R18-H150_window_census.log` - R18-H150 combined-mix serving-window census (CPU only). `R18-H150_window_census.py` reads the clean public mix UNTRUNCATED the way the R16-H142 twin trainer reads it (`R10-H108_lane.public_train()` with `M59.CFG.chunk_max_chars` lifted - the cached 1,500-char mix would read every clean row as single-window and understate the baseline), windows at 1,500/750 with the final window flush to the end, and reports per-group clean statistics plus the dilution the H146 misbind lane and the H150 scale/unit lane cause. Writes `R18-H150_window_census.json`
- `R18-H150_scaleword_build.log` - R18-H150 amendment A2 scale_word extension, stages 1 and 3 (CPU only). Stage 1 `R18-H150_edgar_extract.py` cuts section_7 (MD&A) of the R14-H136 restricted EDGAR slice into the project's 1,500-char windows and censuses the magnitude vocabulary, writing `R18-H150_edgar_chunks.parquet` and `R18-H150_edgar_census.json`. Stage 3 `R18-H150_scaleword_census.py` runs only over gate-admitted chunks and sizes, PER DIRECTION, the two adjacency-proof scale_word constructions (caption-bound line items; identical-numeral two-scale chunks) against the adjacency-solvable baseline, plus the InfoTabs secondary source, writing `R18-H150_scaleword_census.json`
- `R18-H150_edgar_gate.log` - R18-H150 amendment A2 EDGAR provenance + contamination gate (CPU only), `R18-H150_edgar_gate.py`, a hard precondition of any scale_word row. Four blocking checks: the tracked Apache-2.0 licence sidecar with the R14-H136 restriction clauses and a complete fetch state; re-derivation of the restriction from the data (filing year >= 2020, no 1999-2019 S&P 500 CIK); the registered R14-H136 provenance gate (`provenance_gate.py`, 8-gram Jaccard >= 0.3 vs the finqa and tatqa arena documents, KILL > 2%) behind a spike control that must detect injected arena units; and a contamination wall (13-gram containment over all ten arena subsets, plus exact-chunk and content-fingerprint overlap against the banked R17-H143 eval set and the H144/H145/H146/H150 lanes). Writes the admitted set `R18-H150_edgar_admitted.parquet` and the sidecar `R18-H150_edgar_gate.json`; exits non-zero unless the status is GREEN
- `R18-H150_census.log` - R18-H150 convergence arm draw 1 CPU dry run (`bash experiments/grounding-semantic/R18-H150_campaign.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched, nothing trained). Dispatches `R18-H150_arm_run.py --stage census`, which rebinds the BANKED G1 twin trainer (`R16-H142_G1_arm.py`) to seed 1150, the 14-group map and the 3-source mix, then runs that trainer's own census path: clean public mix read UNTRUNCATED (685,670 rows) plus the R17-H146 misbind lane (30,000 rows, group `quant_misbind`) plus the R18-H150 unit_swap lane (5,540 rows, group `quant_scale_unit`), each lane verified row-for-row against its banked pair and negative-family counts. Logs the per-group row and window statistics, the 1,500/750 window census cross-checked against the banked `R18-H150_window_census.json` combined block (hard abort on disagreement), the zero-init adapter assertion, the trunk+task_head init fingerprint, the permutation fingerprint and the step count, then exits before any model reaches a card
- `R18-H150_campaign_d1.log` - R18-H150 convergence arm draw 1 full campaign (`R18-H150_campaign.sh`, GPU by env `GPU=n`; `PYTORCH_CUDA_ALLOC_CONF` deliberately unset - `expandable_segments` kills `.to("cuda")` under WSL2 on this box). Four idempotent stages, each skipped when its artifact is on disk, with the trainer resuming from `models/R18-H150-arm-draw1/resume.pt` on the SAME persisted permutation: (1) train under the twin windowed-MIL protocol verbatim plus the in-domain suite (gold, gold_full, RAGTruth EN and the seven translations) -> `R18-H150_arm_draw1_result.json`; (2) the PRIMARY blind windowed decomposed-min arena read through the twin's own adapter-aware reader -> `R18-H150_arm_draw1_windowed_result.json`; (3) the anti-gaming held-out near-miss eval on the untraced banked set at prefix R18-H150 -> `R18-H150_antigaming_draw1_result.json`; (4) the probe bank - bind_col, bind_row, compare, the scale/unit quads, P1 and the H133 triples -> `R18-H150_probes_draw1_result.json`. Terminal markers `=== H150 ARM CAMPAIGN COMPLETE ===` and `=== FAILED: <label> ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R18-H150_unitswap_probe.log` - R18-H150 held-out unit_swap PROBE build + verify (CPU only; measurement infrastructure that never trains). `R18-H150_unitswap_probe.py` at seed 11500 imports the lane builder `R18-H150_scaleunit_lane.py` and reuses its templates, unit vocabulary, surface-disjointness rule, value-surface bucket matching, hub-family assembly and full verify battery, so the probe is constructed and measured by the same code as the lane. Supply is the UNUSED unit-bearing tables: every one of the lane's 410 documents is excluded by doc_id, the R17-H143 eval set is excluded on content fingerprints, and overlap with the H144/H145/H146 corpora is measured. Logs the per-unit and per-dimension positive histogram, the hub-family offered/kept report, the trim to target and the bars; writes `R18-H150_unitswap_probe.parquet`, `..._manifest.json` and `..._verify.json`, and exits non-zero on any failed bar or any shared lane document
- `R18-H151_score_dump.log` - R18-H151a variance-anatomy per-window score dump (`R18-H151_score_dump.py`, GPU0 only, measurement only - no training). Scores both twin checkpoints (seed 1142 `models/R16-H142-G1-twin`, seed 2142 `models/R16-H142-T-draw2`) through the banked windowed decomposed-min reader lineage (`R16-H142_G1_arm.load_run` + a pair-for-pair replica of `ARM.score_sets` that KEEPS per-pair logits) on six arena subsets (tatqa, techqa, pubmedqa, hotpotqa, covidqa, emanual) plus gold_full. Rows {subset, item_id, sentence_id, window_id, score, label, n_windows_in_sentence, checkpoint_seed} land in per-(seed, subset) shards under `experiments/grounding-semantic/R18-H151_shards/` (kill-resumable), then a sanity gate recomputes item AUROC from the dumped rows (max over windows per sentence, min over sentences) and requires every arena subset within 1e-4 of the banked windowed read before writing `R18-H151_scores_<seed>.parquet`; any miss stops the run with no parquet written. Terminal markers `=== R18-H151a SCORE DUMP DONE ===` and `SANITY GATE FAILED`
- `R18-H150_census_d2.log` - R18-H150 convergence arm draw 2 CPU dry run (`bash experiments/grounding-semantic/R18-H150_campaign_d2.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched, nothing trained). Dispatches `R18-H150_arm_draw2_run.py --stage census`, the thin wrapper that rebinds the banked `R18-H150_arm_run.py` dispatcher to seed 2150, checkpoint `models/R18-H150-arm-draw2` and the draw-2 result names; the mix is unchanged (721,210 rows, 14 DANN groups, every assembly abort live) and the window-census cross-check against the banked `R18-H150_window_census.json` fires as in draw 1. Logs the trunk+task_head init fingerprint, the permutation fingerprint (`np.random.default_rng(2150)`, distinct from the banked 1142/2142/1150/3151/3152 permutations) and the step count, then exits before any model reaches a card
- `R18-H150_campaign_d2.log` - R18-H150 convergence arm draw 2 full campaign (`R18-H150_campaign_d2.sh`, GPU by env `GPU=n`; `PYTORCH_CUDA_ALLOC_CONF` deliberately unset - `expandable_segments` kills `.to("cuda")` under WSL2 on this box). Amendment A3's confirming draw at seed 2150 through byte-identical banked code (the `R18-H150_arm_draw2_run.py` wrapper rebinds seed/checkpoint/result names only). Four idempotent stages, each skipped when its artifact is on disk, with the trainer resuming from `models/R18-H150-arm-draw2/resume.pt` on the SAME persisted permutation: (1) train under the twin windowed-MIL protocol verbatim plus the in-domain suite (gold, gold_full, RAGTruth EN and the seven translations) -> `R18-H150_arm_draw2_result.json`; (2) the PRIMARY blind windowed decomposed-min arena read -> `R18-H150_arm_draw2_windowed_result.json`; (3) the anti-gaming held-out near-miss eval at prefix R18-H150-d2 -> `R18-H150-d2_antigaming_draw2_result.json`; (4) the probe bank at the same prefix -> `R18-H150-d2_probes_draw2_result.json`. The -d2 prefix namespaces draw 2's set/scores parquets off draw 1's banked ones; `models/R18-H150-d2-arm-draw2` is a symlink to the trainer checkpoint so the banked readers resolve it (the R16-H142_T_draw2.sh pattern). Terminal markers `=== H150 ARM DRAW 2 COMPLETE ===` and `=== FAILED: <label> ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R18-H152_gpu0_vram_probe.log` - R18-H152 GPU0 fit probe (24 GB RTX PRO 4000, pure measurement, no campaign state): the wrapper's own train_step (EMA + window dropout engaged, seed 3151 config verified against the banked fingerprints) for the run's first 15 steps plus the 5 largest 96-pair batches. VERDICT FAIL - peak allocated 36.96 GB / reserved 56.56 GB vs the 21.81 GB bar (85% of 25.66 GB); no OOM only because WSL2 oversubscribes GPU memory into system RAM at 7-30 s/step (vs 0.918 s/step on the 96 GB card). Script `tmp/R18-H152_gpu0_vram_probe.py` (gitignored); the identical registered recipe does NOT fit 24 GB
- `R18-H152_split_exec_proof.log` - R18-H152 SPLIT-EXECUTOR equivalence proof (GPU0 only, measurement only - no registered draw launched): the window-chunked memory-bounded executor `R18-H152_split_exec.py` (pass A scores all windows no-grad in 32-window chunks, pass B re-encodes in 8-window grad chunks, one optimizer step per registered batch, the wrapper's own window-dropout draw and a bitwise-captured full-shape domain-dropout mask keeping the RNG stream in lock-step) against the banked H152 training path on the draw-2 config (seed 3152, init and permutation fingerprints asserted against the banked census values) for the first 50 registered steps plus 5 hand-picked deep batches (deepest rows 40/29/25/17/17 windows, up to 96 pairs). Three runs: reference (banked path, oversubscribed at 7-200 s/step), refnoise (banked path under the cuDNN sdpa backend where the default pick is mem-efficient - the reference-vs-reference noise floor), split. VERDICT PASS: final CUDA RNG state identical across all three runs; split-vs-reference task-loss max/median |diff| 3.15e-04 / 4.97e-05 against a noise floor of 2.28e-04 / 5.54e-05 (domain loss 1.40e-04 vs floor 3.13e-04); no signed bias (sign consistency 0.58); end-of-run raw trunk max-abs weight diff 2.53e-05 vs the noise run's own 2.49e-05 (EMA 6.3e-06 both); drop-mask near-tie eligibility flips 26 split vs 47 noise (immaterial - a dropped window is never the MIL argmax). Split executor peak memory 8.53 GB allocated / 10.35 GB reserved on the deepest batch vs the reference's 34.4 GB allocated - under the 21.81 GB bar with 2x headroom. Driver `R18-H152_exec_equivalence.py` (modes reference/refnoise/split/report, one process per mode); proof JSON `R18-H152_exec_equivalence.json` carries per-step signed diffs, both diff distributions, the verdict logic and the sha256 of all four code files; per-mode records and final raw+EMA states in `_equiv_records_*.json` / `_equiv_state_*.pt`. Numbers recorded, never adjudicated - the draw launch decision returns to the coordinator
- `R18-H155_census.log` - R18-H155 init-vs-order attribution pair CPU dry run (`bash experiments/grounding-semantic/R18-H155_campaign.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched, nothing trained). Dispatches `R18-H155_arm_run.py --stage census`: the clean 685,670-row mix and window census cross-checked against the banked values, then the decoupling proof - init seed 5155 SHARED by both draws (init fingerprint cd8417f37e347a05301cec5c8e9deebd identical, weights + dropout stream paired), perm seeds 51551/51552 (the registered 5155a/5155b) giving different perm fingerprints (07fe223aeb6686fb / 76a057088e834027, 14,299 / 14,301 steps), both distinct from every banked permutation, and the rebound `np.random.default_rng` call site reproducing each census perm. Scripts `R18-H155_arm_run.py`, `R18-H155_campaign.sh`
- `lexical-arena-crossover.log` - R14 lexical arena crossover (`experiments/grounding-lexical/arena_crossover.py`, CPU only, eval-only, no GPU touched, nothing trained). The shipped lexical pipeline (regex `extract_claims` -> per-claim `ground()` under the unmodified bundled config, R12 HIGH manifold, global cut 0.50) scored blind over the frozen RAGBench arena gate (10 subsets, 2,264 items, byte-identical item sets to the semantic reads) plus the RAGTruth EN eval slice (600 items). Item score = min over claim `lex_p`, zero-claim items pass at 1.0. Per-subset checkpoints land in `experiments/grounding-lexical/arena_crossover_ckpt/` and merge to `arena_crossover_ragbench.json` / `arena_crossover_ragtruth.json`; aggregate metrics only, no client text. Terminal marker `=== R14 LEXICAL ARENA CROSSOVER DONE ===`
- `R19_supply_build.log` - R19 supply wave build (`experiments/grounding-semantic/R19_supply_campaign.sh`, CPU + network only, no GPU touched, nothing trained). Three idempotent stages over the six recon-admitted corpora (fava, pubhealth, minicheck, factscore, findver, attributionbench), each checkpointing per corpus: (1) fetch via `scripts/fetch_grounding_datasets.py` with generated sidecars and stage-then-archive (GitHub/Drive corpora as extracted trees), (2) the R14-H136 8-gram Jaccard contamination gate + restriction re-verification per corpus -> `experiments/grounding-semantic/R19_<name>_gate.json`, (3) pair-formatting of gate-GREEN corpora -> `R19_<name>_lane.parquet` + manifest + verify JSON. SUPPLY ONLY - nothing enters a training mix. Resume by re-running the campaign script; completed corpora no-op
- `R18-H156_census.log` - R18-H156 learned window-aggregator twin CPU dry run (`bash experiments/grounding-semantic/R18-H156_campaign.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched, nothing trained). Dispatches `R18-H156_arm_run.py --stage census`: the H150 3-source mix (721,210 rows, 14 DANN groups) with the lane census aborts and the window-census cross-check against the banked `R18-H150_window_census.json` combined block, per-draw fingerprints (seed 1156: init `2b37223cea36b985d6b12c67dcf1fec1`, perm `a75c4b59777d442d`, 15,036 steps; seed 2156: 15,038 steps - both perms distinct from every banked permutation), then the aggregator/cotangent engagement proof on a CPU-tiny stub (zero-init scorer verified, alpha = sigmoid(3) = 0.9526 gated at max, beta/scorer move through the monolithic `train_step`, the cotangent split accumulation reproduces the monolithic gradient bitwise, resume payload round-trips model + aggregator exactly)
- `R18-H156_exec_equivalence.log` - R18-H156 COTANGENT-SPLIT equivalence proof (GPU2 RTX 5000 Ada sm_89 only, measurement only - gates any registered draw): the cotangent window-chunked executor `R18-H156_split_exec.py` (pass A encodes all windows no-grad and backprops the aggregator loss on the detached leaves, yielding per-window cotangents; pass B re-encodes in grad chunks and applies them; one optimizer step per registered batch; one dropout([P, 256]) mask capture per step keeping the RNG stream in lock-step) against the monolithic aggregator-twin reference `R18-H156_arm_run.py::train_step` on the draw-1 config (seed 1156, H150 3-source mix) at REDUCED geometry (8 sets / 16 pairs per batch, split chunks 8/4 - the chunking identity is scale-free in P, recorded in the proof JSON) for the first 50 reduced-geometry steps plus 5 hand-picked deep batches. Three runs: reference, refnoise (cuDNN sdpa backend - the reference-vs-reference noise floor), split. Driver `R18-H156_exec_equivalence.py` (modes reference/refnoise/split/report, one process per mode); proof JSON `R18-H156_exec_equivalence.json` carries per-step signed diffs, both diff distributions, the verdict logic and the sha256 of the code files; per-mode records and final raw + aggregator states in `_equiv_records_h156_*.json` / `_equiv_state_h156_*.pt`. Numbers recorded, never adjudicated
- `R18-H156_smoke.log` - R18-H156 cotangent split executor 200-step smoke on GPU2 (`R18-H156_split_exec.py --draw 1 --max-steps 200`, no checkpoint, no eval): the full draw-1 training path (mix build, fingerprints, kernel self-check, RNG-audited cotangent steps at the registered 48-set / 96-pair geometry) run to measure the launch pace in s/step before the campaign launch
- `R18-H156_campaign.log` - R18-H156 learned window-aggregator twin draw 1 full campaign (`R18-H156_campaign.sh`, GPU by env `GPU=n`, registered GPU2; `PYTORCH_CUDA_ALLOC_CONF` deliberately unset - `expandable_segments` kills `.to("cuda")` under WSL2 on this box). Five idempotent stages, each skipped when its artifact is on disk, with the trainer resuming from `models/R18-H156-arm-draw1/resume.pt` (model + aggregator + optimizer + scheduler + step + permutation fingerprint + RNG states) on the SAME persisted permutation: (1) train under the H150 recipe verbatim (NO EMA, NO window dropout) with task loss BCE(s_agg, y) through the cotangent split executor, plus the in-domain suite -> `R18-H156_arm_draw1_result.json`, checkpoint `models/R18-H156-arm-draw1` with the aggregator sidecar `agg_head.pt`; (2) the PRIMARY blind windowed arena read through the learned aggregator -> `R18-H156_arm_draw1_windowed_agg_result.json`; (3) the registered secondary hard-max windowed read on the same checkpoint through the banked reader unchanged -> `R18-H156_arm_draw1_windowed_result.json`; (4) the anti-gaming held-out near-miss eval at prefix R18-H156 -> `R18-H156_antigaming_draw1_result.json`; (5) the probe bank at the same prefix -> `R18-H156_probes_draw1_result.json`. Terminal markers `=== H156 DRAW 1 COMPLETE ===` and `=== FAILED: <label> ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R18-H158_soup.log` - R18-H158 weight-soup DIAGNOSTIC (`R18-H158_soup.py`, GPU by env `GPU=n`, run on GPU1; `PYTORCH_CUDA_ALLOC_CONF` deliberately unset - `expandable_segments` kills `.to("cuda")` under WSL2 on this box). ZERO TRAINING: three 0.5/0.5 elementwise averages of checkpoints already banked - `same_init` (R18-H155 draw 1 + draw 2, shared init fingerprint `cd8417f3…`), `cross_init` (R18-H150 draw 1 + draw 2), `cross_arm` (R18-H150 draw 1 + R18-H152 EMA draw 1). Two stages: `--stage verify` is CPU-only and touches no card (init-fingerprint agreement with a hard abort when a cell requires a shared init, trunk and task-head key/shape/dtype alignment with the key diff reported on any mismatch, adapter-output-layer-is-zero assertion on both ingredients, and the basin diagnostics - L2 over trunk+task_head, cosine over the flattened trunk in raw and in update space anchored on the pretrained mmBERT trunk); `--stage run` repeats those checks, writes each soup to a NEW directory `models/R18-H158-soup-{same_init,cross_init,cross_arm}` (trunk + task head averaged, domain head and the zero adapter and the tokenizer copied from ingredient A, no ingredient ever overwritten), then reads it through the BANKED blind windowed decomposed-min arena reader `R16-H142_G1_reads.py` reused unchanged (checkpoint binding and output path rebound only, the R18-H150 wrapper pattern) plus gold_full through the banked `R16-H142_G1_arm.score_claims`. Per-cell records checkpoint to `R18-H158_soup_cell_<cell>.json` as they land and merge into `R18-H158_soup_result.json` after every cell, so one failing cell cannot lose the others; a relaunch skips completed cells, soups and reads. DIAGNOSTIC ONLY, binding - the arm has no bars, no promotion path, and no number in the log or the result files is a record, a flagship or a publication claim. Terminal marker `=== R18-H158 SOUP DIAGNOSTIC DONE ===`
- `R19-H159_census.log` - R19-H159 enriched-mix arm CPU dry run (`bash experiments/grounding-semantic/R19-H159_campaign.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched, nothing trained). Dispatches `R19-H159_arm_run.py --stage census`, which rebinds the BANKED G1 twin trainer (`R16-H142_G1_arm.py`) to seed 1159, the 20-group map and the 9-source mix - flagship 721,210 (clean public 685,670 read UNTRUNCATED + R17-H146 misbind 30,000 + R18-H150 unit_swap 5,540, both verified row-for-row against their banked pair and negative-family counts) plus the six R19 supply lanes (FAVA 30,073, AttributionBench 16,444, MiniCheck 14,356, FActScore 13,653, PubHealth 12,251, FinDVer 2,400, each verified on rows + positive count + its own `tag` column) = 810,387 rows. The census stage WRITES `R19-H159_window_census.json`; every later stage cross-checks against it and aborts on disagreement. First run 2026-08-14 09:35 fired the banked BATCH-CAP ABORT - the R19 lane evidence is not chunk-capped, so one AttributionBench row windows to 533 windows at 1,500/750 and 1,359 rows exceed the 96-pair batch cap (factscore 1,341, attributionbench 18); the arm is held for a coordinator ruling on the lane presentation
- `R19-H159_campaign.log` - R19-H159 enriched-mix arm draw 1 full campaign (`R19-H159_campaign.sh`, GPU by env `GPU=n`, placement GPU1 unsplit; `PYTORCH_CUDA_ALLOC_CONF` deliberately unset - `expandable_segments` kills `.to("cuda")` under WSL2 on this box). Four idempotent stages, each skipped when its artifact is on disk, with the trainer resuming from `models/R19-H159-arm-draw1/resume.pt` on the SAME persisted permutation: (1) train under the flagship recipe verbatim (NO EMA, NO window dropout) plus the in-domain suite (gold, gold_full, RAGTruth EN and the seven translations) -> `R19-H159_arm_draw1_result.json`; (2) the PRIMARY blind windowed decomposed-min arena read through the banked reader unchanged -> `R19-H159_arm_draw1_windowed_result.json`; (3) the anti-gaming held-out near-miss eval at prefix R19-H159 -> `R19-H159_antigaming_draw1_result.json`; (4) the probe bank at the same prefix -> `R19-H159_probes_draw1_result.json`. Terminal markers `=== H159 DRAW 1 COMPLETE ===` and `=== FAILED: <label> ===`. NOT YET LAUNCHED - blocked on the census BATCH-CAP abort recorded in `R19-H159_census.log`. Numbers recorded, never adjudicated - the coordinator holds the verdict

Amendment A1 (2026-08-14) rewrote the two R19-H159 entries above: FActScore is withdrawn from the arm (whole-Wikipedia-biography evidence unit - 40.10 mean windows/row, 1,341 rows over the 96-pair cap - needs a lane build with its own registration, not a presentation tweak), the five remaining lanes admit UNTRUNCATED per the twin protocol (the 1,500-char lane cap was priced and rejected as trading train/serve presentation fidelity for arithmetic convenience), and the 18 AttributionBench rows over the cap are dropped at the banked trainer's own guard threshold with the count, the dropped pair total and every dropped row's window count recorded in `R19-H159_window_census.json` under `over_cap_drop` and mirrored into the result JSON. The corrected mix is 796,716 rows over 19 DANN groups, 1,295,811 pairs, mean 1.626 windows/row, max 95, mean target 0.46894, 16,718 packed steps at seed 1159.

- `R19-H160_census_d3.log`, `R19-H160_census_d4.log` - R19-H160 seed-diverse weight-averaging arm CPU dry runs (`DRAW=n bash experiments/grounding-semantic/R19-H160_campaign.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched, nothing trained). Dispatch `R19-H160_arm_run.py --stage census`, which rebinds the BANKED G1 twin trainer (`R16-H142_G1_arm.py`) to the draw's seed and reuses the banked `R18-H150_arm_run.make_build_mix` unchanged, so the flagship 3-source mix (clean public 685,670 read UNTRUNCATED + R17-H146 misbind 30,000 + R18-H150 unit_swap 5,540 = 721,210 rows, 14 DANN groups) is assembled by the flagship's own code with every lane and group abort live and the 1,500/750 window census cross-checked against the banked `R18-H150_window_census.json` combined block. Logs the per-group row and window statistics, the zero-init adapter assertion, the trunk+task_head init fingerprint and the permutation fingerprint, then exits before any model reaches a card. Draw 3 (seed 3150): init `3f1ff2a147e86ede4b965c1f33906f47`, perm `a867296772f8314a`, 15,036 steps. Draw 4 (seed 4150): init `00e3b80a8ad9a3644ab38ea87131d5b1`, perm `709afd02843c742e`, 15,035 steps. Both permutations distinct from every banked draw
- `R19-H160_vram_probe.log` - R19-H160 MONOLITHIC fit probe on GPU2 (32 GB RTX 5000 Ada, pure measurement, no campaign state): `R19-H160_arm_run.py --stage vram_probe` runs the banked unsplit training step at the REGISTERED geometry (48 sets / 96 pairs) for the run's first 8 steps plus the 4 deepest packed batches, reporting `torch.cuda.max_memory_allocated()` per step. VERDICT FAIL - peak allocated 33.21 GB / reserved 56.88 GB against the card's 31.99 GB total and the 27.19 GB bar (85%); no OOM only because WSL2 oversubscribes GPU memory into system RAM, at 14-22 s/step on the deep batches against ~1.4 s/step for the split path. Confirms the banked H152 measurement on the card actually in use and settles the executor choice; writes `R19-H160_vram_probe_draw3.json`
- `R19-H160_exec_equivalence.log` - R19-H160 SPLIT equivalence proof (GPU2 only, measurement only - gates any registered draw): the cotangent window-chunked executor `R19-H160_split_exec.py` (pass A encodes every window no-grad in chunks and backprops the banked MIL max-BCE on the detached logit leaf, yielding per-window cotangents; pass B re-encodes in grad chunks and applies them plus the chunk domain CE; one optimizer step per registered batch; one `dropout([P, hidden])` mask capture per step keeping the RNG stream in lock-step) against the monolithic reference `R19-H160_arm_run.py::train_step` - the banked `R16-H142_G1_arm.main()` loop body transcribed verbatim - on the draw-3 config (seed 3150, H150 3-source mix) at REDUCED geometry (8 sets / 16 pairs, split chunks 8/4; the chunking identity is scale-free in P, recorded in the proof JSON) over the first 50 steps plus 5 hand-picked deep batches. Three runs: reference, refnoise (cuDNN sdpa backend - the reference-vs-reference noise floor), split. VERDICT PASS: final CUDA RNG state identical across all three runs (`be1167cdeed5b32a`) and init fingerprints identical; split-vs-reference task-loss max/median |diff| 6.96e-04 / 9.30e-05 against a noise floor of 3.47e-04 / 1.09e-04, domain loss 4.34e-04 vs floor 3.89e-04; no signed bias (sign consistency 0.54, mean signed +8.3e-06); end-of-run raw trunk+task_head max-abs weight diff 1.84e-05 vs the noise run's own 1.54e-05. Driver `R19-H160_exec_equivalence.py` (modes reference/refnoise/split/report, one process per mode); proof JSON `R19-H160_exec_equivalence.json` carries per-step signed diffs, both diff distributions, the verdict logic and the sha256 of all five code files; per-mode records and final raw states in `_equiv_records_h160_*.json` / `_equiv_state_h160_*.pt`
- `R19-H160_split_smoke_gpu0.log` - R19-H160 split executor 30-step smoke on GPU0 (`R19-H160_split_exec.py --draw 3 --max-steps 30`, no checkpoint, no eval): the full draw-3 training path (mix build, fingerprints matching the census, kernel self-check, RNG-audited cotangent steps at the registered 48-set / 96-pair geometry) run to price the launch pace and the peak memory on the 24 GB card before the campaign launch - 1.46 s/step including warmup, ~8.9 GB resident
- `R19-H160_campaign_d3.log`, `R19-H160_campaign_d4.log` - R19-H160 draw 3 (seed 3150, GPU0) and draw 4 (seed 4150, GPU2) full campaigns (`GPU=n DRAW=m bash experiments/grounding-semantic/R19-H160_campaign.sh`; `PYTORCH_CUDA_ALLOC_CONF` deliberately unset - `expandable_segments` kills `.to("cuda")` under WSL2 on this box; every H160 entry point aborts if `CUDA_VISIBLE_DEVICES` is unset or equals 1, because GPU1 is reserved exclusively for R19-H159). Two idempotent stages, each skipped when its artifact is on disk, with the trainer resuming from `models/R19-H160-arm-draw<N>/resume.pt` (model + optimizer + scheduler + step + permutation fingerprint + both RNG states) on the SAME persisted permutation: (1) train the R18-H150 flagship recipe VERBATIM (3-source mix, 14 DANN groups, MIL max-over-windows BCE, adapter frozen at zero, NO EMA, NO window dropout) through the cotangent split executor, plus the in-domain suite (gold, gold_full, RAGTruth EN and the seven translations) -> `R19-H160_arm_draw<N>_result.json`; (2) the blind windowed decomposed-min arena read through the banked reader unchanged -> `R19-H160_arm_draw<N>_windowed_result.json`. The draws are the ingredients of the arm's PRIMARY soup and also carry the recipe-reproduction check (each draw >= 0.7079, the H150 registered draw bar). Terminal markers `=== H160 DRAW <N> COMPLETE ===` and `=== FAILED: <label> ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R19-H160_soup.log` - R19-H160 soups, reads and bar table (`GPU=n bash experiments/grounding-semantic/R19-H160_campaign.sh --soup`). ZERO TRAINING. `R19-H160_soup.py` imports the BANKED `R18-H158_soup.py` and calls its helpers directly for the fingerprint/key/shape/dtype verification, the adapter-zero assertion, the basin diagnostics (L2 over trunk+task_head, cosine over the flattened trunk in raw and in update space anchored on the pretrained mmBERT trunk) and, for the k = 2 PRIMARY, its `write_soup` verbatim; `write_soup_k` is the uniform 1/k generalisation used only by the registered k-sweep secondaries. Three cells: `soupB` (the PRIMARY - draws 3 + 4, the UNSEEN confirmatory soup), `k3` (H150 d1 + d2 + H160 d3) and `k4` (all four draws). UNIFORM average only - greedy or score-selected soups are barred by the registration, and no selection exists in the code. Each soup is written to a NEW directory `models/R19-H160-soup-{B,k3,k4}` (no ingredient is ever overwritten), then read through the BANKED blind windowed decomposed-min arena reader `R16-H142_G1_reads.py` reused unchanged (checkpoint binding and output path rebound only) plus the in-domain path - the full banked `evaluate()` on soup B, so the registered non-EN hold is measured, gold_full alone on the k-sweep cells. Per-cell records checkpoint to `R19-H160_soup_cell_<cell>.json` and merge into `R19-H160_soup_result.json` after every cell, so one failing cell cannot lose the others. The stage then runs the anti-gaming held-out near-miss eval and the probe bank on soup B at prefix `R19-H160-soupB` (with `models/R19-H160-soupB-arm-draw1` linked to the soup so the banked readers resolve it - the R16-H142_T_draw2.sh pattern), and finally `R19-H160_report.py`, which assembles every registered bar: the PRIMARY (>= 0.72049 GRADUATE, < 0.71549 KILL), the ten variance-aware subset floors (floor_i = flagship_i - max(0.02, swing_i, 2 x SE_i), SE Hanley-McNeil from the frozen R8-H77 arena class counts), the holds (gold_full >= 0.84, non-EN >= 0.82, anti-gaming >= 0.7438), the per-draw 0.7079 check and the k-sweep monotonicity -> `R19-H160_bars_report.json`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R19-H161_dump.log` - R19-H161 distribution-shift autopsy, shared substrate dump (GPU0, ANALYSIS ONLY, zero training). `R19-H161_dump.py` scores all 10 frozen RAGBench arena subsets under three banked checkpoints (`models/R18-H150-arm-draw{1,2}` and `models/R19-H159-arm-draw1`) through the banked read path, keeping per-PAIR logits with full window provenance, and joins the deterministic per-pair text features (token containment, Jaccard, numeral containment, longest common n-gram) computed once into `R19-H161_features.parquet`. Each checkpoint is gated by a positive control requiring its reproduced per-subset AUROC to match the banked windowed value to <= 1e-3 and the structural fingerprint (items / sentences / pairs) to match exactly; a miss voids that checkpoint rather than being adjusted away. Outputs `R19-H161_pairs_{h150d1,h150d2,h159d1}.parquet` + `R19-H161_dump_schema.json`. Markers `=== H161 DUMP COMPLETE <tag> ===`, `=== H161 DUMP ALL COMPLETE ===`, `=== H161 DUMP FAILED: <reason> ===`
- `R19-H161_chain.log` - R19-H161 lane chain (`R19-H161_chain.sh`): waits on the dump's completion marker, aborts on its failure marker, then runs the three lanes that read the substrate - L1 overlap-prior sensitivity (CPU), L4 window-count strata and argmax-window drift (CPU), L3 adversarial group geometry (GPU0). Written by the coordinator after a session limit killed the fanout's executors at 17:08 on 2026-08-14; their lane scripts survived on disk and only the wait-and-run wrappers were missing. Lane results land in `R19-H161_L{1,3,4}_result.json`
- `R19-H161_L2.log` - R19-H161 lane L2, failure-class autopsy extended to finqa, tatqa and delucionqa across all three checkpoints, reusing the R18-H157 taxonomy verbatim so the classes stay comparable with the banked finqa autopsy. delucionqa carries no tables and is the discriminator between the column-binding-dilution and overlap-prior-suppression hypotheses. Own wait-and-run wrapper `R19-H161_L2_chain.sh` (same structure as `R19-H161_chain.sh`, which covers L1/L3/L4 only) polls the dump log for its completion marker and then runs `R19-H161_L2.py` on CPU. Adds one named taxonomy class, `near_copy_verbatim`, as an orthogonal overlay on the dump's frozen surface features rather than a change to the H157 precedence chain. Results `R19-H161_L2_result.json` + `R19-H161_L2_items.parquet`
- `R19-H162_hotpotqa_probe.log` - R19-H162 hotpotqa mechanism dissection, stage 1 (CPU ONLY, ANALYSIS ONLY, zero training, no GPU). `R19-H162_hotpotqa_probe.py` reads the frozen 250-item hotpotqa gate sample through the banked loader (`R8-H77.load_subsets` -> `R8-H92.sentences`) and takes every model score from the banked `R19-H161_pairs_h150d1.parquet` dump rather than recomputing any. Gated by a structural fingerprint (250 items / 293 sentences / 1,177 pairs) and a positive control requiring the reconstructed max-over-windows AUROC to match the banked 0.6766 to <= 1e-3. Measures the hop census (documents needed per claim sentence by greedy anchor set cover), partial-support saturation (positive vs negative separation of the max window logit split by hop class), the deterministic pooling counterfactual (mean / logsumexp / softmax / top-2 aggregations of the SAME per-window logits, all ten subsets, isolating the aggregation axis without retraining) and argmax-window provenance. Outputs `R19-H162_hotpotqa_probe.json`, `R19-H162_hotpotqa_sentences.parquet`, `R19-H162_hotpotqa_eyeball.md`
- `R19-H162_hotpotqa_families.log` - R19-H162 stage 2, claim-family taxonomy over the stage-1 per-sentence records. `R19-H162_hotpotqa_families.py` classifies each multi-document claim sentence as `bridge_entity` (the intermediate entity linking the claim's endpoints is elided and shared by the covering documents), `conjoin_attrs` (one attribute per entity, the two clauses' best documents differ) or `multi_doc_other`, then reads per-family positive/negative separation with bootstrap CIs, runs the length and anchor-count confound controls, and cross-checks the hop census against the `R16-H140_cache/arena_hotpotqa.npz` cross-window flags computed by an independent method. Outputs `R19-H162_hotpotqa_families.json`, `R19-H162_hotpotqa_families.parquet`, `R19-H162_hotpotqa_families_eyeball.md`; `R19-H162_hotpotqa_consolidate.py` then merges both stages into the wave deliverables `R19-H162_hotpotqa_mechanisms.{md,json}`
- `R19-H163_instrument_audit.log` - R19-H163 probe-bank instrument audit (CPU ONLY, zero GPU, ANALYSIS ONLY). `R19-H163_instrument_audit.py` asks whether the campaign's probe bank has ever predicted the blind arena, over nine explicit (probe_file, arena_file) checkpoint PAIRS with pre-registered per-probe TARGET subsets fixed before the read. Spearman rank correlation plus a two-sided permutation null (exact enumeration at n <= 8, 200,000 samples at n = 9). Verdict rule stated before the read: SUPPORTED if rho > 0 and p < 0.05, DEAD-AS-INSTRUMENT if rho < 0, otherwise INDETERMINATE. RESULT: 0 SUPPORTED, 6 DEAD, 4 INDETERMINATE, with `verbatim_mean_arm` significantly ANTI-predictive on its target (rho -0.8000, p 0.0137). The bank remains valid as an INSTALLATION check and is dead as a STEERING instrument; no later arm may carry a probe clause as a gate. Writes `R19-H163_instrument_audit.json`
- `R19-H165_ladder.log` - R19-H165 context-length ladder (`R19-H165_ladder_chain.sh`, gold held-out ONLY, never the arena, inference only on the banked `R18-H150-arm-draw1` checkpoint - nothing trains). Six cells sweeping the serving presentation the campaign has never revisited since R8-H101: L0 per-chunk 1500/750 at MAX_LEN 512 (the banked presentation, POSITIVE CONTROL), C0 pool-concatenated at the same 512 (isolates CONCATENATION alone - `R16-H142_G1_reads.evidence_sets` windows PER CHUNK, so a wider window can never merge two documents by itself), then L1/L2/L3/L4 concatenated at 1024/2048/4096/8192 with WIN held at ~3.6 chars per token and STRIDE at the banked 2:1. L0 GATES the rest: it must reproduce the checkpoint's banked `gold_full` AUROC of 0.8659 to <= 1e-3 through this script's own code path, and a miss prints `=== H165 LADDER VOID` and exits before any other cell is spent. Control PASSED at 0.8659 (abs delta 1e-05, 123,579 pairs, 44.905 windows/item, 2,083 s, peak 3.57 GB), after which the chain fans out - small cells on GPU0, L2 on GPU2, the two long cells on GPU1. Motivation on record: the trunk is ModernBERT at `max_position_embeddings` 8192 and the campaign serves it at 512, a 16x under-use; on the banked arena dump only 7.3% of items fit their whole evidence pool in one 512-token window against 82.4% at 4,096. Per-cell results in `R19-H165_ladder_<cell>_R18-H150-arm-draw1.json`; markers `=== H165 CELL COMPLETE ===`, `=== H165 CELL FAILED: <cell> ===`, `=== H165 LADDER COMPLETE ===`
- `R19-H168_fetch.log` - R19-H168 EuroBERT-210m weight fetch. `EuroBERT/EuroBERT-210m` was absent from the local HF cache, so this is the one network step in the arm; `HF_HUB_OFFLINE` is unset for the fetch only and `HF_HUB_DISABLE_XET=1` is set (Xet segfaults on large files on this box). Pulls config, safetensors, tokenizer and the model's PINNED remote code (`configuration_eurobert.py`, `modeling_eurobert.py` - EuroBERT cannot load without `trust_remote_code`). 1.24 GB of weights: 211.8M encoder parameters plus a 98.5M untied masked-LM head that the arm never uses. Marker `=== FETCH COMPLETE ===`
- `R19-H168_gate_a.log` - R19-H168 GATE A, the load kill-gate (GPU2 RTX 5000 Ada sm_89 - the compatibility fallback for custom remote kernels, chosen over the Blackwell cards deliberately). `R19-H168_trunk_gate.py` runs seven fatal checks: config and tokenizer load, the RoPE compatibility shim plus its analytic positive control, encoder-only load as `AutoModel` (never the MaskedLM head), the parameter arithmetic against the campaign's published 211.7M / 98.5M / 113.2M split and the 400M budget, hidden size 768 so the mmBERT recipe's heads transfer with ZERO dimensional change, a real fp32 forward, a bf16-autocast forward against it, and the trainer's true 96 x 512 batch shape. First run FAILED at stock load with `KeyError: 'default'` - EuroBERT's pinned code targets transformers 4.40.0.dev0 and this project runs 5.14.1, which removed that key from `ROPE_INIT_FUNCTIONS`; a second trap was then found where 5.x migrates `rope_theta` into `rope_parameters` while also exposing an unrelated `default_theta = 10000.0`. Both closed by `R19-H168_eurobert_compat.py`. The gate's masked-token control then failed 1/3 against mmBERT-base's 3/3 and was ruled NON-DECISIVE on recorded grounds (it cannot separate a weaker cloze model from a degraded one, and fine-tuning discards the masked-LM head entirely); supporting diagnostics in the same log confirm the encoder runs BIDIRECTIONALLY and that `lm_head` is trained rather than randomly initialised
- `R19-H168_gate_b.log` - R19-H168 GATE B, the gate that actually decides the arm (GPU1). `R19-H168_trunk_gate_b.py` measures how linearly separable grounding already is in the FROZEN trunk, mmBERT-base against EuroBERT-210m under an identical protocol: 6,000 class-balanced, group-stratified rows of the PUBLIC TRAINING mix (not the arena, not `gold_full` - nothing is tuned and no threshold is chosen), one window per row, MAX_LEN 512 held identical, frozen `[CLS]`, 5-fold stratified CV logistic probe with the scaler fitted INSIDE each fold. Verdict rule stated before the read: DEAD if EuroBERT < 0.55 while mmBERT > 0.60, DEGRADED if the gap exceeds 0.10, else PROCEED. RESULT `PROCEED` by 0.0068 - mmBERT-base 0.68936 (sd 0.00943), EuroBERT-210m 0.59612 (sd 0.01783), gap 0.09324. Carries the arm's tokenizer-confound census as a free rider: on identical text EuroBERT spends 11.4% more tokens on average with the excess concentrated on the non-English RAGTruth variants (hu +20.1%, pl +20.0%, it +16.8%, de +16.4%, es +14.9%, cn +12.0%) while English is 2.6% cheaper, so at a fixed MAX_LEN of 512 the truncated share moves against EuroBERT precisely where the non-EN hold applies. Writes `R19-H168_trunk_gate_b.json`
- `R19-H168_campaign_d1.log` - R19-H168 EuroBERT-210m trunk-swap draw 1 (`R19-H168_campaign.sh`, GPU1 exclusive). WAITS for the `=== H165 LADDER COMPLETE ===` marker before taking the card - the H158/H159 record carries a co-location stall precedent - then runs two idempotent stages, each skipped when its result JSON is already on disk so a container restart can simply re-run the script: (1) train via `R19-H168_arm_run.py --stage train`, (2) the PRIMARY blind windowed decomposed-min arena read via `--stage windowed`. The runner does NOT restate the recipe: it imports `R18-H150_arm_run` and calls that module's own `rebind()`, so the mix builder, 14-group map, lane manifests, every census abort and the seed all come from the flagship's file and cannot drift. SEED is deliberately held at the flagship's 1150 - the census dry run reproduced perm fingerprint `7d13f9ac86a79574`, so this run consumes the identical rows in the identical order as banked draw 1, which is what makes the difference attributable to the trunk. Three forced load-time deviations, all recorded: `H108.STUDENT` repointed to `EuroBERT/EuroBERT-210m` (the arm), `trust_remote_code=True` injected by monkey-patching `AutoModel`/`AutoTokenizer` in-process so no banked file is edited, and the `R19-H168_eurobert_compat` RoPE shim installed with its analytic control re-checked as a fatal pre-flight. Census-before-spend passed at 1,068,905 pairs over 721,210 rows, student 212.8M parameters, adapter zero-init verified True, 15,036 steps. Markers `=== H168 DRAW 1 COMPLETE ===`, `=== H168 TRAIN FAILED`, `=== H168 READ FAILED`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H172_census_d5.log` / `R20-H172_census_d6.log` - R20-H172 CPU census dry runs (no GPU): mix and window census through the banked H150 assembly, init and permutation fingerprints for seeds 5150 (`a8e708538a5decd8`) and 6150 (`a4244751f7bb646b`), both verified distinct from all ten banked fingerprints, 15,038 / 15,035 steps
- `R20-H172_campaign_d5.log` / `R20-H172_campaign_d6.log` - R20-H172 flagship variance draws 5 (GPU1, seed 5150) and 6 (GPU2, seed 6150), executing author ruling 2 of 2026-08-16 (flagship recipe to k=6 same-recipe draws). `R20-H172_campaign.sh` -> `R20-H172_flagship_run.py`, which injects draws 5/6 into the banked `R19-H160_arm_run.py` and routes the split executor's own module load to the injected copy so training runs through byte-identical banked code. Two idempotent stages per draw (train + in-domain suite, then the PRIMARY blind windowed arena read); resume from `models/R20-H172-arm-draw<N>/resume.pt`; relaunch = same command. No promotion bar - the deliverable is the k=6 mean and its SE 0.00485. Markers `=== H172 DRAW <N> COMPLETE ===`, `=== FAILED: <stage> ===`
- `R20-H174_lane_build.log` - R20-H174 hagrid/emanual portfolio arm, STAGE 0 lane builds and contamination censuses (CPU ONLY, no GPU, nothing trained). Appended by four scripts under `experiments/grounding-semantic/`, all reproducible and idempotent in the sense that re-running rewrites the same parquet from the same seed: `R20-H174_lane_L1.py` (seed 1174, DANN group `frame_reject`) generates the vacuous_claim_reject lane - minimal pairs over one evidence chunk where the label-1 leg is a genuine MiniCheck/VitaminC supported claim and the label-0 leg is assembled ONLY from a closed contentless inventory of provenance frames, discourse fillers, citation marks and bare reference lines, with the frame drawn once per pair and used byte-identically on both legs so frame presence carries zero label information (the registration's frame-NEUTRAL requirement, protecting emanual's grounded recap items) and the negative assembled to the positive's character length so the lane cannot teach "short implies unsupported"; `R20-H174_lane_L2.py` (seed 2174, group `attr_pool`) builds the source_select lane by pooling one true passage with 3-7 BM25-retrieved distractor passages from OTHER documents of the same corpus (MiniCheck MIT + VitaminC train CC-BY-SA-3.0 only - FAVA / PubHealth / FinDVer / AttributionBench are deliberately NOT read, the registration's isolation clause against the H159 table collapse), in two families - `truth_removed`, where the label-0 leg is the same claim against the same distractors with the true passage replaced by one more distractor, and `unsupported_claim`, where two claims about the same document face a byte-identical pool; `R20-H174_lane_L4.py` (seed 4174, group `path_bind`) generates synthetic manual pages whose procedures state a navigation path as a bare token run and whose claims state it arrowed, with negatives that transpose two adjacent path levels (token multiset preserved exactly) or substitute a sibling segment the same page attests, and level assignment randomised from one shared segment pool so no claim-only probe can learn the menu tree; `R20-H174_lane_census.py` then re-runs the R14-H136 wall (`provenance_gate.py`, 8-gram, Jaccard >= 0.3, bidirectional, KILL > 2%, thresholds read from `R19_supply_gates.py`) against all ten walled arena corpora on each built lane's deduplicated evidence units and claims, with the spike control first. Per-lane artifacts `R20-H174_lane_{L1,L2,L4}.parquet`, `_manifest.json` and `_census.json`. Markers `=== R20-H174 LANE <L> BUILT ===` / `FAILED BARS`, `=== <L> CENSUS GREEN ===` / `RED`
- `R20-H174_window_census.log` - R20-H174 STAGE 1 AMENDMENT, the census re-bank (CPU ONLY, no GPU, nothing trained). `experiments/grounding-semantic/R20-H174_window_census.py` reads the clean public mix UNTRUNCATED the way the twin trainer reads it and windows every row of all five lanes at 1,500/750, then computes the combined block from the RAW per-row window counts rather than from rounded per-part means. The portfolio mix is 760,618 rows / 1,215,222 windows / mean 1.5977 / multi-window 0.2094 against the banked flagship geometry 721,210 / 1.4821 / 0.1908, the move being L2 `attr_pool`'s multi-passage pooling (mean 5.9813 windows, 99.77% multi-window, max 13). The script ASSERTS the registration's pre-stated 760,618 / 1.5977 / 0.2094 before writing and aborts with `CENSUS MISMATCH ABORT` without writing anything if the built mix disagrees - so the re-banked `R20-H174_window_census.json` cannot become a constant fitted to whatever was assembled. This repoints the flagship wrapper's `census_crosscheck`; it does not weaken it
- `R20-H174_census_d1.log` / `R20-H174_census_d2.log` - R20-H174 CPU census dry runs (`DRAW=n bash experiments/grounding-semantic/R20-H174_campaign.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched). Dispatch `R20-H174_arm_run.py --stage census`, which assembles the 6-source portfolio mix through the banked `R18-H150_arm_run.make_build_mix` with every lane, group and row abort live and the window census cross-checked against the re-banked `R20-H174_window_census.json`. Both runs reproduce 760,618 rows over 17 DANN groups, mean target 0.484, 1,215,222 pairs, mean set 1.598, max 40 (under the 96-pair cap), and the cross-check prints an exact match on all three figures. Draw 1 (seed 1174): init `2b86c651032042c1e5d009c1854c46e5`, perm `ded543769d14f9e3`, 15,900 steps. Draw 2 (seed 2174): init `3339c96a85d134e9ea49ac0f3ba81770`, perm `a42b9d29e07c9db0`, 15,902 steps. Both permutation fingerprints verified distinct from all twelve banked draws of this recipe
- `R20-H174_campaign_d1.log` / `R20-H174_campaign_d2.log` - R20-H174 hagrid/emanual portfolio arm STAGE 1, training draws 1 (GPU1, seed 1174) and 2 (GPU0, seed 2174). `R20-H174_campaign.sh` -> `R20-H174_arm_run.py`, which injects the 5-lane `LANES` tuple, the 17-group map, the 760,618-row count and the re-banked census path into the banked `R18-H150_arm_run` module, installs the H174 draws in the banked `R19-H160_arm_run` wrapper with its permutation guard widened to cover H160 draws 3/4 and H172 draws 5/6, and routes both modules' own re-loads to the injected copies so training runs through byte-identical banked code (the R20-H172 injection pattern). Recipe is the H150 flagship VERBATIM - untruncated evidence windowed 1,500/750, MIL max-over-windows BCE, DANN lambda 0.02 Ganin ramp, adapter frozen at zero init, MAX_LEN 512, <= 48 sets / <= 96 pairs, LR 1e-5 OneCycleLR, 1 epoch, H126 double seeding, no EMA, no window dropout - executed by the cotangent split executor `R19-H160_split_exec.py` (pass A 32 no-grad windows, pass B 8 grad windows). Two idempotent stages per draw (train + in-domain suite, then the blind windowed decomposed-min arena read); resume from `models/R20-H174-arm-draw<N>/resume.pt`; relaunch = same command. After training the wrapper relabels the result JSON and the checkpoint fingerprint file so the record names H174 rather than the H150 recipe string the banked executor writes; no measured number is touched. Draws 3 and 4 are declared at k=4 but launched only on the coordinator's word after draw 1 clears its gate. Markers `=== H174 DRAW <N> COMPLETE ===`, `=== FAILED: <stage> ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H174_census_d3.log` - R20-H174 draw 3 CPU census dry run (`DRAW=3 bash experiments/grounding-semantic/R20-H174_campaign.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched). Same dispatch as the draw 1/2 censuses; reproduces 760,618 rows over 17 DANN groups, 1,215,222 pairs, mean set 1.598, max 40, and the rebound cross-check prints an exact match on all three figures (760,618 / 1.5977 / 0.2094). Draw 3 (seed 3174): init `f962da87a2b071807ebe4512db778787`, perm `f58c12ea6bac5542`, 15,905 steps. Note the census stage prints the permutation fingerprint but does NOT run the collision guard - that check lives in the split executor's train path and fires at launch
- `R20-H174_campaign_d3.log` - R20-H174 draw 3 training on GPU1 (seed 3174), same script and stages as draws 1/2 above. Committed by the coordinator after draw 1 cleared its kill gate (blind arena mean 0.71806 against the 0.695 bar, table guard passing under Amendment G1). The wrapper's permutation-collision guard was widened here from 12 to the 21 fingerprints the R20-H175b launch adopted, which crucially adds THIS ARM'S OWN draws 1 and 2 (`ded543769d14f9e3`, `a42b9d29e07c9db0`) - without them draw 3 could have collided with its siblings unseen. Widening only strengthens the guard; no bar or measured number is affected. Markers `=== H174 DRAW 3 COMPLETE ===`, `=== FAILED: <stage> ===`, `PERM COLLISION ABORT`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H174_census_d4.log` - R20-H174 draw 4 CPU census dry run (`DRAW=4 bash experiments/grounding-semantic/R20-H174_campaign.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched), plus the permutation-collision guard run appended to the same file. Same dispatch as the draw 1/2/3 censuses; reproduces 760,618 rows over 17 DANN groups, 1,215,222 pairs, mean set 1.598, max 40, and the rebound cross-check prints an exact match on all three registered figures (760,618 / 1.5977 / 0.2094). Draw 4 (seed 4174): init `0c390045e27c3085016db489ae062f7c`, perm `bc0e0d075f712076`, 15,900 steps. The guard is the new disk-derived one (`R20_perm_guard.assert_distinct`), not the wrapper's hardcoded set: 24 prior fingerprints derived from result JSONs and campaign logs, draw 4 distinct from all of them, and the three H174 siblings visible to it include draw 3's `f58c12ea6bac5542`, which the wrapper's hand list does NOT carry. The dry run prints draw 4's own fingerprint into this log, so the check moves this one file aside for the duration and restores it, keeping the comparison against genuinely prior state
- `R20-H174_campaign_d4.log` - R20-H174 draw 4 training on GPU2 (seed 4174), same script and stages as draws 1-3 above. The last of the declared k=4; committed by the coordinator alongside draw 3 when draw 1 cleared its kill gate. Launch fingerprints reproduce the CPU dry run exactly (init `0c390045e27c3085016db489ae062f7c`, perm `bc0e0d075f712076`, 15,900 steps) and the window-census cross-check prints the exact three-figure match. Markers `=== H174 DRAW 4 COMPLETE ===`, `=== FAILED: <stage> ===`, `PERM COLLISION ABORT`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H174_mechanism_gates_d1.log` - R20-H174 mechanism gates A and B, the two registered gates the campaign wrapper prints but never computes (GPU1, ANALYSIS ONLY, nothing trained). `R20-H174_mechanism_gates_d1.py` scores `models/R20-H174-arm-draw1` on the frozen hagrid gate sample through the banked `R19-H161_dump` read path, banks the per-pair dump `R20-H174_pairs_h174d1.parquet`, and then calls `R19-H162_hagrid_mechanisms.py`'s own `misrank_block`, `vacuous_excluded` and `kdoc_curve` unmodified so both gate definitions are the banked ones rather than re-invented. Gate A is the frame-only share of misrank mass, where misrank mass is the count of (positive, negative) item pairs with `pos_score <= neg_score`; gate B is AUROC by evidence-pool depth with the four frame-only items removed, plus the k-truncation curve as a secondary. The banked flagship draws and the R19-H159 enriched-mix existence proof are read under the identical protocol so the comparison is paired, and every checkpoint is gated by a positive control requiring its hagrid AUROC to reproduce the banked windowed read to <= 1e-3 with the 250/537/1941 fingerprint exact. The absolute frame-only and total misrank pair counts are banked next to the share because the share is a ratio a worse-ranking checkpoint can lower without suppressing anything. Output `R20-H174_mechanism_gates_d1.json`. Markers `=== H174 MECHANISM GATES COMPLETE ===`, `=== H174 MECHANISM GATES FAILED: <reason> ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H176_gate_batch.log` - R20-H176 FinDVer instrument read plus the eight-item LICENSED FREE KILL-GATE BATCH (measurement only - nothing trained, nothing registered; GPU0 only, GPUs 1/2 left to the flagship draws). Appended by five scripts under `experiments/grounding-semantic/`. `R20-H176_findver_read.py` (GPU0) scores both banked flagship draws on the 2,400-row FinDVer lane with the shipped windowed protocol - untruncated evidence windowed 1,500/750, claim scored against every window, MAX over windows, no min-over-sentences stage because FinDVer rows are claim-level - and records per-subset AUROC plus the FP/FN split at the macro-F1 operating point -> `R20-H176_findver_read.json` and per-draw score vectors. `R20_gates_derivation.py` (CPU) runs gates 1-3: the depth-1 compare/sign/direction reclassification of the H157 error records against the autopsy's own rank-loss vector (reproduced bit-exact at 0.2797), the FP-explanation misbind classification, the EDGAR + TabFact constructibility censuses and the mmBERT tokenizer fragmentation contrast -> `R20_gate_{1,2,3}.json`. `R20-G0a_hotpotqa_diffgaps.py` (CPU) banks the difference-of-label-gaps statistic over the four same-recipe checkpoints with a seed and estimator sensitivity sweep -> `R20-G0a_hotpotqa_diffgaps.json`. `R20-G0b_composed_probes.py` (CPU generator + short GPU0 read) builds 250 bridge + 250 conjunction TabFact two-table-join probes on HELD-OUT tables only (HotpotQA train and HoVer never touched) and reads the banked flagship draw 1 on them -> `R20-G0b_composed_probes.parquet`, `R20-G0b_composed_probe_baseline.json`. `R20_gates_pubmedqa.py` (CPU) runs the two absence-family supply censuses and reproduces the fanout design-pass replications from the banked dumps -> `R20_gate_{6,7}.json`, `R20_fanout_replications.json`. `R20_gate_batch_collect.py` merges the eight per-gate artifacts into `R20_gate_batch_result.json`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H177_lane_build.log` - R20-H177 numeric-verification portfolio arm, STAGE 0 lane builds, held-out mechanism evals and contamination censuses (CPU ONLY, no GPU touched, nothing trained). Appended by four scripts under `experiments/grounding-semantic/`, all idempotent - a lane whose parquet, manifest and passing verify block are on disk is skipped, `--force` rebuilds from the same seed to the same bytes. `R20-H177_lane_common.py` holds the shared machinery: the source wall in code (admitted supply is exactly the banked EDGAR-restricted slice `R18-H150_edgar_admitted.parquet` and public TabFact TRAIN through `R17-H144_pairs.py`; FinQA/TAT-QA source corpora, HotpotQA train and HoVer are never opened, and FEVEROUS is recorded NOT ADMITTED because the on-disk parquet is an R14-H133 working file rather than an admitted supply with its own provenance verdict), the EDGAR prose extractors (role/value bindings, stated changes, year-bound values, from-to trends, all returning the VERBATIM printed surface), the attestation and value-uniqueness instruments, the deterministic blake2b document split that makes the evals doc-disjoint, and a direction-stratified claim-only probe (H145 finding b - unstratified document folds read BELOW chance and would pass the bar falsely); the banked verify instruments themselves are imported from `R20-H174_lane_common.py`, not re-implemented. `R20-H177_lane_B.py` (seed 1177, DANN group `num_compare`) builds the compare/direction lane - four families over one shared evidence chunk per pair where the twin differs by exactly ONE relation word: `cmp_order` and `cmp_extreme` over TabFact serialisations (ordering of two rows' cells; the highest/lowest among the rows the chunk actually serialises, requiring a unique max AND a unique min so the flip is false rather than merely unstated), `cmp_amount` and `cmp_trend` over EDGAR MD&A prose (ordering of two labelled amounts; a stated from-X-to-Y movement kept only where the stated direction AGREES with the printed ordering). Both operands are printed in the claim and in the evidence, so every numeric channel is identical inside a pair by construction and the only claim-side signal is the relation word, which explicit per-(family, word) target cells hold at an exact 50/50 positive/negative split. `R20-H177_lane_C.py` (seed 1178, group `num_rolebind`) builds the operand-role/sign/period misbind lane over EDGAR prose only - `role_swap` (the amount reattached to another labelled quantity the same passage binds to a different amount), `sign_swap` (the direction the passage states in words, flipped; verb frames not "an increase / a decrease", because a nominal frame would move the article as well as the direction word) and `period_swap` (the amount rebound to another year the passage names, with the corrupting year barred from the sentence carrying the true binding). Every asserted amount is printed verbatim AND occurs exactly once in its passage, so the corrupted binding is not attested elsewhere; role_swap and period_swap are emitted in COUPLES over the same passage, which makes every role phrase and every year appear exactly as often on the supported leg as on the corrupted one. Each lane also writes a held-out mechanism eval (`R20-H177_eval_{B,C}.parquet`, ~1,000 pairs, same generator at seeds 2177/2178, source documents excluded from the training lane) - these are the artifacts the H177 PRIMARY per-lane gates consume; no model is read on them here. `R20-H177_lane_census.py` then re-runs the R14-H136 wall (`provenance_gate.py`, 8-gram, Jaccard >= 0.3, bidirectional, KILL > 2%, thresholds read from `R19_supply_gates.py`) against all ten walled arena corpora on both lanes AND both evals, spike control first, and merges each verdict back into the owning manifest so a manifest stays self-contained. Artifacts `R20-H177_lane_{B,C}.parquet`, `R20-H177_eval_{B,C}.parquet`, `R20-H177_lane_{B,C}_manifest.json`, `R20-H177_{lane_B,lane_C,eval_B,eval_C}_census.json`. Markers `=== R20-H177 LANE <B|C> BUILT ===` / `FAILED BARS`, `=== <artifact> CENSUS GREEN ===` / `RED`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H178_fusion_read.log` - R20-H178 LEXICAL-LATE-FUSION, the zero-training paired system read on the banked flagship pair `models/R18-H150-arm-draw{1,2}` (GPU0 only; GPUs 1/2 left to the H172 draws, nothing trains). `R20-H178_fusion_read.py` blends the deterministic lexical token-containment score into the cross-encoder's per-pair score as `(1-w)*sigmoid(logit) + w*tok_containment` at the (sentence, window) grain and then leaves the shipped aggregation untouched - max over the sentence's windows, MIN over the response's sentences on the arena; max over the claim's chunks in domain - with ONE global subset-blind `w`. Four stages, each idempotent against its on-disk artifact: (1) the two binding sanity gates, w=0 reproducing the banked windowed arena reads to <= 1e-4 on every subset of both draws and the lexical scorer alone reproducing R19-H162's banked containment AUROCs (emanual 0.7763, delucionqa 0.5889) to <= 1e-3, a miss printing `=== R20-H178 ABORT: SANITY GATE MISSED ===` before any arena read is taken; (2) the in-domain per-pair cache on GPU0 through `R19-H161_dump.score_pairs` for `gold_full` and the seven RAGTruth translations -> `R20-H178_indomain_draw{1,2}.parquet`; (3) `w` selected over {0.05 ... 0.50} on `gold_full` ONLY, never the arena, restricted to the values holding `gold_full` >= 0.84 and non-EN >= 0.82 on both draws; (4) ONE blind fused arena read per draw at the selected `w` plus the w=0 baseline. Arena model scores are the banked per-pair dumps `R19-H161_pairs_h150d{1,2}.parquet` rather than a recompute, which the registration sanctions and gate (a) re-verifies. Under amendment A1 the fused figure is a SYSTEM number banked as `system_mean` in `R20-H178_result.json` and never substitutes for the model arena mean. Markers `=== H178 GATES DONE ===`, `=== H178 INDOMAIN DONE ===`, `=== H178 COMPLETE ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H175b_qlane_build.log` - R20-H175b STAGE 0, the question-relevance contrast lane build plus its contamination census (CPU ONLY, no GPU touched, nothing trained). `experiments/grounding-semantic/R20-H175b_qlane.py` (seed 1175 train / 2175 eval, DANN group `qrel_contrast`) builds the supervision that makes the later question-conditioning measurement attributable: nothing in the 721k-row mix teaches question RELEVANCE, so without it a question-conditioned arm can train the channel to a no-op and return a null indistinguishable from "the channel does not help". Every pair is two rows over the SAME PsiloQA wiki passage and the SAME claim (the passage's own `golden_answer`) differing ONLY in the `question` field - label 1 carries the claim's originating question, label 0 carries another question over that very same passage, so topic, entity, vocabulary, language and register are held byte-identical and only relevance moves. A unit is one passage, a subset S of its distinct questions and a DERANGEMENT on S, which makes every question appear exactly once as the true question and exactly once as the wrong one; the consequence, re-measured rather than asserted, is that every claim-only, evidence-only and question-only statistic has AUROC exactly 0.5 and only a question-x-claim interaction can separate the classes. Because a true question naturally shares more words with its own answer, the builder picks per passage the (subset, derangement) minimising the largest sd-normalised gap over five question-x-claim lexical channels, admits passages in ascending mismatch order to a 15,000-pair target, then trims the prefix back to the last point where all twelve barred surface channels stay within 0.04 of 0.5. halueval-qa and ragtruth_en were measured and REJECTED as sources (63 of 9,936 knowledge blocks carry a second question; all 839 ragtruth QA contexts carry exactly one query), so the lane is single-corpus by supply, not by choice. `R20-H175b_lane_census.py` then runs the R14-H136 wall (`provenance_gate.py`, 8-gram, Jaccard >= 0.3, bidirectional, KILL > 2%, thresholds read from `R19_supply_gates.py`) against all ten walled arena corpora on three unit types - evidence passages, claims and, new to this lane, QUESTIONS - with the spike control first, and merges each verdict back into the owning manifest; this is also PsiloQA's first 8-gram census, the corpus having entered the mix at R8-H84 before the instrument existed. Artifacts `R20-H175b_qlane.parquet`, `R20-H175b_qlane_eval.parquet`, `R20-H175b_qlane_manifest.json`, `R20-H175b_{qlane,qlane_eval}_census.json`. Markers `=== R20-H175b QLANE BUILT ===` / `FAILED BARS`, `=== <artifact> CENSUS GREEN ===` / `RED`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H175b_qlane_repair_build.log` - the coordinator-ordered REPAIR PASS on the same lane: identical corpus, guards, document split and seeds, with admission trimmed additionally until a composite lexical-interaction probe (converged liblinear over all twelve barred surface channels plus their squares, 5-fold document-disjoint out of fold) clears 0.55, an instrument this build ADDED beyond the registered bars. Run as `R20-H175b_qlane.py --repair`, writing the parallel artifacts `R20-H175b_qlane_repaired.parquet`, `R20-H175b_qlane_eval_repaired.parquet`, `R20-H175b_qlane_repaired_manifest.json`. Both manifests report `all_bars_pass` over the REGISTERED bars only and `all_bars_pass_including_composite_probe` separately, so an added instrument never silently enters the registered conjunction. Marker `=== R20-H175b QLANE_REPAIRED BUILT ===` / `FAILED BARS`
- `R20-H175b_qlane_repair_sweep.log` - the repair diagnostic that priced the three candidate levers before the repaired lane was built: grounding-floor widening of the candidate pool (claim/passage containment floor 0.5 -> 0.35 -> 0.0, pairs available 16,019 -> 18,185 -> 21,746), an eight-channel L2 matching objective in place of the five-channel max, and windowed greedy vector balancing of the running signed-gap vector in place of plain ascending-mismatch admission. Measured at four volumes each; balancing moves the composite probe by <= 0.004 and pool widening by <= 0.03, so the probe is a monotone function of VOLUME rather than of matcher quality. Scratch script, not a repository artifact; the numbers are what the repair report rests on
- `R20-H175b_qlane_census.log` - the standalone four-artifact run of `R20-H175b_lane_census.py` (original lane and eval, repaired lane and eval) after both builds existed, re-merging every verdict into the two manifests
- `R20-H173_soup_reads.log` - R20-H173 SOUP CLOSURE READS, the k=6 uniform soup plus four registered pair soups (GPU2 ONLY - GPUs 0 and 1 carry the R20-H174 training draws; ZERO TRAINING, weight averaging over banked checkpoints and deterministic blind reads only). `experiments/grounding-semantic/R20-H173_soup.py` loads the BANKED `R19-H160_soup.py` (which itself loads `R18-H158_soup.py`) and calls its functions directly - `verify_members` for fingerprint distinctness, key/shape/dtype alignment, non-float buffer equality and the adapter-zero assertion, `H158.basin_diagnostic` for the pairwise L2 and update-space cosines against the pretrained mmBERT anchor, `H158.write_soup` verbatim for every k=2 cell and `write_soup_k` for the uniform 1/6 average, `H158.null_criterion` / `k_null` for the reporting bands, and `run_cell` itself; this module adds only the six-ingredient table (H172 draws 5 and 6 are new since H160), the H173 cell configuration and H173-namespaced read paths, so no H158/H160 artifact is touched. The averaged scope is trunk + task head, confirmed against the banked cell artifacts' `averaged_scope` - the domain head is training-only and copied from the first member, the adapter is frozen at zero in every member and carried through. A FREE-RIDER read-path control runs FIRST and gates everything: draws 3 and 4 are re-read through this process's own reader and diffed per subset against `R19-H160_arm_draw{3,4}_windowed_result.json` at a <= 1e-4 bar, which doubles as the identity proof that the weights on disk are the ones behind those banked reads; a miss prints `=== READ-PATH CONTROL ABORT ===` and exits before a single soup exists. Five cells then run in order: `S6` (the PRIMARY - the uniform equal-weight average of ALL six flagship endpoints, compared to the banked k=6 single-draw mean 0.71218 under the pre-registered +/-0.005 branches) and the four SECONDARY pair soups `p56` (d5,d6) and `p35` (d3,d5), both split-only, against `p15` (d1,d5) and `p26` (d2,d6), both mixed-executor, each compared to the mean of its OWN two ingredients. Every soup is written to a NEW directory `models/R20-H173-soup-{S6,p56,p35,p15,p26}` (no ingredient is ever overwritten) and read through the BANKED blind windowed decomposed-min arena reader `R16-H142_G1_reads.py` reused unchanged (checkpoint binding and output path rebound only), plus gold_full through the banked `R16-H142_G1_arm.score_claims` - recorded as a diagnostic, this arm carries no in-domain hold. UNIFORM AVERAGING ONLY over exactly the registered ingredient sets: greedy soups, best-subset search and fitted weights are barred by the registration and no selection exists in the code, and no soup outside those five is read. Per-cell records checkpoint to `R20-H173_soup_cell_<cell>.json` and merge into `R20-H173_soup_result.json` after every cell, so one failing cell cannot lose the others; the control lands in `R20-H173_readpath_control.json`. Idempotent across container restarts - a cell whose record is on disk is skipped, an existing soup checkpoint is reused, a read whose result JSON exists is not repeated, relaunch = the same command. Markers `=== R20-H173 SOUP CLOSURE READS DONE ===`, `=== READ-PATH CONTROL ABORT ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20_baseline_legs.log` - three pre-registered BASELINE LEGS on the banked flagship checkpoints (`R20_baseline_legs.py`, GPU2 only, inference only - nothing trains, nothing is tuned). Each leg is a "read this BEFORE the arm trains" step written into its arm's registration, because each arm's PRIMARY is a mechanism gate stated as a rise FROM a measured baseline and is unattributable without it. LEG 1 (R20-H177 numeric-verification portfolio arm): flagship AUROC on the two banked held-out mechanism evals `R20-H177_eval_B.parquet` (1,000 pairs / 458 docs, compare/direction) and `R20-H177_eval_C.parquet` (968 pairs / 213 docs, role/sign/period misbind); registered prediction near-chance, arm PRIMARY after training >= 0.80. LEG 2 (R20-H175b question conditioning): flagship AUROC on `R20-H175b_qlane_eval.parquet` (1,001 pairs / 487 docs) - both rows of a pair carry the SAME claim and SAME evidence chunk and the label lives entirely in the question, so a model with no question channel MUST score the two legs identically and read exactly 0.5000; the leg confirms the eval loads through the standard path, fixes the floor at exactly chance, and counts pairs whose two legs are bit-identical. The banked surface floor 0.5816 is carried through from the stage-0 disposition, not recomputed. LEG 3 (R19-H166 amendment A1): flagship BINARY serving scalar on a held-out VitaminC REFUTES-vs-NEI split, REFUTES positive / NOT ENOUGH INFO negative; registered prediction near-chance since the binary objective cannot express contradiction-versus-absence, arm PRIMARY after training >= 0.85 on the new `con_head`. The held-out split is built from VitaminC `__test` + `__validation` (the flagship mix reads `__train` only), then filtered to zero collisions with the train split on `unique_id`/`case_id`/`page`/`claim`/`evidence`/`wiki_revision_id` AND to zero claim/evidence/pair collisions against the fully assembled 721,210-row flagship training mix rebuilt through the banked loader; residual overlap raises `H166-A1 DISJOINTNESS ABORT` rather than reporting a contaminated number. Read protocol is the shipped one, byte-identical to `R20-H176_findver_read.py` - evidence untruncated, 1,500/750 windows, MAX over windows, frozen trunk + task head via `R15_gate_common.load_ckpt`/`.score`, both banked flagship draws `R18-H150-arm-draw{1,2}`. Writes `R20-H177_baseline_leg.json`, `R20-H175b_baseline_leg.json`, `R19-H166-A1_baseline_leg.json` plus per-set score arrays `R20_baseline_legs_scores_*.npy`. Marker `=== R20 BASELINE LEGS COMPLETE ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict
- `R20-H177_evalC_diagnostic.log` - R20-H177 eval_C ATTRIBUTION DIAGNOSTIC (`R20-H177_evalC_diagnostic.py`, GPU2 only, inference only - nothing trains). Registered by the coordinator on 2026-08-17 off the R20-H177 baseline leg, which found the banked flagship already reading `R20-H177_eval_C.parquet` at 0.9085 against a registration that predicted near-chance. Separates two explanations: (a) the 0.9085 comes from the `R17-H146_lane` 30,000-row `quant_misbind` DANN group already in the flagship mix, so Lane C is largely redundant with an installed lane; or (b) any competent grounding checkpoint reads role/period misbind at ~0.9, so eval_C measures general grounding competence rather than the targeted channel. The separator is a comparable-quality checkpoint trained WITHOUT the misbind lane: the windowed-MIL twin pair `R16-H142-G1-twin` (seed 1142, banked arena mean 0.72498) and `R16-H142-T-draw2` (seed 2142, 0.70073), which bracket the flagship pair (0.71436 / 0.71661). Provenance is proved from each checkpoint's own `init_fingerprint.json` rather than by date - the twin pair carries 12 DANN groups with no `quant_misbind` and no `quant_scale_unit`, the flagship pair carries 14 with both, and a mismatch in either direction raises `DIAGNOSTIC ABORT` before a card is touched. `models/R16-H142-T-arm-draw1` is an untracked symlink to `models/R16-H142-G1-twin` (canonical log, R16-H142 T holds block), so the real directories are used. Second control on the same draws: `R20-H177_eval_B.parquet`, to confirm Lane B's ~0.5 floor holds on old and new checkpoints alike and therefore measures something genuinely absent rather than something H146 installed. Read path byte-identical to `R20_baseline_legs.py` and `R20-H176_findver_read.py` - evidence untruncated, 1,500/750 windows, MAX over windows, frozen trunk + task head via `R15_gate_common.load_ckpt`/`.score`. Confound recorded, not corrected: neither mix contains EDGAR text and eval_C is 100% EDGAR prose, so both sides are equally EDGAR-naive. Writes `R20-H177_evalC_diagnostic.json` with the per-neg-family side-by-side (role_swap / sign_swap / period_swap) plus score arrays `R20-H177_evalC_diagnostic_scores_*.npy`. Marker `=== R20-H177 EVALC DIAGNOSTIC COMPLETE ===`. Numbers recorded, never adjudicated - the coordinator holds the interpretation
- `R20-H175a_census.log` - R20-H175a concat-only trained-through arm, the STAGE 1 AMENDMENT census (CPU ONLY, `CUDA_VISIBLE_DEVICES=""`, no GPU touched, nothing trained, nothing launched). `experiments/grounding-semantic/R20-H175a_window_census.py` assembles the flagship 3-source mix through the banked loaders, computes each row's window count under BOTH presentations - the banked per-document one and the arm's `windows("\n\n".join(pool))` - and censuses the read sets where a multi-document pool actually exists (all ten arena subsets plus `gold_full`). Lane composition is asserted against the banked `R18-H150_arm_run.LANES` tuple, the clean-mix and combined row counts against that module's own constants, and the combined block is recomputed from the per-part totals before anything is written. Result: the two training presentations are IDENTICAL row for row (721,210 rows / 1.4821 mean windows / 0.1908 multi-window / 137,622 multi-window rows, matching the banked `R18-H150_window_census.json` exactly), because every training row carries a single evidence document and the join of a one-element pool is the identity - so `census_crosscheck` does not abort, the pre-stated rebind is not required, and the arm's presentation change has no purchase on training. On the read side concatenation REDUCES windows (arena x0.8789 over all ten subsets; x0.9639 on sentence-weighted pairs recomputed from the banked H165 and H150 arena result files), while `gold_full` gives x1.7367 - the registration's "+74% inference per item" is that in-domain figure, which the banked in-domain protocol produces by truncating each chunk to exactly one window. Artifact `R20-H175a_window_census.json`. Numbers recorded, never adjudicated - the coordinator holds the verdict

- `R20-H175b_census.log` - R20-H175b question-conditioning arm, the STAGE 1 census rebind (CPU ONLY, `CUDA_VISIBLE_DEVICES=""`, no GPU touched, nothing trained). `experiments/grounding-semantic/R20-H175b_window_census.py` recomputes the combined window census of the 4-source question-conditioned mix (clean public 685,670 + R17-H146 misbind 30,000 + R18-H150 unit_swap 5,540 + the stage-0 `R20-H175b_qlane.parquet` contrast lane 17,972 = 739,182 rows over 15 DANN groups) and banks it as `R20-H175b_window_census.json` so the arm's `census_crosscheck` is REPOINTED rather than weakened - it still hard-aborts on drift. The combined figures are not known in advance, so every component is asserted before anything is written: the flagship sub-mix must reproduce the banked `R18-H150_window_census.json` combined block exactly (721,210 / 1.4821 / 0.1908 / 137,622), the contrast lane must reproduce the window census banked in its own `R20-H175b_qlane_manifest.json` (17,972 / 1.1108 / 0.0739 / 1,328), and the combined row count must be 739,182. Measured combined geometry: 739,182 rows, mean 1.4731 windows, multi-window share 0.1880, 138,950 multi-window rows, 1,088,869 total windows, max 40. The question channel does not enter this census - the arm leaves the evidence side and the windowing untouched. Marker `=== R20-H175b WINDOW CENSUS BANKED ===`

- `R20-H175b_qproof.log` - R20-H175b INTERVENTION PROOF (CPU ONLY, no GPU, nothing trained). `R20-H175b_arm_run.py --stage qproof` assembles the full 739,182-row mix and proves, on the built mix and before a card is committed, that the arm's single registered intervention actually changes the model input - the discipline adopted at the R20-H175a withdrawal, where a registered intervention turned out to be an exact identity on the training mix. `R20-H175b_qchannel.py` replays `R10-H108_lane.public_train()`'s source order to recover the question field that loader drops, and proves the replay aligned by comparing the replayed claim list against the loader's own output row for row (a mismatch is `QUESTION-CHANNEL ABORT`). The MANDATORY loader assertion registered at stage 0 then runs: a question must be composed for all 17,972 rows of the `qrel_contrast` lane, every composed input must differ from its bare claim, all 8,986 pairs must differ as composed strings, and a 400-pair sample must differ as TOKENIZED model inputs - any failure is a hard abort, because both legs of a lane pair share claim and chunk and the label lives entirely in the question. Measured: question coverage 114,774 / 739,182 rows = 15.53% mix-wide (96,802 / 721,210 = 13.42% on the flagship sub-mix, matching the registration's ~97k / 13-14%), sourced from ragtruth_en `query` 15,090, halueval-qa `question` 20,000, psiloqa `question` 61,712 and the contrast lane 17,972; 0 pairs identical as strings, 0 of 400 identical as tokens. Artifact `R20-H175b_intervention_proof.json`. Marker `=== R20-H175b INTERVENTION PROOF WRITTEN ===`

- `R20-H175b_census_d1.log` - R20-H175b draw 1 CPU census dry run (`DRAW=1 bash experiments/grounding-semantic/R20-H175b_campaign.sh --census`, `CUDA_VISIBLE_DEVICES=""`, no GPU touched). Dispatches `R20-H175b_arm_run.py --stage census`, which assembles the mix through the banked `R18-H150_arm_run.make_build_mix` with every lane, group and row abort live, composes the question channel with the loader assertion firing, and cross-checks the window census against the re-banked `R20-H175b_window_census.json`. Reproduces 739,182 rows over 15 DANN groups, mean target 0.483, 1,088,869 pairs, mean set 1.473, max 40 (under the 96-pair cap). Draw 1 (seed 1175): init fingerprint `806ae8b02206e84d24a3fff1179ac17e`, permutation fingerprint `54b26347e6467e34`, 15,411 steps. The permutation guard was widened from 8 to 21 banked fingerprints (adding R18-H156 d1, the two R20-H174 draws now training, and the earlier-recipe draws on disk) and reports the draw distinct from all of them

- `R20-H175b_smoke_d1.log` - R20-H175b draw 1 twenty-step GPU2 smoke, run before the real launch to verify the GPU path and to check the init and permutation fingerprints against the CPU dry run. Writes no resume point (the first is at step 1,000) and no result JSON, so it cannot poison the committed run

- `R20-H175b_campaign_d1.log` - R20-H175b QUESTION CONDITIONING (MEASUREMENT ONLY) STAGE 1, training draw 1 on GPU2 (seed 1175). GPUs 0 and 1 carry the R20-H174 draws and are not touched. `R20-H175b_campaign.sh` -> `R20-H175b_arm_run.py`, which injects the 3-lane `LANES` tuple, the 15-group map, the 739,182-row count and the re-banked census path into the banked `R18-H150_arm_run` module, wraps that module's `make_build_mix` with the question channel, installs the H175b draw in the banked `R19-H160_arm_run` wrapper with its permutation guard widened to 21 fingerprints, and routes both modules' own re-loads to the injected copies so training runs through byte-identical banked code (the R20-H172 / R20-H174 injection pattern). Recipe is the H150 flagship VERBATIM - untruncated evidence windowed 1,500/750, MIL max-over-windows BCE, DANN lambda 0.02 Ganin ramp, adapter frozen at zero init, MAX_LEN 512, <= 48 sets / <= 96 pairs, LR 1e-5 OneCycleLR, 1 epoch, H126 double seeding, no EMA, no window dropout - executed by the cotangent split executor `R19-H160_split_exec.py`, with ONE addition: an optional question prefix `"<question[:256]> [SEP] <claim>"` on the claim side of the cross-encoder input, and the bare claim wherever no question exists. Four idempotent stages (intervention proof, train + in-domain suite, blind windowed arena read, PRIMARY question-relevance mechanism read); resume from `models/R20-H175b-arm-draw1/resume.pt`; relaunch = same command. The in-domain suite and the arena read pass BARE claims, so both are the empty-question robustness read rather than question-conditioned ones. After training the wrapper relabels the result JSON and the checkpoint fingerprint file so the record names H175b rather than the H150 recipe string the banked executor writes; no measured number is touched. MEASUREMENT ONLY - no promotion route to the shipped `ground()` / `ground_batch()` API, and nothing under `src/groundrails/` is modified. DRAW 1 ONLY is committed (QUEUE AMENDMENT Q1); further draws exist only on the coordinator's word. Markers `=== H175b DRAW 1 COMPLETE ===`, `=== FAILED: <stage> ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict

- `R20-H175b_qeval_read.log` - R20-H175b PRIMARY mechanism read (`R20-H175b_qeval_read.py`, GPU2 only, inference only - nothing trains). Reads the trained `models/R20-H175b-arm-draw1` checkpoint on the held-out contrast eval `R20-H175b_qlane_eval.parquet` (1,001 pairs / 487 documents, 0 documents shared with the training lane) with the question composed into the claim side, and reports the AUROC against the two BANKED floors - 0.5000, the question-blind flagship measured exactly with 1,001 of 1,001 pairs bit-identical, and 0.5816, the stage-0 surface-probe floor - the gate being >= 0.80 against the higher of them. Neither floor is recomputed. Read protocol is byte-identical to `R20_baseline_legs.py` LEG 2 apart from the composed question: untruncated evidence windowed 1,500/750, MAX over windows, frozen trunk + task head via `R15_gate_common.load_ckpt`/`.score`. Carries its own registered control - the SAME checkpoint on the SAME eval with NO question composed, where both legs of a pair are byte-identical inputs and the read must therefore return exactly 0.5000 with 1,001/1,001 pairs bit-identical; a departure means the read path separates the legs by something other than the question and the PRIMARY number is not cleanly attributable. Writes `R20-H175b_qeval_read.json` plus score arrays `R20-H175b_qeval_scores_*.npy`. Marker `=== R20-H175b PRIMARY MECHANISM READ COMPLETE ===`. Numbers recorded, never adjudicated - the coordinator holds the verdict

- `R20-H175b_qlane_eval_clean.log` - R20-H175b held-out question-relevance eval REBUILT MIX-DISJOINT (`R20-H175b_qlane_eval_clean.py`, CPU ONLY, `CUDA_VISIBLE_DEVICES` forced empty before any import - no card touched, nothing trained). Opened because the banked eval `R20-H175b_qlane_eval.parquet` was verified document-disjoint from the contrast lane only and never against the TRAINING MIX: 449 of its 487 passages are present in the mix through the 61,712 PsiloQA rows `R10-H108_lane.public_train()` carries, so a model can score it by recalling which question went with which answer over a passage it trained on, and both registered floors (0.5000 question-blind, 0.5816 surface probe) are structurally blind to that because a memoriser still needs the question. Construction, guards and bars are unchanged - `R20-H175b_qlane.py` is imported and its `passage_units` / `assemble` / `dedupe` / `verify` called, so only the SUPPLY moves, under a fresh seed 3175. The rebuild's starting premise is REFUTED in the log rather than assumed: PsiloQA's splits are cut per question, not per document, so 5,368 of 5,687 validation+test passages are byte-identical to a train passage the mix carries and "validation and test are clean by construction" is false. The operative criterion is therefore membership of the assembled mix, measured directly - `public_train()` with the evidence cut lifted plus all three arm lanes, 359,960 distinct raw chunks, tested in raw, truncated-to-1,500 and whitespace-collapsed case-folded forms. All three PsiloQA splits are then pooled and filtered on that criterion, which leaves 559 of 26,161 passages absent from the mix and 55 carrying two or more admitted questions. The log also carries the ceiling measurement that the verdict rests on: over the whole clean pool the five question-x-claim lexical channels read 0.70-0.75, so the overlap matching has no freedom left, and an exhaustive prefix scan (not the builder's binary search, which assumes a monotonicity the predicate loses at this scale) finds the largest surface-parity-clean prefix at 8 passages / 16 pairs on the builder's 0.04 trim margin and 13 passages / 26 pairs on the registered 0.05 bar. The memorisation feature the adversarial reviewer built - overlap between the eval claim and any `llm_answer` the mix pairs with that leg's question over that same passage - is measured on BOTH evals so the contaminated floor is on the record. Artifacts `R20-H175b_qlane_eval_clean.parquet`, `_manifest.json`, `_census.json`, `_report.json`; the banked contaminated eval is left untouched as evidence. Marker `=== R20-H175b CLEAN EVAL <status> ===`. This first pass returned BLOCKED at 8 passages / 16 pairs and is SUPERSEDED by the balanced rebuild below; its artifacts are kept under the `_prefix` names as evidence. Measurements only - the coordinator holds the verdict

- `R20-H175b_qlane_eval_clean_balanced.log` - the coordinator-authorised REBUILD of the same clean eval under a better SELECTOR, same script with `SELECTOR = "balanced"` (CPU ONLY, no GPU touched). NO BAR MOVES: surface parity stays 0.05, the trim margin stays 0.04, every leak bar and the census stay as registered, and the supply is the identical 54-passage mix-absent pool. What changes is which passages are admitted. The registered rule admits in ascending mismatch order and keeps a PREFIX, which was chosen when the candidate pool held thousands of passages; on a 54-passage pool it can only cut where that fixed order happens to leave the channels balanced and it reaches 8 passages / 16 pairs. The balanced selector chooses the SET instead - it grows one passage at a time, always taking the passage that leaves the worst barred channel closest to 0.5, and keeps the largest set still inside the 0.04 margin. The criterion is recomputed on each candidate subset's OWN text, its own IDF and after its own de-duplication, so the number the selector optimises is the number the finished artifact is measured on; tokenisation is memoised because the selector calls the channel table about 1,500 times. Result 22 passages / 44 pairs / 88 rows at worst deviation 0.0398, 2.75x the prefix rule, with all eleven registered bars passing, 0 eval passages in the mix across all six string forms, 0 shared with the contrast lane, census GREEN at max 8-gram fraction 0.0 on all three unit types and the memorisation feature undefined at 0% coverage. Reported beside it and NOT promoted into the registered conjunction: the composite lexical-interaction probe reads 0.5625, and language coverage falls to 4 (en, fi, hi, ar) from the banked eval's 14. Artifacts `R20-H175b_qlane_eval_clean.parquet`, `_manifest.json`, `_census.json`, `_report.json`. Marker `=== R20-H175b CLEAN EVAL BUILT ===`

- `R20-H175b_eval_contamination_sweep.log` - R20 BLAST RADIUS scoping (`R20-H175b_eval_contamination_sweep.py`, CPU ONLY, no GPU touched, nothing rebuilt). Asks how far the R20-H175b pattern reaches - a held-out eval verified disjoint only from its own lane and never against the training mix - by checking every held-out eval parquet in the campaign against the same assembled mix in the same six string forms. `gold_full` is excluded, being a different source already audited by `R20_goldfull_split_audit.py`. Measured: `R20-H175b_qlane_eval.parquet` 485/487 passages in the mix (99.6%), `R20-H175b_qlane_eval_repaired.parquet` 406/407 (99.8%), `R20-H177_eval_B.parquet` 33/736 (4.5% on its tabfact half - **19 of the 33 are byte-identical raw matches**, only the remaining 14 need normalisation; the "all through the whitespace-normalised form" reading was a misreading of this log's own `by_form` block and is corrected by `R20-H177_evalB_assessment.log`), `R17-H143_evalset.parquet` 10/547 (1.8%, normalised-form only - 0 raw, 0 truncated, which is where that description does hold), against `R20-H177_eval_C.parquet` 0/380 and the rebuilt clean eval 0/22. Artifact `R20-H175b_eval_contamination_sweep.json`. Marker `=== SWEEP DONE ===`. Measurements only - the coordinator holds the verdict
- `R20-H177_evalB_assessment.log` - R20-H177 Lane B, is the 4.5% eval_B contamination LOAD-BEARING? (`R20-H177_evalB_contamination_assessment.py`, CPU ONLY, no GPU touched, nothing trained or rebuilt). Re-derives the contamination against the FLAGSHIP mix the baseline-leg checkpoints actually trained on (721,210 rows: `public_train()` untruncated + `R17-H146_lane` + `R18-H150_scaleunit_lane`) in the same six string forms, then recomputes the banked baseline leg with the affected rows excluded from the banked per-row score arrays `R20_baseline_legs_scores_eval_B_h150d{1,2}.npy` - the banked per-draw AUROCs are reproduced exactly before any exclusion is applied, and a mismatch aborts rather than producing a number. Measured: 33/736 passages (19 raw, 14 normalisation-only), 126 rows / 63 pairs / 33 docs, all tabfact, families cmp_order 86 and cmp_extreme 40; 2-draw mean floor 0.5064 all rows -> 0.5075 clean (+0.0011), with the contaminated rows alone reading 0.4900; the passage-keyed memorisation-feature analogue reads 0.5043 on those 126 rows at 100% coverage, with 58 of 63 pairs at exactly zero within-pair feature spread. Also measured, and NOT in the sweep: eval_B shares one byte-identical passage with its own training lane `R20-H177_lane_B` under a different `doc_id`, TabFact serialising one table as both `1-` and `2-` prefixed csv ids (15 such stem collisions between lane and eval). Artifact `R20-H177_evalB_contamination_assessment.json`. Marker `=== banked -> ... ===`. Measurements and a soundness reading only - the coordinator holds the verdict

- `R20_goldfull_split_audit.log` - R20 data-integrity audit asking whether the in-domain held-out set `gold_full` is disjoint from the assembled training mix (`R20_goldfull_split_audit.py`, CPU ONLY, torch-free path, `CUDA_VISIBLE_DEVICES` forced empty before any import - no card is touched, nothing trains, no banked number is recomputed). Opened because a baseline-leg build found VitaminC's official split disjoint by `unique_id`/`case_id` but NOT by page, claim text, evidence text or `wiki_revision_id`, and VitaminC supplies over half the mix; `gold_full` had never been audited the same way. Both sides are rebuilt through the banked loaders rather than reimplemented - `R10-H108_lane.gold_full()` for the hold, and `R10-H108_lane.public_train()` read untruncated via `R16-H142_G1_arm.untruncated_evidence()` plus the two lanes named in `R18-H150_arm_run.LANES` for the mix, aborting on any row / pair / family drift. Three channels are measured: exact-match on claim strings, evidence strings and (claim, evidence) pairs, against both the raw mix chunks and the 1,500/750 windows the model was actually shown; near-duplicate overlap through the campaign's own instrument `provenance_gate.run_gate` in the R14-H136 ruling-2 form (8-gram, Jaccard >= 0.3, bidirectional, spike control, thresholds read from `R19_supply_gates.py`) with the training mix standing in for the arena side so the per-bucket breakdown attributes any hit to a DANN group; and provenance, which reports the identifier fields that actually exist on each side plus a verbatim scan for gold trace / user ids in mix text. Results bank incrementally to `experiments/grounding-semantic/R20_goldfull_split_audit.json`. Stage markers `=== STAGE ASSEMBLE DONE ===`, `=== STAGE EXACT DONE ===`, `=== STAGE PROVENANCE DONE ===`, `=== STAGE CENSUS CLAIMS DONE ===`, `=== STAGE CENSUS EVIDENCE DONE ===`, `=== AUDIT DONE ===`. The same log carries the appended run of `R20_goldfull_split_audit_control.py`, which adds two controls the audit cannot supply itself: a REAL-OVERLAP control offering VitaminC's own official test-split claims to the identical gate against the identical mix - genuine near-duplicates by construction, since VitaminC train supplies 370,653 of the 721,210 mix rows - so a hit there beside zero for `gold_full` separates a clean corpus from a gate that cannot fire; and a reproduction of the VitaminC official-split collision counts that motivated the audit, with the distinct-value counts behind them. Marker `=== CONTROL DONE ===`. Measurements only - the coordinator holds the verdict
- `contract-path-bind-verify.log` - dataset-contract verification of the `path_bind` training member (R20-H174 lane L4, 10,000 rows / 5,000 pairs) against clauses C1-C8 of `docs/experiments/dataset-contract.md`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty; instruments reused, not reinvented - the banked containment / claim-only probe / surface-parity helpers from `R20-H174_lane_common.py`, the R14-H136 8-gram Jaccard >= 0.3 bidirectional census from `provenance_gate.py`, and the lane's own generator for the C4 live positive control. Reports the two legs' claim-to-evidence containment distributions (C1), exact-string disjointness in three forms against 38 evaluation surfaces (C2), the census with a graded near-duplicate sensitivity ladder (C4), and the leak and memorisation-channel probes (C5, C6). Script `experiments/grounding-semantic/contract/path_bind_contract_verify.py`, artifact `experiments/grounding-semantic/contract/path_bind_contract_report.json`; counts and metrics only, no evaluation-surface text is emitted

- `psiloqa_contract.log` / `psiloqa_contract_addendum.log` / `psiloqa_contract_fixability.log` / `psiloqa_contract_summary.log` - dataset-contract verification of the `psiloqa` training member (61,712 rows, 9.0% of the 685,670-row clean mix) against clauses C1-C8 of `docs/experiments/dataset-contract.md`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - the three R20-H174 draws on GPUs 0/1/2 are untouched. The member is rebuilt through the BANKED loader (`R10-H108_lane.public_train()` with the evidence cut lifted, rows filtered to the `psiloqa` DANN group) and every instrument is reused rather than reinvented: `provenance_gate.py` in the R14-H136 ruling-2 form for the census (8-gram, Jaccard >= 0.3, bidirectional, KILL > 2%, spike control, thresholds read from `R19_supply_gates.py`), `R20_gates_pubmedqa.content`/`.containment` for C1, `R20-H175b_qlane_eval_clean.norm` for the whitespace-collapsed case-folded form. Because the corpus spans 14 languages and the banked containment tokenizer is ASCII-only, a Unicode-aware companion is reported BESIDE it with coverage stated, never substituted - and the two straddle the C1 rejection band, which the addendum resolves with scale-free AUROC, per-language and per-claim-length readings and both parses of the clause sentence. The fixability log measures the counterfactual each FAIL needs (row subsets that clear the C1 band; passages that would have to leave for C2) and proposes nothing. C4 carries a LIVE positive control as the clause requires: the corpus's own validation+test passages, near-duplicate by construction because the splits cut per (passage, question), fire at 93.4% with mean Jaccard 0.9336. Scripts under `experiments/grounding-semantic/contract/psiloqa_contract*.py`, artifact `experiments/grounding-semantic/contract/psiloqa_contract_report.json`; counts, distributions and identifiers only, no evaluation-surface text emitted. Measurements only - the coordinator holds the verdict

- `contract-quant_scale_unit-*.log` (`mixcache`, `c2c4`, `c3c5c6`, `c5banked`, `nearmiss`) - dataset-contract verification of the `quant_scale_unit` training member (R18-H150 unit_swap lane, 5,540 rows / 2,770 pairs, DANN group `quant_scale_unit`) against clauses C1-C8 of `docs/experiments/dataset-contract.md`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - the three R20-H174 draws on GPUs 0/1/2 are untouched. The mix is rebuilt through the BANKED path (`R10-H108_lane.public_train()` read untruncated via `R16-H142_G1_arm.untruncated_evidence()` plus both `R18-H150_arm_run.LANES`, reproducing 721,210 rows exactly) and every instrument is reused rather than reinvented: the R20-H175b precedent containment measure for C1 beside the R19-H161 frozen content measure and a unit-resolved variant built from the lane's own `UNITS` table; `provenance_gate.py` in the R14-H136 ruling-2 form for C4 (8-gram, Jaccard >= 0.3, bidirectional, KILL 2%, spike control) with a LIVE positive control offering `R17-H145_scaleunit`'s chunks, near-duplicate by construction from the same TabFact/FEVEROUS train tables through the same serializers; and the lane's own `R18-H150_scaleunit_lane.verify()` for the whole C5 leak suite, run under `PYTHONHASHSEED=0` over a rebuilt corpus that reproduces the manifest's admission counts exactly (23,423 candidate tables, 3,516 carrying eval content, 16,738 admitted, all 410 lane documents resolved). The `c3c5c6` log carries an executor DEFECT on the record rather than deleted: a first-pass reimplementation of the claim-only probe used unstratified document folds and read within-pair 0.7815 on the `km<->m` family, which is the min(a,b)/(a+b) artifact the lane's build documented, not a leak; the reading was discarded for the banked instrument's 0.4629. Two supplementary sweeps go beyond the clauses and are labelled as such - a TabFact document-id stem-collision census (19 within-lane collisions from the `1-`/`2-` csv-id prefixes) and a near-duplicate sweep against six mechanism evals that finds 13 TabFact documents shared with `R20-H177_eval_B` at zero shared passages in all three exact forms. Scripts under `experiments/grounding-semantic/contract/quant_scale_unit_*.py`, artifact `experiments/grounding-semantic/contract/quant_scale_unit_contract_report.json`; counts, distributions and identifiers only, no evaluation-surface text emitted. Measurements only - the coordinator holds the verdict

- `contract-halueval-*.log` (`core`, `c1supp`, `disjoint`, `census`, `probe`, `lenmatch`) - dataset-contract verification of the `halueval` training member (40,000 rows / 20,000 pairs, DANN group `halueval`, 5.83% of the 685,670-row clean mix) against clauses C1-C8 of `docs/experiments/dataset-contract.md`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - the three R20-H174 draws on GPUs 0/1/2 are untouched. The member is rebuilt through the BANKED path the arms actually call (`R16-H142_G1_arm.H108.public_train()` under `untruncated_evidence()`, rows filtered to the `halueval` group); because that loader drops the QA `question` field and emits no pair key, the archive read order is replayed and PROVED aligned to the loader's own output row-for-row across all 40,000 rows before the pair structure is used, aborting on any mismatch. Instruments are reused rather than reinvented: `R20-H174_lane_common.containment` for C1 on both the untruncated and 1,500-char presentations, its converged liblinear claim-only probe and surface-parity table for the leak readings, and `provenance_gate.py` in the R14-H136 ruling-2 form for C4 (8-gram, Jaccard >= 0.3, bidirectional, KILL 2%, thresholds read from `R19_supply_gates.py`). C4 carries two live positive controls beside the synthetic spike - ten arena documents thinned to two of every three sentences, and the corpus-native one that needs no construction at all, HaluEval QA knowledge blocks and the arena `hotpotqa` subset both being HotpotQA paragraphs (45 hits, max Jaccard 0.7903). The `lenmatch` log carries the control that decides fixability: restricted to the 1,483 summarization pairs whose two claims are within 10% in length, the length channel is at chance (0.4930) while the claim-only probe still reads 0.9408. Scripts `experiments/grounding-semantic/contract/halueval_contract.py` and `halueval_report.py`, artifact `experiments/grounding-semantic/contract/halueval_contract_report.json`; counts, distributions and MIT-licensed corpus excerpts only, no evaluation-surface text emitted. Measurements only - the coordinator holds the verdict

- `contract-ragtruth-en.log` - DATASET CONTRACT PHASE 1, verification of the `ragtruth_en` training member against `docs/experiments/dataset-contract.md` clauses C1-C8 (`experiments/grounding-semantic/contract/ragtruth_en_contract.py`, CPU ONLY, `CUDA_VISIBLE_DEVICES` forced empty before any import - no card touched, nothing trained, GPUs 0/1/2 left to the R20-H174 draws). The member is rebuilt through the BANKED loader rather than reimplemented: `R16-H142_G1_arm`'s own `R10-H108_lane` instance supplies `public_train()`, read twice - once at the R10-H108 truncation (`CFG.chunk_max_chars` 1,500) and once under that module's `untruncated_evidence()` lift, the presentation the R18-H150 / R20-H174 flagship trains on. Loading a SECOND `R10-H108` instance makes the context manager a silent no-op, because each instance holds its own `M59.CFG` object; a first pass did exactly that and reported the truncated text as untruncated, caught because all three presentations returned byte-identical distributions. Instruments are reused: `provenance_gate.py` in the R14-H136 ruling-2 form (8-gram, Jaccard >= 0.3, bidirectional, KILL > 2%, thresholds read from `R19_supply_gates.py`) and `R20-H174_lane_common` for `containment` / `auroc` / `claim_only_probe`. Measured: 15,090 rows of the 685,670-row clean mix, 8,369 positive / 6,721 negative, 2,514 distinct contexts at 6.00 responses each, mean 2.6871 windows and 0.7618 multi-window (both reproducing the canonical log exactly). C1 claim-to-evidence containment positives 0.6881 / negatives 0.5328, containment AUROC 0.7608, negatives at containment >= 0.90 0.67% against positives 7.90%. C2 zero overlapping units across three string forms, both directions, against the arena and eight held-out surfaces. C3 the archive split is document-disjoint (0 shared contexts raw and whitespace-normalised; 2 of 450 test contexts near-duplicate at Jaccard 0.3719). C4 census max fraction 0.0 with the spike control 10/10 at 0 baseline and a constructed near-duplicate live control firing 200/200. C6 leave-one-out context-key AUROC 0.6509. Artifact `experiments/grounding-semantic/contract/ragtruth_en_contract_report.json`. Marker `report -> ...ragtruth_en_contract_report.json`. Measurements only - the coordinator holds the verdict

- `psiloqa-conformed-build.log` / `psiloqa-conformed-verify.log` - the CONFORMING PIPELINE for the `psiloqa` training member and its full re-verification against every clause C1-C8 of `docs/experiments/dataset-contract.md`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - the three R20-H174 draws on GPUs 0/1/2 are untouched. Opened because the member failed two clauses: C1 by 0.0069 (pooled attestation-rate delta 0.0931 inside the <= 0.10 rejection band on the full-coverage Unicode containment instrument) and C2 on the 485 and 406 passages it shares with the two withdrawn PsiloQA-derived H175b evals. The build (`psiloqa_conformed_build.py`) removes rows and nothing else - no label changed, no leg re-weighted, no threshold moved, no instrument re-tuned. Filter F1 drops every row whose passage or claim collides with any of the nine evaluation surfaces under any of the six string-form pairings clause C2 tests (1,379 rows / 485 distinct passages, all against the two withdrawn evals, zero against any live surface). Filter F2 drops every row whose claim exceeds a Unicode content-token cap, the cap chosen by a published sweep from 12 to unbounded under a rule fixed before the sweep was read - the largest cap whose pooled delta clears the band by at least 0.01 on BOTH containment instruments - which lands at 42 tokens and a further 9,859 rows. The re-verification (`psiloqa_conformed_verify.py`) re-runs the SAME clause instruments imported from `psiloqa_contract.py` over the conformed rows, rebuilding the member through the banked loader (`R10-H108_lane.public_train()` with the evidence cut lifted) and aborting on any row-alignment drift between the loader, the archive replay and the conformed parquet's recorded indices; the assembled-mix chunk set is rebuilt with the conformed member SUBSTITUTED for the original from the tagged rows rather than approximated by set subtraction. Result: 50,474 rows (18.2% volume cost from 61,712), all 14 languages retained, positive rate 0.1092 -> 0.1099, and every clause PASS - C1 delta 0.1102 unicode / 0.1295 ASCII with both instruments now on the same side of the band, C2 zero against all nine surfaces in every form and both directions, C4 census max fraction 0.0 with the spike control 10/10 and the live positive control firing at 83.9%, C6 worst leave-one-out key-majority delta -0.0096 against baseline. Residuals recorded rather than argued away: the C1 margin is 2.14 binomial standard errors, 10,993 conformed rows still sit in the 25+ content-token band where containment reads AUROC 0.434, and C3's per-question split remains a corpus property no pipeline over the train split can change. Artifacts `experiments/grounding-semantic/contract/psiloqa_conformed.parquet`, `psiloqa_conformed_build.json`, `psiloqa_conformed_report.json`. Markers `=== conformed member -> ...`, `=== report written -> ...`. Measurements only - the coordinator holds the verdict

- `vitaminc_contract.log` / `vitaminc_contract_census.log` - dataset-contract verification of the `vitaminc` training member (370,653 rows, 48.73% of the 760,618-row R20-H174 mix) against clauses C1-C8 of `docs/experiments/dataset-contract.md`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - the three R20-H174 draws on GPUs 0/1/2 are untouched. The member is rebuilt through the BANKED path (`R10-H108_lane.public_train()` read untruncated via `R16-H142_G1_arm.untruncated_evidence()` plus all five `R20-H174_arm_run.LANES`, reproducing 760,618 rows and 17 DANN groups exactly) and every instrument is reused rather than reinvented: `R20-H175b_qlane.containment` (Unicode) beside `R20-H174_lane_common.containment` (ASCII) for C1, `R20_baseline_legs.vitaminc_holdout` to rebuild the held-out mechanism eval, and `provenance_gate.py` in the R14-H136 ruling-2 form for C4 (8-gram, Jaccard >= 0.3, bidirectional, KILL 2%, spike control, thresholds read from `R19_supply_gates.py`). C4 carries a LIVE positive control as the clause requires - VitaminC own official test split offered to the identical instrument against the train side, firing at 2.06% of evidence units with max Jaccard 1.0 while the arena-facing gate reads 0.00081. C1 adds a label-varying-channel census over the 111,323 mixed-label contrastive cases, the mechanical form of the H175b diagnosis: 0 cases hold both claim and evidence fixed. C2 tests 18 evaluation surfaces in three string forms both directions and is followed by an exhaustive sweep over every top-level parquet artifact. Scripts `experiments/grounding-semantic/contract/vitaminc_contract.py` and `vitaminc_census.py`, artifacts `vitaminc_contract_report.json` and `vitaminc_census.json`; counts, distributions and public CC-BY-SA corpus excerpts only, no evaluation-surface text emitted. Measurements only - the coordinator holds the verdict

- `contract-tabfact-conform-build.log` / `contract-tabfact-conformed-c2.log` / `contract-tabfact-conformed-c4.log` / `contract-tabfact-conformed-clauses.log` / `contract-tabfact-conformed-scope.log` - the CONFORMING PIPELINE for the `tabfact` training member and its full re-verification against every clause C1-C8 of `docs/experiments/dataset-contract.md`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - the three R20-H174 draws on GPUs 0/1/2 are untouched. Opened because the member failed C2, C3 and C8. The build (`tabfact_conform_build.py`) rebuilds the member through the banked loader (`R10-H108_lane.public_train()` under `R16-H142_G1_arm.untruncated_evidence()`, reached through the arm so the context manager patches the loader's own M59), asserts it row-for-row against the archive, and then cuts on the DOCUMENT and nothing else - no label changed, no chunk rewritten, no threshold moved. Cuts: X1 every member table whose `table_id` stem occurs in TabFact validation or test (164 documents), X2 the 325 documents `R20-H177_eval_B` draws on, X3 the 350 documents `R17-H143_evalset` draws on - a channel the first pass could not read, recovered by joining the evalset's 547 passages to `R17-H143_evalset_source.parquet` - X4 the 14 anti-gaming sets' tables (a subset of X1, measured not assumed), then a string residual (0), a content residual of 43 further documents at 8-gram Jaccard >= 0.3 against an eval-side TabFact table (worst 0.9852, second round 0), and 88 rows of contradictory supervision. Result: 86,206 rows from 92,585 (6.89% volume cost) over 11,882 documents from 12,753, mix 685,670 -> 679,291, member share 0.1350 -> 0.1269, and every clause PASS or NOT-APPLICABLE - C2 zero on all three string forms both directions on evidence and claims across 12 surfaces and zero on every document channel (eval_B 0/325, H143 0/350, all 14 anti-gaming sets 0), C3 zero ids / zero stems / zero evidence in all forms against both held-out splits, C4 census 0.0 both unit types with the spike 10/10 and three live controls (register proof still fires 166/3,391 at max Jaccard 1.0 against the banked member, 0/3,391 against the conformed one, 701/951 interior arena windows fire on the arena side), C6 the prescribed channel now CLOSED at coverage 0.0 on both surfaces sharing a key, C8 the retrieval date 2026-07-31 derived from three fetch-run artifact stamps and recorded. Residuals recorded rather than argued away: the archive's own split is not document-disjoint and no pipeline over the train split can change that; 8 validation and 7 test rows still carry a statement string the member carries on a DIFFERENT document; and the tracked sidecar's declared split volumes remain wrong (one `size` string in `scripts/fetch_grounding_datasets.py`), corrected in the report but not edited. Artifacts `experiments/grounding-semantic/contract/tabfact_member_conformed.parquet`, `tabfact_conform_build.json`, `tabfact_conformed_c2.json`, `tabfact_conformed_c4.json`, `tabfact_conformed_clauses.json`, `tabfact_conformed_scope.json`, `tabfact_conformed_report.json`. The scope sweep is the check on what C2 was read against: all 147 round-directory parquets under 30 MB compared to the conformed member on passages, claims and TabFact document ids - 30 still share text or a document, every one a training lane, a lane source pool, a generation or gate sample, or a lane-side probe, none a banked held-out mechanism eval. Measurements only - the coordinator holds the verdict
- `vitaminc_contract_claim_probe.log` - executor-added claim-only diagnostic for the `vitaminc` member, run BESIDE the contract verification above and merged into its report under `C1.executor_added_probes` (`experiments/grounding-semantic/contract/vitaminc_claim_probe.py`, CPU only, no GPU touched). Not a C5 bar - C5 is NOT-APPLICABLE to a source corpus - but the question C5 would otherwise have answered, and one C1 makes worth asking because 121,700 of the 370,653 rows are SYNTHETIC revisions that could leave a claim-side artifact. The banked instrument is reused unchanged (`R20-H174_lane_common.claim_only_probe`, out-of-fold char_wb TF-IDF 2-5 plus liblinear at tol 1e-7, folds disjoint on `page`), over a stated page-disjoint subsample of 4,000 pages / 65,749 rows. Result: claim-only AUROC 0.4998 and within-case claim-only accuracy 0.4999 over 12,949 claim-varying contrastive cases - exactly chance on both, so the label is not readable from the claim alone. Marker `merged into ...vitaminc_contract_report.json`. Measurements only - the coordinator holds the verdict
- `attr_pool_conformed_supply.log` / `attr_pool_conformed_build.log` / `attr_pool_conformed_diag.log` / `attr_pool_conformed_verify_{core,disjoint,census,memo,frontier}.log` / `attr_pool_conformed_memo_supp.log` - the CONFORMING PIPELINE for the `attr_pool` lane (R20-H174 lane L2) and its full re-verification against every clause C1-C8 of `docs/experiments/dataset-contract.md`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - the three R20-H174 draws on GPUs 0/1/2 train on the PARENT lane and are untouched, and the parent parquet is never overwritten. Opened because the member failed C2, C5, C6 and C8. The build (`attr_pool_conformed_build.py`) drops the VitaminC half entirely - VitaminC train is itself a mix member, so the mix pairs every one of the lane's 6,894 VitaminC claims with the evidence sentence its truth passage is assembled from, the C6 channel, and the missing VitaminC retrieval date is the C8 gap and the colliding C2 claim is VitaminC's - then admits a MiniCheck claim only if its string appears NOWHERE in the 739,210-row assembled mix (raw or case-folded, which also removes the 605 claims shared with `frame_reject`) and collides with none of the 8 evaluation surfaces on any of the three contract string forms, with a sub-8-gram substring guard closing the coverage residual the parent left open. A diagnostic (`attr_pool_conformed_diag.py`) then forced a fourth change: the parent's within-pair claim-only 0.5594 on `unsupported_claim` is the mean of a MiniCheck half at 0.6418 and a VitaminC half at 0.4998, and retrained on the MiniCheck half alone it reads 0.6825 against a C5 bar of < 0.60, so that family is dropped and `truth_removed` - which holds the claim byte-identical across the legs - survives alone. Result: 4,442 rows / 2,221 pairs from 21,408 / 10,704, a 79.25% volume cost, below the registered 20-30k row band. Seven clauses PASS - C2 zero in all 135 cells, C4 both gates 0.0 with the spike 10/10 and the live positive control firing at 0.996 against the full MiniCheck archive, C6 coverage exactly 0.0000 so the channel that failed the parent at within-pair 0.9999 is UNDEFINED, C8 retrieval date 2026-08-13 recorded and 0 claims carrying a single label. C5 still FAILS on one channel: claim-to-chunk containment 0.5978 against [0.45, 0.55]. The frontier stage sweeps the family of row-dropping pipelines that could close it and finds ZERO feasible points under the reading of C1 this process applied to the parent - the channel first enters C5's band at 0.5493 and by then C1's attestation gap has fallen from 0.1108 to 0.0650 - so C5 is recorded as a clause conflict, a corpus property, not a pipeline gap. Residuals recorded rather than argued away: C1 passes by 0.0108 with a bootstrap CI of [0.095, 0.127] and 10% of redraws failing; the best-single-passage reading of C1 reads gap 0.0581; and an executor-added document-keyed probe separates the legs at within-pair 0.5973, shown by the supplement to be a sighting imbalance (truth documents 51.4% seen in the mix vs swap documents 31.5%) with no label gradient, since all 1,677 such passages come from `frame_reject` and carry mix mean label exactly 0.5. Artifacts `experiments/grounding-semantic/R20-H174_lane_L2_conformed.parquet`, its manifest, and `experiments/grounding-semantic/contract/attr_pool_conformed_report.json`. Public MIT corpus text only, no client or company name, no evaluation-surface text emitted. Measurements only - the coordinator holds the verdict

- `quant_misbind_conformed_build.log` / `quant_misbind_conformed_verify.log` / `quant_misbind_conformed_c2_full.log` / `quant_misbind_conformed_docdisjoint.log` / `quant_misbind_conformed_live_control.log` - the CONFORMING PIPELINE for the `quant_misbind` lane (`R17-H146_lane.parquet`, 30,000 rows / 15,000 pairs) and its full re-verification against every clause C1-C8 of `docs/experiments/dataset-contract.md`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - the three R20-H174 draws on GPUs 0/1/2 train on the PARENT lane and are untouched, and the parent parquet is never overwritten. Opened because the member failed C2, C3 and C8. The build (`quant_misbind_conformed_build.py`) removes whole pairs and nothing else - no row rewritten, no label changed, no construction rule loosened, and all 18,652 surviving rows are byte-identical rows of the parent. Five removals: D1 the entire FEVEROUS third (5,055 pairs), whose split axis is not measurable from the artifact on disk and whose ids are positionally unstable (C3) and which carries no licence, no retrieval date and no tracked source (C8); D2 the 78 pairs colliding with an evaluation surface under C2's three string forms; D4 the 489 pairs whose SOURCE DOCUMENT appears in an evaluation surface - a read the parent could not make, because with the FEVEROUS half gone every member document carries TabFact's own stable `table_id`, and it found `R20-H177_eval_B` sharing 229 of its 458 source documents (458 of its 1,000 pairs) invisibly to all three string forms; D5 the 124 pairs colliding with the 21 evaluation and probe surfaces the banked C2 instrument does not cover, including the eleven further anti-gaming probe sets - the exact gap that caused the parent's failure, `R14-H133_antigaming.py` line 57 having hard-wired its exclusion to a different lane; and D3 the 21 pairs needed to restore exact per-family direction balance afterwards. `R17-H143_evalset_source` is measured but NOT dropped against - it is the 50,000-row pool the H143 eval was sampled from, not a surface anything is read on, and the eval drawn from it reads zero on both tests. Result: 18,652 rows / 9,326 pairs over 4,716 documents, a 37.83% volume cost, TabFact-train only, and every clause PASS - C1 structural test 0 of 9,326 pairs with the negative leg's binding attestation 0.0 against the positive's 1.0 on the full population, C2 zero in all three string forms both directions across 34 surfaces plus zero on every document-identity channel, C3 the axis measured from the archive (0 shared `table_id` train vs test and train vs validation) with all 4,716 member tables in train and 0 of the mechanism eval's 352 stable-id source documents present, C4 both gates 0.0 with the spike 10/10 and the tiered live control firing 1.0 verbatim, C5 claim-only 0.5059 against < 0.55 with every parity channel inside the band, C6 the mix-supplied association at 0.5, C8 TabFact CC-BY-4.0 with the retrieval date 2026-07-31T18:10:02 MEASURED from the archive rather than asserted. Residual recorded rather than argued away: the arm wrappers still register the parent at 30,000 / 15,000, so adopting the conformed member requires updating those counts or the LANE ABORT guard fires. Artifacts `experiments/grounding-semantic/R17-H146_lane_conformed.parquet` and `experiments/grounding-semantic/contract/quant_misbind_conformed_*.json`. Public TabFact corpus text only, no client or company name, no evaluation-surface text emitted. Measurements only - the coordinator holds the verdict
- `R20-H177_evalB_rebuild_supply.log` / `R20-H177_eval_B_rebuild.log` / `R20-H177_eval_B_rebuild_repro.log` / `R20-H177_eval_B_rebuilt_verify.log` / `R20-H177_eval_B_rebuilt_claimcollide.log` - the REBUILD of `R20-H177_eval_B`, the held-out mechanism eval for the R20-H177 lane B arm, on supply genuinely disjoint from the training mix, plus its verification against every clause C1-C8 of `docs/experiments/dataset-contract.md` with amendments C-A1 and C-A2 applied. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import, `HF_HUB_OFFLINE=1` - the three training draws on GPUs 0/1/2 are untouched. Opened because the banked eval leaks on two channels: 33 of 736 passages sit in the flagship mix (19 byte-identical, 126 rows / 63 pairs) and 325 of 325 of its TabFact documents are in the `tabfact` mix member, covering 1,300 of 2,000 rows (65%), which string matching misses because the eval re-serialises the same tables into six forms while the member carries only the pipe form. The supply stage (`R20-H177_evalB_rebuild_supply.py`) measures the frontier before anything is built: the member IS the whole TabFact train split, and 8,541 of 8,541 stems of the 8,816 admissible train tables are inside it, so ZERO TabFact train supply survives a stem-keyed exclusion - the contamination is structural and no reseeding or re-splitting can fix it. The build (`R20-H177_eval_B_rebuild.py`) imports the banked construction from `R20-H177_lane_B.py` unchanged (BUILDERS, assemble, verify, emit, EVAL_TARGETS, SEED_EVAL 2177) and changes only the supply and the split: TabFact TEST+VALIDATION minus every table reachable by id, by id STEM or by whitespace-normalised serialised text (raw or 1,500-cut) from the member, leaving 1,522 clean tables, plus the untouched EDGAR eval half; and the split becomes CORPUS HALF rather than `blake2b(doc_id)`, which TabFact defeats by writing one Wikipedia table under both a `1-` and a `2-` csv id. A first pass read zero on evidence and zero on documents but 12 rows on the CLAIM STRING against lane B - six pairs colliding on both legs with the lane's label agreeing - so a claim wall was added at build time (a candidate is rejected if its claim under either relation word and either template of its frame is a string lane B or the 760,618-row mix already carries; 16 rejections) rather than the bar being relaxed. Result: 2,000 rows / 1,000 pairs / 465 documents / 727 passages, every target cell filled exactly, family composition identical to the original, so the arm's `>= 0.80` gate reads at the same precision (2 standard errors = 0.0198 at AUROC 0.80). Every channel zero: C2 all six string forms both directions on evidence AND claims against the flagship mix, lane B, the blind arena and `gold_full`; the stem-keyed document channel 0 of 324 against the member and 0 against every mix lane; C6 key coverage exactly 0.0 so the clause is not applicable with no proxy substituted; C4 census 0.0 both unit types with the spike 10/10. Three live positive controls all fire on the artifact being replaced: the document channel reads 325 of 325 and 1,300 rows, the string cross 33 passages / 126 rows, the n-gram gate 100 of 556 of its TabFact passages at max Jaccard 1.0 against the member while the rebuilt reads 0 of 546 at max 0.2169. Two findings recorded rather than smoothed away: the banked `R17-H144_pairs.excluded_tables` breaks ties over a frozenset, so it is hash-seed dependent and two runs of identical code excluded 373 and 372 validation tables - the build is byte-reproducible only with `PYTHONHASHSEED=0`, verified by two runs producing the same sha256; and one cmp_extreme positive of the ORIGINAL eval reads unsupported under a predicate-sensitive instrument because the 1,500-character cut removes the row carrying the extreme (0 such rows in the rebuilt). Artifacts `experiments/grounding-semantic/R20-H177_eval_B_rebuilt.parquet`, its manifest, `R20-H177_eval_B_rebuilt_report.json`, `R20-H177_evalB_rebuild_supply.json` and `R20-H177_eval_B_rebuilt_claimcollide.json`. Public TabFact and EDGAR text only, no client or company name. Measurements only - the coordinator holds the verdict
- `R17-H143_evalset_assessment.log` - full dataset-contract assessment of the `R17-H143_evalset.parquet` evaluation surface (1,050 rows / 550 pairs / 547 passages / 536 documents), the one eval never given this treatment. CPU only, `CUDA_VISIBLE_DEVICES` forced empty, `HF_HUB_OFFLINE=1`. Measures C2 disjointness against BOTH assembled mixes (flagship 721,210 rows and portfolio 760,618 rows) in eight string-form pairings crossed both directions on evidence and claims, plus the document channel raw and stem-normalised; recomputes all twelve banked model reads on this eval with and without the contaminated rows, reproducing every banked AUROC exactly before any exclusion is applied; and runs C1, C3, C4, C5, C6, C7 and C8 with a live positive control on every gate. Script `experiments/grounding-semantic/R17-H143_evalset_assessment.py`, artifact `experiments/grounding-semantic/R17-H143_evalset_report.json`; counts and metrics only, no evaluation-surface text is emitted
- `R17-H143_evalset_assessment_dryrun.log` - the same script under `--skip-c4`, run first to validate the pipeline before the n-gram census was added
- `R21-H179_arena_scores.log` - R21-H179 stage 1, the blind-arena PER-ITEM scoring pass that the failure-mode autopsy is built on. The campaign banks only per-subset aggregates for the arena, so no per-item score existed anywhere; this stage closes that gap for all six banked flagship endpoints (H150 d1/d2, H160 d3/d4, H172 d5/d6) over all ten RAGBench subsets, 2,264 responses. The shipped read is REUSED UNCHANGED - `R16-H142_G1_reads.py` is loaded and its `main()` called with only the checkpoint binding and the output path rebound, the R20-H173 soup-read pattern verbatim - and per-item scores are obtained by WRAPPING `ARM.score_sets`, which keeps the banked function's return value and passes it through untouched, so no aggregation is re-implemented and the read arithmetic stays byte-identical to every banked arena read. The AUROC unit is the RESPONSE (min over H92 sentences of max over 1,500/750 windows); per-sentence scores (8,277) are banked alongside as the sub-unit it is a min over. Fidelity control per checkpoint per subset, run before anything is banked: the structural fingerprint (n, n_sent, n_pairs) must equal the banked `*_windowed_result.json` block exactly and the AUROC recomputed from the banked per-item scores must reproduce the banked per-subset AUROC to <= 1e-4, with a failure ABORTING the run and banking the failure record instead of the scores. Three stages, each idempotent against on-disk artifacts: `items` (CPU, `R21-H179_arena_items.parquet` - RAGBench id, question, response, H92 sentences, retained documents[:8] and the sentence-level annotation block), `score` (GPU0 only, one `R21-H179_arena_scores_<checkpoint>.npz` per draw plus `R21-H179_arena_scores_fidelity.json`), `consensus` (CPU, per-draw agreement, consensus errors and rank-loss mass -> `R21-H179_consensus_errors.json` / `.parquet`). R18-H157's `rank_loss` and `op_threshold` are lifted VERBATIM from the banked autopsy by source extraction, with their source text and digests recorded in the output. The score stage waits for GPU0 to be released by the R20-H174 draw-2 job before binding a card - the draw-2 campaign log must show DRAW 2 COMPLETE, no draw-2 process may be alive, and GPU0 memory must have fallen below 2 GB; `nvidia-smi --query-compute-apps` is NOT used because under WSL2 it lists every compute process against every GPU. GPUs 1 and 2 carry the other two training draws and are never touched. Scripts `experiments/grounding-semantic/R21-H179_arena_scores.py` and `R21-H179_campaign.sh`. Markers `--- <draw> <checkpoint> windowed arena read`, `=== R21-H179 STAGE 1 COMPLETE ===`, abort marker `=== FIDELITY ABORT ===`. Measurements only - the coordinator holds the verdict; the annotation, clustering and mechanism stages are owned by other agents
- `R21-H179_laneA_gpu2.log` / `R21-H179_laneB_gpu0.log` - the TWO-CARD execution of that pass, superseding the single-card `R21-H179_campaign.sh` wait-for-GPU0 job (killed while still idle, before it had done any GPU work). The pass now needs twelve arena reads, not six: the six banked flagship draws under TRUE evidence and the same six under ABLATED evidence, so the work splits cleanly across two cards. Lane A binds GPU2 and owns draws d1/d2/d3, lane B waits for the R20-H174 draw-2 job to release GPU0 and owns d4/d5/d6; GPU1 carries the draw-3 training run and is never touched. The lanes are disjoint in ARTIFACTS as well as in work - each checkpoint's npz and result JSON are per-checkpoint, and the fidelity record is written as a per-draw shard (`R21-H179_arena_scores_fidelity_<tag>.json`) that the consensus stage merges once into the canonical `R21-H179_arena_scores_fidelity.json`, so no two processes ever write the same file. Each lane asserts its card is idle (under 2 GB used) before binding and aborts rather than sharing a card with a training draw; `nvidia-smi --query-compute-apps` is NOT used because under WSL2 it lists every compute process against every GPU. Script `experiments/grounding-semantic/R21-H179_lane.sh`, parameterised by `GPU`, `LANE` and `DRAWS`. Markers `--- TRUE evidence score, draw <d>`, `--- ABLATED evidence score, draw <d>`, `=== R21-H179 LANE <X> COMPLETE ===`
- `R21-H179Q_probes.log` - the three questions attached to that pass, run once over the banked per-item scores. `q1` re-aggregates the arena mean with the contamination-exposed responses dropped - the 16 responses retrieving one of the 17 arena documents proven byte-for-byte inside a HaluEval training chunk, and the 117 responses retrieving a document at 8-gram containment >= 0.10 - and reports the per-subset deltas and the 6-draw mean against the 0.71218 headline. Nothing is re-scored: the same per-item response scores are re-aggregated over a subset of their own items. The exposed-response lists are NOT re-derived - the verbatim document ids are the surface report's own `verbatim_substring_proof.documents[*].doc_blake2b_64`, and the containment reading comes from the checkpointed census the report itself was computed from (`tmp/arena_surface/census.npz :: best_c|documents`) because the report banks only counts at that threshold; both rebuilt sets must reproduce the report's banked per-subset counts EXACTLY or the run aborts. A matched random-drop control over 20 seeds is carried beside the observed delta as an executor-added diagnostic. `ablate` re-reads the arena with the evidence PERMUTED across items inside each subset by a fixed-seed (179) derangement, which preserves every surface statistic of the evidence side - length, register, token distribution, the multiset of per-item document counts, the total window count - and destroys only the claim-evidence relation; the permutation is banked in the output npz with its digest. `q2` rolls the ablated means up against the true-evidence means, and `q3` joins the two exposure flags onto the autopsy's consensus table so its error classes cannot be confounded by the contamination q1 measures. Script `experiments/grounding-semantic/R21-H179Q_probes.py`, artifacts `R21-H179Q_contamination_drop.json`, `R21-H179Q_evidence_ablation.json`, `R21-H179Q_autopsy_exposure.json`, `R21-H179Q_exposed_responses.parquet`. Measurements only - the coordinator adjudicates
- `R20_claimonly_sweep.log` - the CLAIM-ONLY SHORTCUT SWEEP: how far the HaluEval leak found by the dataset-contract pass (`contract/halueval_contract_report.json`, claim-only AUROC 0.9519) extends across the assembled mix, and whether it transfers to the blind arena. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import, `HF_HUB_OFFLINE=1` - the three training draws on GPUs 0/1/2 are untouched. The mix is rebuilt through the BANKED assembly (`R10-H108_lane.public_train()` plus `R17-H146_lane.parquet` and `R18-H150_scaleunit_lane.parquet`, the `R18-H150_arm_run.py` flagship composition, 721,210 rows and 14 DANN groups, reproduced exactly); the arena comes from `R8-H77_unseen_arena.load_subsets()` (10 RAGBench subsets, 2,264 responses) and the in-domain surface from `R10-H108_lane.gold_full()` (2,752 claims). `public_train` is called with its own evidence truncation in place because `untruncated_evidence` lifts the cut on the EVIDENCE only, leaving claims, labels, tags and the row set identical, and this sweep reads no evidence. The instrument is deterministic and torch-free - TF-IDF char_wb(2,5) plus word(1,2) of the CLAIM STRING ALONE into liblinear logistic regression at tol 1e-7 - fitted on a fixed-seed stratified 70/30 split and on an evidence-disjoint 70/30 split, with a third pair-disjoint split (lane `pair_id`, or the claim string for VitaminC) for the members whose twins share a claim rather than a document. Stage `amend` applies the coordinator's correction: every verdict restated two-sided on `leak_strength = |AUROC - 0.5|` with the equivalent one-sided AUROC beside it, the verdict-bearing split chosen from a MEASURED pair census rather than assumed, and a label-shuffled negative control on the worst and the cleanest member. Read: HaluEval 0.9565 and all eight RAGTruth members 0.79-0.83, PsiloQA 0.7684, TabFact 0.5911, the unit-swap lane 0.6358, the misbind lane 0.4965; VitaminC's inverted 0.2132 is pure claim-twin memorisation (99.29% of its distinct claim strings carry both labels) and reads 0.5010 once the split is claim-disjoint. Transfer: arena subset-mean 0.5351 fit on HaluEval and 0.5683 fit on the whole mix, per-subset from 0.3848 to 0.6492, gold_full 0.6139 and 0.4802. The negative control reads 0.4975 and 0.4945, so the instrument manufactures nothing. Script `experiments/grounding-semantic/R20_claimonly_sweep.py` (stages `sweep` and `amend`, the amend stage idempotent), artifact `experiments/grounding-semantic/R20_claimonly_sweep.json`; counts and metrics only, no evaluation-surface text emitted. Measurements only - the coordinator holds the verdict
- `contract-halueval-conform-{scores,diag,frontier-a,frontier-b,frontier-c,frontier-merge,deep-peel,deep-comp,fine,perhalf,floorcheck,honesty,honesty2,transforms,build-verify}.log` - the CONFORMANCE ATTEMPT on the `halueval` training member after its phase-1 C5 failure (claim-only converged probe AUROC 0.9519 against a 0.55 bar, `contract/halueval_contract_report.json`). CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import, `HF_HUB_OFFLINE=1`; the member is rebuilt through the banked loader and its archive replay re-proved aligned row-for-row on all 40,000 rows at the start of every stage. `scores` banks the baseline probe and the per-row style features. `diag` decomposes the channel - length alone, punctuation and case alone, hedging and function-word rate alone, register with content masked, content with register deleted, word order destroyed, and the full probe inside claim-length deciles - pooled and per half, and carries the live positive control the whole reading rests on: a toy whose legs differ only in register reads 1.0, a globally label-shuffled copy reads 0.5017 and a within-pair-shuffled copy 0.4951. `frontier-{a,b,c}` sweep eight pair-level subset strategies (ambiguity peels on the full probe, on the content-masked shape probe and on the numeric style probe; length matching; claim-to-claim similarity; a seeded random control; two composites) across five retention levels, `deep-*` and `fine` push the winners down to 1% retention with five probe seeds per subset, `perhalf` repeats the search with the peel run inside each half so neither half is starved, `floorcheck` proves the sub-bar readings at 400-1,600 rows are real rather than an exhausted vocabulary, `honesty`/`honesty2` re-run the peel with the ranking produced by a probe fitted on a disjoint split so the retained band is not selected on its own noise, and `transforms` measures the rewrite repairs (claim truncation, normalisation, register masking) together with their label-integrity cost. `build-verify` materialises the two variants and re-verifies each against all eight clauses under amendments C-A1 and C-A2. Scripts `experiments/grounding-semantic/contract/halueval_conform.py`, `halueval_conform_perhalf.py`, `halueval_conform_floorcheck.py`; artifacts `halueval_conformed.parquet` (480 rows), `halueval_conform_best_effort.parquet` (4,000 rows) and `halueval_conformed_report.json`. Counts, distributions and MIT-licensed corpus excerpts only, no evaluation-surface text emitted. Measurements only - the coordinator holds the verdict
- `gold_full_surface.log` / `gold_full_surface_spike_diag.log` / `gold_full_surface_cluster.log` - full dataset-contract verification of the `gold_full` EVALUATION SURFACE (2,752 claims, 123,579 loaded (claim, chunk) rows, 10,228 unique chunks, 619 source documents), the in-domain hold every campaign arm carries at bar >= 0.84. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import, `HF_HUB_OFFLINE=1` - the three training draws on GPUs 0/1/2 are untouched. Two jobs: RE-VERIFY the prior split audit rather than trust it, and EXTEND it to the clauses it did not cover. Reproduction recomputes 30 headline numbers of `R20_goldfull_split_audit.py` from scratch through the same banked instruments against the same 14-group / 721,210-row mix; 29 reproduce exactly (all four exact channels 0, both censuses max_fraction 0.0 with best Jaccard 0.0185 claims and 0.0070 evidence, the id substring scan 0/0, and the live positive control 58 of 25,689 VitaminC test claims at max Jaccard 1.0). The single mismatch is that audit's own CLAIMS-channel spike control, banked 10/10 and recomputed 9/10, and `gold_full_surface_spike_diag.py` measures the cause instead of asserting one: `provenance_gate.spike_control` injects the first unit of the first ten buckets in dict order, the audit builds those buckets from `mix.group_by("tag")` whose iteration order is NOT deterministic (five distinct orders in five calls), each bucket is `sorted(set(claims))` so the injected unit is a group's lexicographically smallest claim, and HaluEval's smallest claim normalises to ZERO tokens and therefore carries no 8-gram and cannot hit under any correct gate - so the shipped construction returns 9 or 10 on this mix depending on whether HaluEval lands in the first ten buckets. A conforming control that injects only units carrying at least one n-gram detects 10 of 10 with 0 baseline hits, so it is the CONTROL that is order-dependent, not the gate. The extension measures C2 in all three string forms (raw, 1,500-truncated, whitespace-collapsed case-folded) in BOTH directions across six channels - claims, evidence against raw mix chunks, evidence against the 1,500/750 windows, (claim, evidence) pairs, and documents against both chunks and windows - against BOTH assembled mixes (721,210 and 760,618), every cell zero; the DOCUMENT channel by 8-gram containment, max 0.018349 for any of 386,418 mix chunks into the 619 gold documents and max 0.000246 coverage the other way, with two live positive controls firing on it (gold chunks into their own parent documents mean 0.9736 with 7,672 of 10,227 at exactly 1.0, and ten re-wrapped documents 10/10 at containment 1.0); and C6 key coverage exactly 0.0 on all six shared-field key definitions so the clause's feature is undefined and no proxy is substituted, with a synthetic mix-supplied association built to the `attr_pool` failure mode firing at AUROC 0.9981 on 100% coverage. C3 reads the axis as CORPUS-level rather than any within-corpus cut: zero overlap on document, claim, evidence and identifier, no provenance join computable in either direction, and every mix member and constructed lane sourced from a public corpus (tabfact, feverous, vitaminc, minicheck or a template generator), none from the surface's own corpus. Two corpus properties recorded with their consequence: 55,145 of the 123,579 loaded rows (44.6%) are exact repeats inside a claim's own chunk list, and the label is strongly clustered by document (claim-level document-keyed leave-one-out 0.9070 against a 0.7144 base rate, label ICC 0.7821, design effect 3.70, effective sample size 745 rather than 2,752, which widens a 95% interval at p = 0.84 from +/-0.0137 to +/-0.0263). PRIVATE surface - counts, rates and blake2b fingerprints only, no claim, evidence or document text and no client or company name in any artifact. Scripts `experiments/grounding-semantic/contract/gold_full_surface.py`, `gold_full_surface_spike_diag.py`, `gold_full_surface_cluster.py`; artifacts `contract/gold_full_surface_report.json` and `contract/gold_full_surface_spike_diag.json`. Markers `=== GOLD_FULL SURFACE VERIFICATION DONE ===`, `=== SPIKE DIAGNOSTIC DONE ===`, `=== CLUSTER DIAGNOSTIC DONE ===`. Measurements only - the coordinator holds the verdict
- `contract-mechanism-evals.log`, `contract-mechanism-evals-antigaming.log`, `contract-mechanism-evals-spotchecks.log` - dataset-contract verification of every REMAINING held-out mechanism eval, phase 2 of the contract workflow. Fourteen instruments enumerated from disk rather than from a list (`experiments/grounding-semantic/*.parquet`), excluding the four artifacts other agents own this session - `R20-H177_eval_B`, `R17-H143_evalset`, the blind arena and `gold_full`. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import. The main log covers `mechanism_evals_verify.py`: it indexes the assembled 760,618-row mix (a strict superset of the 721,210-row flagship, both carried as separate surfaces) in all four contract string forms on 16-byte digests so the six-form cross runs in both directions on claims and evidence against four surfaces, adds the DOCUMENT channel raw and stem-normalised, and runs C1 (structural, plus predicate-sensitive attestation where one is computable), C3, C5, C6 and C7 per eval. Two instruments bank no evidence at all - the ANTI-GAMING set and the R15 probe bank keep only a TabFact `table_id` - and their evidence is reconstituted with the builders' own serialisation rather than skipped. The R19-H166-A1 VitaminC holdout is not banked as a parquet and is rebuilt through `R20_baseline_legs.vitaminc_holdout()`, reproducing 38,126 rows over 5,553 pages exactly. Five live positive controls on the disjointness instrument: 251 mix passages read back at 251 of 251; the same passages whitespace-re-wrapped, raw reading 1 and normalised 251; 300 member table ids with the `1-`/`2-` prefix flipped, raw 0 and stem 300; the banked `R20-H175b_qlane_eval` reproducing its 485-of-487 figure including the 449-raw/485-truncated split; and the original `R20-H177_eval_B` document channel at 325 of 325. The antigaming log carries `mechanism_evals_antigaming_supp.py`, which measures pairwise overlap across the 14 banked per-arm anti-gaming sets (near-miss half Jaccard 1.0 everywhere, bind_row half 0.0017-0.0127) and locates the drift in `R15_gate_common.held_tabfact()`, whose `unique(subset=["table_id"], keep="first")` leaves membership identical and ORDER unstable across runs. The spotchecks log re-derives the four strongest findings from the archives directly, without the digest machinery, so a bug in that machinery cannot produce the same number twice. Scripts under `experiments/grounding-semantic/contract/` (`mechanism_evals_verify.py`, `mechanism_evals_antigaming_supp.py`, `mechanism_evals_spotchecks.py`, `mechanism_evals_rank.py`); artifacts `mechanism_evals_report.json`, `mechanism_evals_summary.md`, `mechanism_evals_antigaming_supp.json`, `mechanism_evals_spotchecks.json`. Measurement only - no verdict adjudicated, no bar amended

- `contract-arena_surface.log`, `contract-arena_surface_substring.log` - dataset-contract verification of the BLIND ARENA from the SURFACE side (`experiments/grounding-semantic/contract/arena_surface_verify.py` plus the three addenda `arena_surface_supplement.py`, `arena_surface_containment_exposure.py`, `arena_surface_substring_proof.py`). CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import, `HF_HUB_OFFLINE=1` - the live training draws on GPU0/1/2 are untouched. The arena is rebuilt filter for filter from `R8-H77_unseen_arena.load_subsets()` (10 RAGBench `__test` subsets, 2,264 responses, 6,784 distinct documents, 10,227 distinct response sentences) and asserted against the frozen figures; the mix is rebuilt through ONE module instance the way `R18-H150_arm_run.make_build_mix` does it (clean public 685,670 read UNTRUNCATED + the five `R20-H174_arm_run.LANES` = 760,618 rows / 17 groups, flagship sub-mix 721,210) and its 1,500/750 window geometry asserted against the banked `R20-H174_window_census.json` (1,215,222 windows, mean 1.5977, multi-window 0.2094) before anything is measured. Channels: C2 exact disjointness, three arena channels x three mix channels (claims, raw chunks, windows) x the eight string-form pairings crossed both directions, per subset and per DANN group; the document channel over every provenance identifier field the mix members carry, raw and stem-normalised; C4 through the banked R14-H136 instrument (8-gram, Jaccard >= 0.3, bidirectional, KILL 2%, per-corpus attribution) with a synthetic spike, TWO live near-duplicate controls (real mix chunks re-wrapped at a different window offset, and VitaminC's own official test+validation evidence) and a token-permutation negative control; a sub-document 8-gram CONTAINMENT reading per subset with its upper tail; C3 split semantics including a static scan of every mix loader and every document of every other RAGBench split checked against the mix. Artifact `experiments/grounding-semantic/contract/arena_surface_report.json`, census checkpointed to `tmp/arena_surface/`. Counts, identifiers and metrics only - no evaluation-surface text is emitted. Measurements only - the coordinator holds the verdict

- `phase1-readjudication-structural.log` - re-runs amendment C-A1's STRUCTURAL C1 test (does a distinct (claim, evidence) pair carry BOTH labels?) on every one of the eleven loaded training members, plus a uniform predicate-blind attestation reading of both legs for amendment C-A2's tests 2 and 3. CPU only, `CUDA_VISIBLE_DEVICES` forced empty, `HF_HUB_OFFLINE=1`; the three live training draws on GPUs 0/1/2 are untouched. The mix is rebuilt through the banked loader (`R10-H108_lane.public_train` under `R16-H142_G1_arm.untruncated_evidence` plus the five `R20-H174_arm_run.LANES`) and asserted at 685,670 clean + 760,618 total rows before any measurement. Grouping is on 64-bit hashes of each side with every flagged group re-verified on the literal strings, so a hash collision cannot manufacture a hit. LIVE POSITIVE CONTROL: the withdrawn poisoned `R20-H175b_qlane.parquet` is fed to the same gate and must read 8,986 pairs / 17,972 rows - it does, in all three string forms, with both legs identical to six decimals. Result: three source corpora carry both-label pairs (vitaminc 115 pairs / 236 rows, tabfact 45 / 90, psiloqa 5 / 10) and all five constructed lanes read zero, reproducing the amendment's pre-adoption control claim. Writes `experiments/grounding-semantic/contract/phase1_readjudication_structural.json`.
- `phase1-readjudication-conformed.log` - the same two readings on every conformed artifact already on disk (psiloqa, vitaminc, tabfact, quant_misbind, attr_pool, and the two halueval variants). Artifacts are READ, never rebuilt. Establishes that `psiloqa_conformed` still carries its parent's 5 structural pairs (its pipeline predates the amendment and has no structural filter) while `vitaminc_conformed` and `tabfact_conformed` read zero. Writes `experiments/grounding-semantic/contract/phase1_readjudication_conformed.json`.
- `R21-H180-eval-build.log` - build of the R21-H180 RAGTruth held-out evaluation surface, the campaign's second of three evaluation surfaces. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - the three training draws on GPUs 0/1/2 are untouched. Takes the English RAGTruth archive's TEST split whole (2,700 rows over 450 contexts, 6 responder models per context; neither the mix loader's `context > 50 chars` predicate nor the lineage read's `output > 20 chars` predicate drops a row) into the flat long serving shape of `R20-H177_eval_B.parquet` - one row per (claim, evidence) unit with UNTRUNCATED evidence, so the banked read machinery windows it at 1,500/750 without modification. The label unit is written into the artifact (`label_unit` column, every row `response`) and the human span annotations are carried through as columns, so a claim-level version can be derived later without re-deriving the eval. Script `experiments/grounding-semantic/R21-H180_eval_build.py`, artifact `R21-H180_ragtruth_eval.parquet`. Counts and distributions only; no model is scored
- `R21-H180-contract.log` - dataset-contract verification of that eval against clauses C1-C8 of `docs/experiments/dataset-contract.md` including amendments C-A1 and C-A2. CPU only. C2 runs the three string forms both directions on claims AND evidence against all 14 training-mix groups and the other evaluation surfaces, then the DOCUMENT channel - 8-gram containment of each eval context, each blank-line sub-passage and each eval claim into every reference unit, the read that came out an order of magnitude worse than the string channel on `R20-H177_eval_B`. C3 verifies the split axis and that the seven translated RAGTruth members are row-aligned translations of the TRAIN split only. C4 runs the R14-H136 gate with a spike control and a LIVE positive control built from perturbed eval contexts. Part C prices the claim-only channel with the banked `R20_claimonly_sweep` probe, both surface channels, a label-shuffled negative control and a transfer probe fitted on the ragtruth_en train member. Script `experiments/grounding-semantic/contract/ragtruth_eval_contract.py`, artifact `contract/ragtruth_eval_report.json`. Measurements only - the coordinator holds the verdict
- `R21-H180-arena-containment.log` - supplement to the R21-H180 eval contract pass, adding the two containment readings the main pass did not carry. CPU only. First, the ARENA channel measured with the banked arena-audit instrument reused verbatim (`contract/arena_surface_verify._max_pair`, Jaccard AND containment from one search, plus the whole-channel union pool, over the frozen R8-H77 arena of 10 subsets / 2,264 responses) - the same channel that caught 17 arena documents inside `halueval` chunks when exact matching saw none. Both directions, rolled up to the audit's own response-level exposure thresholds 0.10 / 0.25 / 0.50 / 0.90 / 1.00, with a live re-chunk positive control. Second, WHOLE-REFERENCE-UNION containment - the fraction of an eval unit's 8-grams present anywhere in a training-mix group rather than inside one single unit, which is what a source document re-chunked across several mix rows requires - per DANN group and cumulative, with its own re-chunk control and a composition breakdown of the exposed units by task type and label balance. Script `experiments/grounding-semantic/contract/ragtruth_eval_arena_containment.py`; merges into `contract/ragtruth_eval_report.json` under `clauses.C2.document_channel`. Measurements only - the coordinator holds the verdict
- `R21-H181-eval-build.log` - build of the R21-H181 HaluEval-`dialogue` held-out evaluation surface, the campaign's third evaluation surface and its FIRST out-of-domain one. CPU only, `CUDA_VISIBLE_DEVICES` forced empty before any import - GPU0/GPU2 carry an arena scoring pass and GPU1 a training draw, all untouched. HaluEval is a TRAINING member, but the mix loader takes only two of the archive's four configurations: `R10-H108_lane.public_train()` iterates exactly (`qa`,`knowledge`,`right_answer`,`hallucinated_answer`) and (`summarization`,`document`,`right_summary`,`hallucinated_summary`) with no split filter and no row filter, so `dialogue` and `general` are never read. `general` is excluded on top of that because it ships no evidence column at all. `dialogue` ships `knowledge`, so it is a grounding task and is the admissible surface. Built into the flat long serving shape of `R20-H177_eval_B.parquet` - one row per (claim, evidence) unit - which is what `R20_baseline_legs.flatten()` consumes unmodified; `dialogue_history` is carried as a column even though the score path does not read it. Measured against the registration block rather than trusting it: the archive holds 10,000 `dialogue` rows and each supplies TWO serving rows, so the surface is 20,000 rows over 10,000 pairs, twice the registered figure in both units. The longest knowledge block is 433 characters, far under the 1,500-char serving cut, so truncation and 1,500/750 windowing are both no-ops here. Script `experiments/grounding-semantic/R21-H181_eval_build.py`, artifact `R21-H181_halueval_dialogue_eval.parquet`. Idempotent - a complete artifact is verified and re-censused rather than rebuilt. Counts and distributions only; no model is scored
- `contract-halueval_dialogue_eval.log` - the three pre-registered measurements on that surface plus its full dataset-contract pass, in the order the registration block fixes, all before any model is ever scored on it. CPU only. B1 tests disjointness from the two TRAINED HaluEval halves in four SEPARATE channels - byte-exact, the eight raw/truncated/normalised form pairings both directions, 8-gram sub-document containment both directions, and the document channel - because a string-channel zero is not a disjointness result. B2 repeats all four against the blind arena, `gold_full` and sixteen other banked mechanism evals, REUSING `contract/arena_surface_verify.py`'s own `_index`/`_max_pair` over the `provenance_gate` 8-gram primitives at the same containment thresholds the arena audit used, so the numbers are directly comparable to the halueval-qa/hotpotqa exposure that audit found. B3 prices the claim-only shortcut with the `R20_claimonly_sweep` instrument on the pair-aware evidence-disjoint split chosen from a MEASURED pair census, with a label-shuffled negative control. C1-C8 are then verified including amendments C-A1 and C-A2, with C1's structural test and C-A2's strict-separation test both run. Scripts `experiments/grounding-semantic/contract/halueval_dialogue_eval_contract.py` and its supplement `halueval_dialogue_eval_supplement.py`, artifact `contract/halueval_dialogue_eval_report.json`. The supplement characterises the C2 non-zero string by string, reads out the sub-document threshold ladder, re-runs the live cross-config positive control UNDILUTED against the halueval group's own chunk set after the main pass's 60k-of-760k random sample diluted it to 0.169, and states the 8-gram containment denominator asymmetry that decides which arena direction is load-bearing. Markers `=== HALUEVAL-DIALOGUE EVAL CONTRACT PASS COMPLETE ===` and `=== SUPPLEMENT COMPLETE ===`. Measurements only - the coordinator adjudicates
- `R22-H183_H185_finqa_channels.log` - the three registered finqa channel measurements (R22-H183 / H184 / H185) over the 250 finqa items of the frozen blind arena (`R21-H179_arena_items.parquet`), CPU only, torch never imported. H183 prices every RESPONSE-ONLY surface feature (length, sentence count, numeric-token count and density, derivation markers, currency marks, digit runs, hedges, plus ten executor-added features kept in a separate block) by AUROC against the item label. H184 is the decisive one - deterministic number attribution from (response, evidence) in two variants reported separately and never combined: raw comma-stripped substring containment, and a scale-aware value matcher that parses both sides to floats, resolves parenthesised and leading-minus negatives, applies trailing scale words and document-level `(in thousands/millions/billions)` headers as multipliers, admits percent forms, and matches within 0.2% relative tolerance to absorb restatement rounding; five sensitivity arms (digit-boundary containment, no-x100 variants, zero tolerance, bare years excluded) bound how much of each reading is matcher looseness. H185 measures serving-window fragmentation using the shipped splitter `groundrails.dataset.shape.windows` at the default `Presentation` (1,500/750), with the offset geometry asserted byte-equal to the library output on every document read, binding each cited number to its table row label and to the whole column-header row (prose falls back to the containing sentence start), and reporting the exposure counts so a zero split rate can be told apart from an untestable one. Every AUROC carries a 2,000-draw item-resample bootstrap 95% interval at seed 184 - 20 negatives against 230 positives, so the intervals are the instrument. Script `experiments/grounding-semantic/R22-H183_H185_finqa_channels.py`, artifact `experiments/grounding-semantic/R22-H183_H185_finqa_channels.json`. Measurements only - the coordinator adjudicates
- `R22-H187_num_derive.log` - the build of the `num_derive` training member, the missing lane that teaches the model to CHECK A COMPUTATION (`experiments/grounding-semantic/R22-H187_num_derive_lane.py`, seed 2187, CPU only, no GPU touched, no torch). 30,000 rows / 15,000 minimal pairs over PUBLIC TabFact-train tables only - never FEVEROUS, whose half is exactly where `quant_misbind` is non-conforming on C3 and C8. Both legs state the same two operand values, each bound to a named row and a named column and readable verbatim in the evidence; the positive asserts the correct difference / percentage / sum / product and the negative asserts a wrong one, with the result ABSENT from the evidence on both legs by construction. The twin numeral is obtained by swapping correct results between two constructions from the SAME table sharing a digit-surface signature, which holds every response-only surface channel at exactly 0.5 and keeps both appearances of a numeral inside one document. Two earlier corruption designs were built, measured and discarded first (pooled draw conditioned on direction, claim-only probe 0.5761; cross-document swap, 0.1923) and are recorded in the builder docstring and manifest. Artifacts `R22-H187_num_derive_lane.parquet` and `R22-H187_num_derive_lane_manifest.json`. Measurements only - the coordinator adjudicates
- `R22-H187_num_derive_contract.log` - the eight-clause dataset-contract verification of the `num_derive` member against `docs/experiments/dataset-contract.md` including amendments C-A1 and C-A2 (`experiments/grounding-semantic/contract/num_derive_contract.py`, CPU only, no GPU touched). Every instrument is imported rather than re-implemented: the containment tokenizer, the whitespace normaliser, the AUROC, the distribution summary and the thirteen evaluation surfaces come from `contract/quant_misbind_verify.py`, the census from `provenance_gate.py`, the numeral helpers from `R17-H144_pairs.py`. C1 is read structurally first (C-A1) and then with an arithmetic checker that recomputes the result from the two stated operands, because containment is blind on this construction by design; C2 is re-measured on all three string forms against all thirteen surfaces after the build enforced the rejection at construction time. Per-stage artifacts `contract/num_derive_c{1,2,3,4,5,6,78}.json`, report `contract/num_derive_contract_report.json`. Measurements only - the coordinator adjudicates
- `R22_derivation_supply.log` - fetch of the five R22 derivation-supply corpora (`equate`, `numglue`, `hitab`, `scitab`, `drop`) through the banked pipeline `scripts/fetch_grounding_datasets.py`, whose `DATASETS` spec dict is the only thing extended for them. CPU only, no torch, no GPU; detached. The wave answers the R22 audit finding that no member of the mix carries a claim stating a value DERIVED from evidence values - every numeric member substitutes an OPERAND, so no row shows the model correct operands and a false computed result. Each corpus lands as `data/external/datasets/dataset-<name>.zip` (gitignored) plus a tracked `dataset-<name>.md` sidecar generated from the spec, written once dry and re-rendered post-fetch with the counts the download actually produced. Licence re-read at pull time from the source pulled, never from the survey: EQUATE MIT, NumGLUE ODC-By, HiTab C-UDA v1.0, SciTab MIT, DROP CC-BY-SA-4.0. SUPPLY ONLY - nothing here enters a training mix without its own registered hypothesis and arm
- `R22_supply_gates.log` - the contamination and derivation gates on those five corpora (`experiments/grounding-semantic/R22_supply_gates.py`, the R19 wave's structure over the banked `provenance_gate.py` instrument). CPU only. Three checks per corpus: the licence sidecar records the tag read at pull time; the derivation claim is RE-DERIVED from the data rather than trusted from the survey (AWPNLI's paired premises differing only in numeric tokens, HiTab's aggregation operators and answer-formula coverage, NumGLUE's Type_5/6 DROP overlap, SciTab's label vocabulary and `[BOLD]` rate, DROP's answer types); and the registered R14-H136 provenance gate - 8-gram, Jaccard >= 0.3, bidirectional, KILL above 2% max fraction - against all ten walled arena corpora with a spike control run first. Gate units are the deduplicated evidence texts (premises, passages, linearized tables), full coverage on every corpus. Verdicts land in `experiments/grounding-semantic/R22_<name>_gate.json`. Measurements only - the coordinator adjudicates
- `R22-H188_draw1_gpu1.log` / `R22-H188_draw2_gpu2.log` - the two training draws of R22-H188 DERIVATION-ENHANCED MIX, the H150 flagship recipe verbatim plus the `num_derive` lane (751,210 rows, 15 DANN groups; seeds 1188 / 2188, both permutation-checked distinct against the 25 orderings on disk before launch). Driven by `experiments/grounding-semantic/R22-H188_campaign.sh`, which chains train -> blind windowed decomposed-min arena read and stops there; each stage skips when its artifact is on disk, and the trainer resumes from `models/R22-H188-arm-draw<N>/resume.pt` replaying the same persisted permutation, so a relaunch after a container restart is the same command. `PYTORCH_CUDA_ALLOC_CONF` deliberately never set. The window-census control is repointed to `R22-H188_window_census.json` rather than weakened, so a drifting mix still aborts before a card
- `R22-H188_measure.log` - the post-training measurements `R22-H188_campaign.sh` does NOT run: the arm's PRIMARY gate (FinDVer read on both new checkpoints, `R22-H188_findver_read.py`) and the finqa detail owed by the registration (`R22-H188_finqa_detail.py`, score per draw then analyse). Driven by `experiments/grounding-semantic/R22-H188_measure.sh`, detached with `setsid` so it survives any agent or session death - the executor that built the campaign held these stages in its own in-process watcher, which died with it. It waits on ARTIFACTS (`R22-H188_arm_draw{1,2}_windowed_result.json`), never on processes: a process watch cannot tell a finished run from a killed one, and `pgrep -f` self-matches its own command line, a bug that idled a card for 40 minutes earlier in this campaign. Every stage skips when its artifact exists. FinDVer is an EVALUATION surface and is not in the mix; its flagship baseline (ie 0.6561/0.6658, numeric 0.4950/0.4967, knowledge 0.5715/0.5961) was recomputed from the banked score arrays and matched to four decimals before the arm launched. Measurements only - the coordinator adjudicates
- `R22-H189_lambda0_gpu1.log` - the R22-H189 mmBERT LAMBDA-0 ABLATION, the campaign's first DANN ablation on its own trunk in 22 rounds (`experiments/grounding-semantic/R22-H189_lambda0_run.py`, driven by `R22-H189_campaign.sh`, which chains train -> windowed arena read). One variable against `R18-H150` draw 1: `LAMBDA_MAX` 0.02 -> 0.0, so the domain head still trains on its own cross-entropy and only the reversed gradient reaching the trunk is removed. PAIRED BY DESIGN - seed 1150 matches the control, so the permutation fingerprint MUST collide with `R18-H150-arm-draw1`; a collision is normally a defect but here it is the design, and `R20_perm_guard` must not be read as failing. Both fingerprints were verified on a CPU census stage before any card was touched (init `a8dfdd7275073adf0057cfb40344c45b`, perm `7d13f9ac86a79574`) and are re-checked on the result file, which VOIDS the run on drift. PRIMARY is `ragtruth_nonen` against the k=6 mean 0.84472 with a 0.01106 floor - LOAD-BEARING at or below 0.83366, else BOUNDED SMALL, which asserts the adversary's contribution is under 0.011 and NOT a null. The arena read is REPORTED with a 0.02355 floor and carries no promotion path at k=1. `PYTORCH_CUDA_ALLOC_CONF` deliberately never set. Measurements only - the coordinator adjudicates
- `R22-H189_lambda0_gpu0_aborted_oom.log` - the first launch of the same arm on GPU0, ABORTED after 11 minutes without reaching step 200. The RTX PRO 4000's 24 GB ran at 24,088/24,467 MiB (98.4%), the oversubscription regime where the allocator thrashes rather than fails, giving above 3.3 s/step against 1.1 s/step on the 96 GB card. Only `models/R22-H189-lambda0/init_fingerprint.json` had been written, no resume point and no result, so the relaunch on GPU1 restarts from a deterministic identical state. Kept as the record of why the card was changed
- `R22-H188_draw2_rescore_gpu2.log` - the re-run of draw 2's per-item arena re-score on GPU2, the card that produced its banked read, after the first attempt on GPU1 hit the banked fidelity control (tatqa `|d|` 2.98e-04 against a 1e-4 tolerance). Same card reproduces at 5.08e-06 and all ten subsets pass, which CONFIRMS the abort was a cross-card difference and not a scoring error; the failing record is preserved as `R21-H179_arena_scores_fidelity_h188d2_crosscard_FAIL.json`. The guard is deliberately NOT loosened - on seven of the ten arena subsets a single rank flip already exceeds 1e-4, so the control demands bit-identical recomputation there, and the obligation sits with the caller to pin the card. Finding recorded in the R22-H188 ADDENDUM block of the canonical log
