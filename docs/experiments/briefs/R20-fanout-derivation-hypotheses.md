# R20 fanout brief - finqa/tatqa in-model derivation hypotheses

Fable hypothesis-design agent (read-only, 2026-08-16). Input to the R20 fanout adjudication; the coordinator adjudicates.

# Derivation failure mode - failure analysis, do-not-revisit list, and ranked hypotheses (R20 registration shapes)

All citations resolve to `/home/lab/workspace/private/ai-assistants/groundrails/docs/experiments/semantic-grounding-experiments.md` (the canonical log, cited by line), `/home/lab/workspace/private/ai-assistants/groundrails/experiments/grounding-semantic/R18-H157_finqa_failure_memo.md`, and the artifacts named inline. Standing frame: flagship k=4 mean 0.71583 (SE 0.00595, log:3831), pooled per-draw sd 0.01090 frozen under VARIANCE PROTOCOL AMENDMENT V1 (log:3846-3855), detection floors k=2 0.01542 / k=4 0.01090 / k=6 0.00890, target 0.74.

## (a) Failure analysis

**The R19 reframe governs everything below.** finqa is a comparative WIN vs the incumbent under the incumbent's own convention (+0.0688 point estimate; re-priced to UNRESOLVED at 1.77 SE against finqa's own 0.0620 two-draw spread, log:3765) and ranks 5th-weakest of our ten subsets (0.6825 2-draw; pubmedqa 0.6096 / hagrid 0.6424 / hotpotqa 0.6706 / emanual 0.6780 sit below it, log:3742-3749). The "sole loss" framing was a +0.1033 harness inflation of the incumbent on finqa (log:3649). What was NOT retired (fourth correction pass, log:3766): **"numeric-derivation blindness" stands as an in-model measurement** - R18-H157 put derivation errors at 47-57% of finqa's error mass, and no baseline convention touches that. Consequence for pricing: any derivation arm is now capability work and portfolio-secondary to R20-H174 (hagrid/emanual), not the campaign's main line - its bars must be honest about that.

**The H157 decomposition** (`R18-H157_finqa_autopsy.json`, memo above; 250 items, 20 negatives, both flagship draws reproduced bit-exact):

- **derivation_arithmetic dominates every view**: consensus 7/15 (0.467, SE 0.129), draw1 24/42 (0.571), draw2 12/23 (0.522); threshold-free rank-loss mass **0.280** vs table_binding's 0.114. Both directions: true derived figures under-credited (FNs: 101 `8.2-5.5=2.7`, 147 `134-63=71`, 213 `(5829-5735)/5735=1.6%`, 191 `172+179+147=498`), wrong derived figures over-credited (FPs)
- **The FP direction carries the negative class**: 55-75% of unsupported items pass the operating point (FP rate 0.55 d1 / 0.75 d2, SE ~0.10 - a coarse instrument at n=20)
- **Read the FP exemplars closely - most are not wrong arithmetic, they are wrong OPERAND/ROLE/SIGN/PERIOD selection**: item 114 (74.9M *repurchase* misused as the *decrease*), 189 (sign, −305 claimed +305), 31 (direction word, "decrease" vs actual +2,751 increase), 157 (sum from wrong operands), 36/229 (correct arithmetic, wrong year), 71 (per-share \$78.29 claimed as a total in millions), 67 (correct value cited, question asked for a portion). The compare subtype (100: "291 greater than 180" scored 0.136 as FN) sits inside the derivation family per the probe bank's relational-compare leg
- **Secondary classes are small**: table_binding consensus 3/15 (binding probes read installed, bind_col 0.948-0.960 / bind_row 0.988-0.992); scale_unit 1/15; entity_confusion 2/15; window_boundary ~0
- **tatqa shows the same argmin pattern**: 75%/68% of finqa/tatqa bottom-quartile argmins are derived-arithmetic sentences scoring ~0.01 (log:1942, log:2062); the taxonomy-bearing tatqa error records exist in `R19-H161_L2_items.parquet` (250 rows, finqa/tatqa/delucionqa x 3 checkpoints, H157 classes + sinking sentences) - tatqa's largest-growing class under the H159 enriched mix was scale_unit, not binding (`R19-H161_L2_result.json`)
- **Instrument caveats**: FP-side per-class counts carry SE ~0.10; a visible share of FP mass is question-relative ("does not provide the ratio the question asks") and structurally unreachable under the question-blind harness read - the serving-convention door the H151 ruling closed and the R20 sweep escalated to the author (log:3862)

**Why the encoder cannot do this, three independent chance measurements on the mmBERT trunk**: the H133 licensing diagnostic read AUROC(correct vs wrong derivation) **0.4924** on 985 held-out TabFact tables while penalising both maximally (0.24 vs 0.91 verbatim; log:2679); relational_compare probe 0.51 on both flagship draws; tier-1 bare-derivation probes at chance on both H133 arm checkpoints even after training 42,500 derivation rows (log:2846). The R17-H143 ladder located the capability cliff between 362M and 596M and attributed it to reasoning post-training, not size (tinies 0.50-0.51, Qwen3-0.6B 0.8514, Qwen3-32B 0.9708; log:2842, 2859). H169 killed the EuroBERT bet that a different sub-400M trunk changes this (chance in-domain with and without the adversary; log:3789-3807).

**The H133 postmortem, precisely** (`R14-H133_launch_design.md` + log:2870-2880): the lane went through three forms - v1 DEFECTIVE (claim-side leak), v2 leak-clean, v3 trace-conditioned after the author's verbatim ruling ("grounding models are not reasoning models - exclude this from the data; but for the same claims we can attach a reasoning trace... and add this to the corpus"). v3 made verification a matching task (trace operands ground against evidence cells, claim number matches trace conclusion; 42,500 core + 7,500 relational rows, all verify bars passed). Draw 1 cleared its pilot (finqa 0.6870, +0.0537 over control) but the **2-draw verdict was REFUTED at bar**: draw 2 finqa 0.6557, mean 0.68474 breaching arena/hotpotqa/tatqa holds (hold-unstable across seeds), and the binding anti-gaming clause breached on both forms (traced −0.0659, driven by magnitude_shift −0.0992; log:2852). What the arm DID install: scale/unit 0.9527-0.9582 and bind_col 0.9084-0.9321. The follow-on pair separated skill from damage: **R17-H145** (relational slice alone at sub-block scale) KILLED - bind_col 0.5721, sub-block scale installs nothing (log:2943); **R17-H146** (misbind at core scale, 30k rows, zero derivation rows) SHIPPED - bind_col 0.9555, all holds green, graduated into the flagship as quant_misbind (log:3116). The recorded mechanism law: *verification families install at core scale and leave holds green; derivation tasks damage regardless of form; the install variable is family scale, the damage variable is arithmetic content.*

**Under the repaired variance protocol, H133's kill is formally unresolved but its mechanism verdict stands**: the V1 re-adjudication (log:3853) leaves H145/H133-class kills "unresolved as before" on the arena mean - but the anti-gaming breach, the chance diagnostics, and the H145/H146 scale dissociation are non-arena measurements and are untouched. Note also ruling 3 (log:3832): the anti-gaming hold that killed H133 is now DIAGNOSTIC, never a promotion clause, until a pooled sd exists for it.

**Adjacent standing facts a new arm must respect**: the probe bank is DEAD as a steering instrument (R19-H163; no probe SUPPORTED as arena-predictive, corrections log:3691 - mechanism gates must be fresh held-out evals, not the banked probe bank); the guard suite does not predict blind transfer (two consecutive kills with every guard green, log:3384); H159's table collapse (finqa −0.1429, tatqa −0.1328 with FinDVer riding as 2,400 of 796,716 rows) has **NO CONFIRMED MECHANISM** after the four-lane H161 autopsy (log:3432) - so "the prose lanes did it" is a brief's diagnosis, not a banked verdict, and any arm adding lanes near the table subsets needs the H174-style TABLE GUARD (finqa/tatqa/delucionqa within one across-seed spread: 0.062/0.025/0.012, log:3874).

## (b) Do-not-revisit list

1. **Bare derived-value claims as training data** - excluded by the author's verbatim H133 ruling; three chance measurements (0.4924 diagnostic, compare probe 0.51, tier-1 probes) say the trunk cannot learn the discrimination. Any lane whose negatives are detectable ONLY by computing (off-by-one-step, wrong-value-close) re-enters H133's corpse
2. **Trace-conditioned derivation matching (H133 v3 shape)** - REFUTED at bar, ~12 GPU-h; hold-unstable across seeds, anti-gaming breach −0.0659; the 42,500 derivation rows were the damage carrier (H145/H146 dissociation)
3. **Sub-block-scale skill slices** - H145 killed at draw 1 on both co-primaries; 1-2% mix shares install nothing
4. **Serving-side numeric canonicalization on frozen weights** - R12-H119 REFUTED in both directions despite +8.3/+13.0 pt surface-agreement gains; effect is checkpoint-idiosyncratic (tatqa +0.045→−0.023 across draws); the serving-wrapper canonicalization line is closed (log:2579-2593). The LEGALITY precedent survives and is quoted in H-D below
5. **Serving-read / aggregation changes** - H151 closed the door (max is load-bearing AND minimum-spread; log:3281-3288); H156 learned aggregator resolves-below under V1 (z −2.08). Question-conditioned reads are author-escalated strategy, not registrable here
6. **Decoder-LM / trunk-swap route for numeric competence** - EuroBERT dead at chance twice (H168/H169); tiny decoders dead at chance (H143 Stage A); the distill student closed NOT-VIABLE-AT-BAR after amendment A2 (H144, log:3020); sub-400M generative escalation is the author's held question, not a lane
7. **Batch-composition / curriculum levers on numeric lanes** - H135 refuted in the strongest direction (paired presentation actively harmed; "batch-composition levers on the admitted lane close", log:2761); loss-upweighting the table lanes has no mechanism support - H161/L2 found binding error mass FELL while bind_col fell, dilution NOT_SUPPORTED both legs
8. **Walled supply** - FinQA and TAT-QA source corpora (RAGBench arena parents) are WALLED, never proposed (H157 memo §Lever mapping); raw EDGAR outside the restricted slice stays banned (`dataset-edgar-restricted.md`)
9. **Probe-bank-gated registration** - H163 killed the bank as an instrument; bars below may not cite bind_col/scale_unit/compare probe numbers as PRIMARY evidence

## (c) Ranked hypotheses, registration shape

All four are priced under amendment V1 (pooled sd 0.01090 FROZEN). None predicts an arena-mean effect above the k=2 floor 0.01542, and finqa's own subset noise (two-draw spread 0.0620 → subset-level 2-draw detection floor ~0.078) makes finqa-level promotion bars inadmissible at any affordable k. **Every training arm below is therefore declared EXPLORATORY on the arena mean with a non-arena mechanism gate as PRIMARY** - the exact H166-A1 pattern (log:3880-3886). Guard for all: k=2 arena mean >= flagship k-mean − 0.01542; TABLE GUARD triple (0.062/0.025/0.012); gold_full >= 0.84; non-EN >= 0.82; anti-gaming recorded diagnostic per ruling 3.

---

### H-A (rank 1) - FinDVer frozen-instrument read: does the in-model derivation deficit exist off-arena?

**Causal claim**: the flagship's derivation blindness (measured only on arena finqa and on synthetic TabFact probes) reproduces on human-annotated refuted financial computations it has never touched; therefore FinDVer-numeric AUROC on banked flagship draws will read materially below the flagship's own arena-table level, FP-dominated.

- **Design**: measurement only, zero training. Score banked flagship draws (d1/d2 now; d1-d6 when R20-H172 lands) on `R19_findver_lane.parquet` (2,400 human entailed/refuted claims over 2024 10-K/10-Q, balanced 1199/1201; subsets ie 850 / numeric 850 / knowledge 700), windowed decomposed read, per-subset AUROC + FP/FN split at the macro-F1 threshold
- **Kill-gate (this IS the gate - the arm is itself diagnostic)**: none needed beyond artifact integrity; the lane, manifest and verify JSON are banked
- **Supply/licence/wall**: FinDVer MIT (re-read at fetch 2026-08-13, `data/external/datasets/dataset-findver.md`); gate GREEN, max 8-gram fraction 0.000231 vs 0.02 bar, spike control 10/10 (`R19_findver_gate.json`); wall-safe by the EDGAR-population precedent (2024 filings vs walled pre-2020 corpora)
- **Pre-registered branches**: numeric-subset AUROC < 0.65 with numeric < (ie+knowledge)/2 − 0.05 → deficit CONFIRMED derivation-specific off-arena; FinDVer-numeric is banked as the standing non-arena mechanism instrument for every arm below (replacing the dead probe bank). 0.65-0.75 → partial, instrument still usable as a delta gate. >= 0.75, or numeric ≈ other subsets → **the in-model deficit does not transfer to human financial claims; H-B/H-C lose their finqa-side premise and the derivation direction is DE-PRIORITISED behind H174** - the cheap kill of this whole report
- **Predicted finqa/tatqa/mean effect**: none (no training)
- **Cost**: one short GPU read (<0.5 GPU-h) or CPU scoring of 2,400 pairs; k n/a
- **Why not H133**: it trains nothing; it builds the instrument H133 never had

### H-B (rank 2) - core-scale relational-compare / direction verification lane

**Causal claim**: comparison of two verbatim-present values ("X greater than Y", "increased from X to Y") is a VERIFICATION family, not arithmetic, and - like misbind, which failed at sub-block scale (H145 bind_col 0.5721) and installed at core scale (H146 0.9555) - it will install when trained at core scale (~25-30k pairs), which the campaign has never done (compare has only ever ridden at 1,500 sub-block pairs → probe 0.5198, no transfer). This is the explicit new question distinguishing it from H133: **H133 asked whether the trunk can verify arithmetic (no); H-B asks whether the sub-block→core-scale install law that rescued misbind also holds for ordering/direction, the one derivation subtype requiring zero computation.**

- **Lane**: claims asserting greater/less/highest/increase/decrease between two values both verbatim in evidence; negative twin flips ONLY the relation/direction word (the comparative_flip construction, already proven as an eval family) under full H146 leak discipline (claim-only converged probe < 0.55, within-pair < 0.60, surface parity, direction 50/50). Sources: TabFact/FEVEROUS tables + EDGAR-restricted MD&A prose (Apache-2.0, provenance gate GREEN banked, 34,014 admitted chunks in `R18-H150_edgar_admitted.parquet`; new pairings re-run the R14-H136 8-gram census per the H174 clause). Optional rider: the 850 FinDVer-numeric human pairs as one small DANN group (census re-run; too small to register alone)
- **Diagnostic kill-gate (free, CPU, before build)**: (i) error-mass precondition - reclassify the 50 H157 error records + the tatqa/finqa records in `R19-H161_L2_items.parquet` into depth-1 compare/sign/direction (decidable by ordering present values) vs multi-op arithmetic; **PASS if >= 20% of derivation-class rank-loss mass (i.e. >= 0.056 absolute of finqa rank loss) is compare-decidable**; (ii) constructibility census on the EDGAR admitted parquet + TabFact: >= 25,000 buildable pairs with both operands verbatim; (iii) H-A must not have fired its >= 0.75 branch
- **Bars**: EXPLORATORY, declared k=2. PRIMARY (mechanism, non-arena): fresh held-out compare eval (doc-disjoint, built by the lane generator at a different seed, H146-style) arm >= 0.80 from a measured chance baseline on the flagship checkpoint, AND FinDVer-numeric (per H-A baseline) not below baseline − 0.02. GUARDS/HOLDS as in the header. Record-only: finqa/tatqa arena reads
- **Predicted effects**: finqa +0.01..+0.03, tatqa +0.005..+0.015 (both sub-floor at subset level, record-only), arena mean +0.00..+0.006 (sub-floor, hence exploratory)
- **Kill**: gate (i) or (ii) fails → never built; draw-1 arena mean < 0.695 or table-guard breach → draw 2 unspent; mechanism eval < 0.65 at draw 1 → family closed as capacity-limited alongside derivation
- **Cost**: CPU build + ~13 GPU-h (2 draws)
- **Why it survives where H133 died**: zero arithmetic rows - the damage carrier identified by the H145/H146 dissociation is absent by construction; core scale, per the H145 falsification; the anti-gaming clause that killed H133 is now diagnostic (ruling 3), and the word-flip family is the one the H133 anti-gaming set itself scored as untraceable-but-clean

### H-C (rank 3) - operand-role / sign / period misbind lane over EDGAR prose

**Causal claim**: the dominant FP exemplars are not failed arithmetic but failed BINDING of a present value to its semantic role, sign, or period in prose (114 repurchase-as-decrease, 189 sign flip, 36/229 wrong year, 71 per-share-as-total); the graduated misbind construction (present value, word-level corruption, no computation) extended from table row/column to prose role/sign/period at core scale (~25-30k pairs, EDGAR MD&A register - the measured 382x finqa-side register gap) will install role binding while holds stay green, exactly as quant_misbind and the A2-ruled scale_word lane did for their channels.

- **Families**: role-swap (value reattached to the wrong labelled quantity in the same passage), sign/direction-swap on stated changes ("a decrease of \$X" ↔ "an increase of \$X" where the passage states the direction in words), period-swap (the H157 lever-3 freebie: wrong-year variants of correct statements). All values verbatim-present; H146 leak bars + the H145 build lessons (dimension gates, direction-stratified probe folds)
- **Diagnostic kill-gate (free, CPU)**: (i) reclassify the H157 FP records by their `unsupported_explanation`: **PASS if >= 30% of FP rank-loss mass traces to mislabeled operand/role/sign/period rather than wrong computation or question-relativity**; (ii) EDGAR census: >= 20k sentences carrying value + role word + period in the admitted chunks; (iii) H-A branch check as in H-B
- **Bars**: EXPLORATORY, k=2. PRIMARY: fresh held-out role/sign/period eval arm >= 0.80 from measured chance baseline AND FinDVer-numeric FP-side improvement (refuted-claim detection rate at fixed FN, vs the H-A baseline) - this is the only lane on the board that targets the FP direction the autopsy says carries the negative class. Guards/holds as header
- **Predicted effects**: finqa +0.005..+0.02 (FP-side), tatqa +0.00..+0.01, mean +0.00..+0.005 (sub-floor)
- **Kill/cost**: as H-B; ~13 GPU-h. May be merged with H-B into one H174-style two-lane portfolio arm (halves GPU cost; per-lane mechanism gates keep attribution) - coordinator's call at registration
- **Why not H133**: word-level corruption of present values is the H146/scale_word graduated pattern, not the derivation task; it attacks the operand-SELECTION half of the "derivation" error mass, which requires no arithmetic to verify

### H-D (rank 4) - numeracy canonicalization trained-through (digit-grouping / currency-spacing normalization at train AND serve)

**Causal claim**: H119 proved the transform is legal and surface-effective (+8.29 pts thousands-separator, +13.01 pts currency-spacing bare-token agreement on finqa; all 3,240 finqa `$` occurrences are spaced against unspaced claims) but refuted it on FROZEN weights (checkpoint-idiosyncratic sign flips); training THROUGH the canonicalized surface - never tested - lets the trunk learn one numeric surface form, converting the idiosyncrasy into a stable representation gain on numeric subsets.

- **Legality, quoted (log:2482)**: "H119 legality - GRANTED: an idempotent, subset-blind text transform before tokenization is inside the frozen-read boundary (R8-H101 precedent), **conditional on shipping in the library serving path identically for every corpus and every future input. A transform retained because it helps one arena subset is arena-fitted preprocessing and voids the lever.**" A trained-through transform also survives H151, which closed aggregation/read changes, not pre-tokenization text transforms; the ship-identically clause binds the serving path
- **Design**: flagship recipe verbatim + the two measured-effective rules only (thousands-sep, currency-spacing; percent rule measured +0.0 and stays dropped), applied identically to every training row and every serving input; 2 draws
- **Diagnostic kill-gate (free, CPU)**: tokenize the H157 sinking sentences and their argmax windows with the mmBERT tokenizer; **PASS if error items' load-bearing numerals are more fragmented (tokens per numeral) than matched non-error numerals at >= 1 sd**, i.e. surface fragmentation actually co-varies with the failure; else the mechanism has no footing and the arm dies free
- **Bars**: EXPLORATORY, k=2. PRIMARY: FinDVer-numeric delta vs H-A baseline >= +0.03 AND non-numeric subsets unchanged (the H119 "localized" property re-confirmed under training). Guards/holds as header. Arena record-only; predicted mean ±0.005, finqa +0.00..+0.02
- **Kill/cost**: gate fail → free kill; ~13 GPU-h otherwise
- **Why not H119's corpse**: H119 never retrained; its refutation is specifically a frozen-weights result and its own mechanism note ("what each checkpoint does with the surface gap is idiosyncratic to its weights") is the premise here - training fixes the weights to the surface

### Explicitly refuted directions from the brief (not registered)

- **Direction (a) as stated** (wrong-derived-value negatives: off-by-one-step, wrong-value-close): the negative is detectable only by computing → walled by the author's H133 ruling and three chance measurements. Its word-detectable subtypes (sign flips, percent-vs-point, direction) are exactly H-B/H-C and are registered there. FinDVer as a training lane alone is supply-dead (2,400 rows, ~0.3% of mix, undetectable everywhere; already rode once inside H159, killed, mechanism unattributed) - it serves as instrument (H-A) and rider (H-B)
- **Direction (c)** (auxiliary numeric same/different objective): isomorphic to the H133 matching task on a trunk measured at 0.4924 on that discrimination; an aux head cannot outrun its trunk representation, and suppressing computed-register numbers false-alarms on the 75.7% of absent-number finqa claims that are gold-supported (log:2664) - the A4-(ii) product clause forbids it
- **Direction (d)** (curriculum/loss-weighting on numeric rows): H135 closed batch-composition levers; H161/L2 refuted the dilution mechanism both legs; binding sits at ceiling - there is no measured deficit that upweighting existing lanes addresses

**Recommended sequencing**: H-A immediately (near-free, and its >= 0.75 branch kills H-B/H-C's premise before any build); H-B/H-C kill-gates on CPU in parallel; any surviving training arm queues BEHIND R20-H174 and R19-H166-A1 per the standing queue - under the R19 reframe the derivation line no longer owns first claim on a GPU card.
