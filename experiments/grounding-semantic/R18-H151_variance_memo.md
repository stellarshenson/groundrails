# R18-H151 Variance Knowledge Memo

Harvest of every seed-variance and aggregation-pooling measurement the campaign has already banked, assembled for the H151 variance attack (anatomy + pooling-variant selection on the twin checkpoints, seeds 1142/2142). Each bullet states the number, the protocol regime it was measured under, and the artifact that carries it.

## Seed-variance constants by protocol regime

- **Truncated-training + pre-windowed decomposed-min read (R8 era)** - identical-recipe rerun gap 0.0295 on the blind mean (H90 0.7213 vs H100 0.6918); the campaign's original ±0.03 noise constant (canonical log, R8-H100)
- **Min-over-sentences amplifies training noise ~4x** - same H90/H100 pair: whole-response gap 0.0074 vs decomposed-min gap 0.0295; the min concentrates each response's score on its weakest sentence so per-sentence miscalibration compounds (canonical log, R8-H100)
- **Truncated-training + windowed read, clean recipe (the banked per-subset seed sd)** - pooled within-pair sd from the four paired draws (H105 pair + H108 pair): pubmedqa 0.0216, hotpotqa 0.0144, tatqa 0.0290, delucionqa 0.0658 (banked constants); recomputed here for all 10 subsets: covidqa 0.0169, emanual 0.0308, expertqa 0.0220, finqa 0.0191, hagrid 0.0147, techqa 0.0395 (`R9-H105_windowed_result.json`, `R9-H105_draw2_windowed_result.json`, `R10-H108_lane_draw{1,2}_windowed_result.json`)
- **Clean-recipe arena-mean draw spread is far tighter than the old ladder's** - H105 pair |0.70471 - 0.70151| = 0.0032; H108 pair |0.70618 - 0.70373| = 0.00245 (canonical log, R9-H105 / R10-H108; flagged at log line "the clean recipe's draw spread so far (0.0032) is far tighter than the contaminated ladder's")
- **Lane-mix recipes are noisier than the clean recipe under the same truncated training** - H133 derivation-lane pair mean spread 0.0190 (0.70374 / 0.68474); per-subset swings: emanual -0.0557, tatqa -0.0418, finqa -0.0313, delucionqa -0.0257, pubmedqa +0.0248, hotpotqa -0.0230 (`R14-H133_arm_draw{1,2}_windowed_result.json`); H107 lane pair spread 0.0114 with finqa swinging 0.0548 (0.4809 / 0.4261) (canonical log, R10-H107)
- **Windowed-MIL twin protocol (the regime under attack)** - arena-mean two-seed spread 0.0243 (0.72498 / 0.70073), roughly 8-10x the clean truncated pairs' mean spread; per-subset draw1 → draw2: tatqa -0.0760, techqa -0.0719, pubmedqa -0.0452, hotpotqa -0.0351, finqa -0.0336, hagrid -0.0182, expertqa -0.0149, covidqa +0.0016, delucionqa +0.0242, emanual +0.0266 (canonical log, H142-T reflection; `R16-H142_G1_twin_windowed_result.json`, `R16-H142_T_draw2_windowed_result.json`)
- **Implied per-subset sd inflation, twin vs truncated pairs** - |swing|/sqrt(2) vs the truncated-regime sd: tatqa 0.0537 vs 0.0290 (1.9x), hotpotqa 0.0248 vs 0.0144 (1.7x), pubmedqa 0.0320 vs 0.0216 (1.5x), techqa 0.0508 vs 0.0395 (1.3x), finqa 0.0238 vs 0.0191 (1.2x); covidqa, delucionqa, emanual, expertqa, hagrid at or below their truncated-regime sd (derived from the two tables above)
- **techqa was already the noisiest subset under the truncated regime** - its 0.0395 truncated-protocol sd (H108 pair alone spread 0.0767) is the largest of the 10; the twin protocol inflates an already-noisy subset, it does not create the noise (derived from the H105/H108 pair JSONs)
- **In-domain suites are far more seed-stable than arena deep subsets on the same twin pair** - gold_full 0.8484 / 0.8562 (spread 0.0078), gold 0.8327 / 0.8333 (0.0006), RAGTruth EN 0.8160 / 0.8315 (0.0155), non-EN 0.8415 / 0.8454 (0.0039) (`R16-H142_G1_twin_result.json`, `R16-H142_T_draw2_result.json`, H142-T draw-2 adjudication block)
- **Anti-gaming instrument noise is priced** - paired-bootstrap SE_delta 0.00635 on the n=543 headline-pair instrument; the twin pair's anti-gaming spread 0.7507 / 0.7724 = 0.0217 is ~3.4x that SE (`R16-H142_T_seband_result.json`, H142-T draw-2 adjudication)
- **Small-scale in-domain training noise (lexical-era reference)** - mmBERT-22L trained 3x: 0.8479 / 0.8485 / 0.8502, spread 0.0023 on gold (canonical log, round-3 era)
- **Readout-head seed noise (frozen trunk, 331k-param attention readout)** - pubmedqa delta across 4 seeds 0.0711 / 0.0666 / 0.0800 / 0.0787 (mean 0.0741, std 0.0064); blind-mean delta std 0.00541; per-subset delta std up to finqa 0.0364, delucionqa 0.0270, techqa 0.0222, tatqa 0.0204; even a small head's training seed moves per-subset arena deltas by ±0.02-0.036 (`R16-H141_controls.json`, control B)

## Window-depth census (what the max selects over)

- **(sentence, window) pairs per response** - techqa 156.2, expertqa 68.7, delucionqa 27.7, pubmedqa 26.3, emanual 22.6, finqa 11.7, hagrid 7.8, tatqa 7.8, covidqa 6.3, hotpotqa 4.7 (`R16-H141_autopsy.json`, arena_window_stats)
- **Windows per sentence (the set the max actually runs over)** - techqa mean 22.49 / max 82; expertqa mean 10.71 / max 135; delucionqa 5.48; finqa 5.18; tatqa 4.15; hotpotqa 4.02; covidqa exactly 4.0; emanual 3.99; hagrid 3.61; pubmedqa exactly 5.0 with zero dispersion (`R16-H141_autopsy.json`, arena_window_stats)
- **pubmedqa and covidqa window counts are per-response constants** - a count-aware correction (max - lambda*log n) collapses to a per-response shift there and is mathematically unable to move their AUROC; all 9 penmax variants read exactly 0.0 delta on both (H141 autopsy verdict + caveat)
- **pubmedqa/covidqa/hotpotqa have zero truncation exposure** - on the twin checkpoint the windowed and truncated reads are identical to 4 dp (pubmedqa 0.6725 / 0.6725, covidqa 0.7645 / 0.7645, hotpotqa 0.6728 / 0.6728); their "windows" are whole chunks, so their seed swings cannot come from within-document window argmax flips (`R16-H142_G1_pair_result.json`, truncated vs windowed read)
- **Subsets that actually engage multi-window scoring on the twin** - windowed minus truncated on the same draw-1 checkpoint: emanual +0.0920, techqa +0.0588, delucionqa +0.0422, expertqa +0.0160, finqa +0.0128, tatqa +0.0039, hagrid -0.0140 (`R16-H142_G1_pair_result.json`)
- **Twin training mix is shallow** - mean 1.507 windows/row, 20.07% multi-window, max 40 windows/row; vitaminc (54% of the mix) is single-window (`R16-H142_G1_twin_result.json`, window_census; H142-T mechanism note)
- **H141 killed the max-saturation premise by measurement** - the registered "~26 windows/item" pubmedqa premise counted (sentence, window) pairs, not the max's window set (5, constant); count-aware aggregation was DEAD pre-registration on that measurement (canonical log, R16-H141 autopsy verdict)

## Pooling and aggregation variants already measured

All arena numbers below are single-checkpoint deterministic reads on the H108 draw-1 weights (truncated-training regime) unless stated; none was ever measured for seed spread - the variance side of every variant is unmeasured and is what H151a/H151b must supply.

- **top-2 mean over the sentence's windows** - blind mean 0.70442, -0.00176 vs hard-max 0.70618; per-subset: pubmedqa +0.0050, covidqa +0.0016, techqa -0.0079, tatqa -0.0179, hotpotqa -0.0447 (H141 autopsy, raw_top2_mean; isotonic version -0.00265)
- **top-3 mean** - 0.68957, -0.01661; hotpotqa -0.0619, techqa -0.0314; the only large gain anywhere: delucionqa +0.0833 (H141 autopsy, raw_top3_mean)
- **top-5 mean** - 0.66959, -0.03659 (H141 autopsy)
- **full mean over windows** - 0.65189, -0.05429; techqa -0.1452, tatqa -0.0987 (H141 autopsy)
- **penmax (max - lambda*log n), lambda 0.05..2.0** - best mean at lambda 0.05: 0.70610, -0.00008; monotonically worse with lambda (lambda 2.0: -0.04884); exactly 0.0 on the constant-window subsets by construction (H141 autopsy)
- **logsumexp over window logits, 6 temperatures** - T0.1 +0.00065, T0.25 +0.00171, T0.5 +0.00186 (but techqa -0.0205), T1.0 -0.00137 (techqa -0.0597), T2.0 -0.01893 (techqa -0.1479), T4.0 -0.05193 (techqa -0.2411); softening costs monotonically and the cost concentrates on the deepest subset (H141 controls, `R16-H141_controls.json`)
- **softmax-weighted mean, 6 temperatures** - T0.1 +0.00025 with worst subset -0.0015 (the gentlest variant measured); degrades monotonically to T4.0 -0.01870 (techqa -0.0668, tatqa -0.0542) (H141 controls)
- **logistic regression over 8 window-score statistics** - 0.68622, -0.01996; techqa -0.1266; recovers only +0.0092 of the readout's +0.0711 pubmedqa lift (13%) (H141 autopsy, the EMBEDDING-branch decision number)
- **capacity-matched MLP over the same 8 scalars** - 0.66934 / -0.03684 (arch-matched) and 0.66691 / -0.03927 (param-matched); fits the train slice better than logreg (0.8718 vs 0.8620 val) yet reads the arena worse - learned scalar aggregations extrapolate badly on deep window sets (H141 controls)
- **consensus top-2 (within-chunk mean of top-2 windows replaces the max)** - hagrid -0.0042 / -0.0058 on the H108 pair vs a +0.010 bar, -0.0037 / -0.0032 on the H105 pair; techqa -0.0346 on H108 d2; REFUTED in sign - the closest direct precedent to H151b's top-k candidate, measured on the truncated regime (canonical log, R13-H124)
- **top-2-union composite premise (top-2 units concatenated as one extra premise)** - hotpotqa -0.0056 vs +0.030 bar, mean +0.00044; fire-rate diagnostic refuted the premise independently of AUROC (canonical log, R13-H125)
- **learned attention readout over window embeddings (331k params)** - blind mean 0.70991, +0.00373 over hard-max; pubmedqa +0.0711 (seed-hardened 0.0741 ± 0.0064), emanual +0.040, expertqa +0.037, at hotpotqa -0.052 / tatqa -0.048; NOT LICENSED - the trade re-ranks registers (H140 G1 pilot + H141 control B)
- **Sentence-side variants (min over sentences, the other aggregation axis)** - pure min is primary; softmin tau 0.5 / 1 / 2 / 4 read 0.6975 / 0.6912 / 0.6872 / 0.6853 vs min 0.6893 (softening the min costs monotonically); RAGTruth-tuned softmin tau 2.0 read 0.6613, -0.0280 - proxy-tuned aggregation shapes do not transfer to the arena (canonical log, R8-H92/H94; the H94 lesson H151b's gold-side-only selection inherits)
- **H147 re-aggregations** - 8 re-aggregations of banked per-sentence scores on hagrid/emanual: the shipped min is best or within 0.03 everywhere; arg-min localisation 0.636-0.727 - the min already finds the bad sentence (canonical log, R17-H147)
- **Lexical-tier distributional pooling** - top-2 mean, logsumexp, count-above-threshold, top1-top2 margin moved macro-F1 -0.005 to +0.005; max-over-chunks already extracts what the score distribution knows (canonical log, R1-H10; different model, same aggregation question)
- **Train-slice (in-domain-like) sentence AUCs under the same variants** - max 0.83798, penmax-best 0.83646, top-2 0.81319, top-3 0.79088, mean 0.76323, logreg 0.86203; LSE T2.0 0.85796 BEATS hard-max on the train slice while costing -0.1479 on arena techqa - in-domain fit and arena cost point in opposite directions for soft poolings, the central tension H151b's gold-side selection must navigate (H141 autopsy fit block + controls)

## Model-level averaging measurements (variance levers already priced)

- **Weight-space soup of two same-recipe draws** - 0.69218, below both parents (0.70471 / 0.70151); KILLED - cross-seed weight averaging compounds OOD function differences (canonical log, R11-H118)
- **Output-probability mean of the same two draws** - 0.72067, +0.01756 over the pair mean, beats both members on 8/10 subsets - the draws' functions are individually noisy OOD in ways that cancel in probability space; cannot ship (614M over the serving budget) but is the campaign's strongest measured variance-cancellation result (canonical log, R13 anchor-teacher)
- **Output ensembling pays only between near-equals** - H92 pair averaging +0.0037 over the best member; a third member 0.025 below the pair dilutes (H97 REFUTED) (canonical log, R8-H92/H97)
- **Deterministic transforms still produce checkpoint-idiosyncratic subset swings** - the H119 numeric-separator transform (zero read-side variance) swung tatqa +0.0448 / +0.0012 / -0.0142 / -0.0227 across four frozen checkpoints; "what each checkpoint does with the surface gap is idiosyncratic to its weights" (canonical log, R12-H119)
- **Cross-checkpoint error overlap bounds the seed-idiosyncratic share** - error Jaccard 0.43 across five checkpoints (within-seed 0.47) on hagrid+emanual; 16+16 items erred by all five - a large checkpoint-stable error core coexists with the swingy tail (canonical log, R17-H147)

## Tensions the banked numbers surface for H151

- **tatqa breaks the window-count prediction** - largest twin swing (-0.0760) on a shallow subset (4.15 windows/sentence, 7.8 pairs/response); H119 already showed tatqa is checkpoint-idiosyncratic under deterministic reads - H151a's flip-rate decomposition must separate argmax flips from score-level drift here specifically
- **pubmedqa's swing cannot be within-document window flips** - its windowed read equals its truncated read on the twin (whole-chunk windows, zero truncation exposure since H101); its -0.0452 must be chunk-argmax flips or score drift - the reflection's "two deepest-document subsets" framing pairs it with techqa imprecisely (156 genuine-window pairs vs 26 whole-chunk pairs)
- **The reflection's scaling claim is half-supported** - techqa (22.5 windows/sentence, the deepest genuine-window subset) swings -0.0719 as predicted; but expertqa (10.71, max 135) swings only -0.0149 while shallow tatqa/finqa swing -0.0760/-0.0336 - window count is at most one axis of the anatomy
- **Every banked pooling number prices the MEAN side, none prices the SPREAD side** - H151b's prediction (top-k cuts deep-subset seed spread >= 40% at <= 0.002 mean cost) has its cost side bounded by H141 (top-2 -0.00176, LSE T<=0.5 within +0.002, smax T0.1 +0.00025) on the truncated regime; the spread side has zero measurements until H151a's two-seed dumps land
- **The truncated-regime pooling costs may not transfer to the twin regime** - all H141/H124 numbers sit on truncated-training checkpoints whose mid-document windows were never trained on; the twin's windows are trained-on, so the max's neighbours carry real signal - softening could cost less (or help) under the windowed-MIL regime; the H151c two-read adjudication measures this directly
- **The recorded process failure this round repairs** - the H141 autopsy's max-noise-saturation identification on techqa/pubmedqa was banked 2026-08-10 and not carried into the H142-T bar-setting; the -0.01 subset floor was written against truncated-regime noise (canonical log, H142-T reflection)
